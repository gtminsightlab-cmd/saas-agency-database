# Agency Signal + Seven16 Family Hub — Backlog

Last reviewed: 2026-05-24 (**SESSION_36 — 5-arc marathon close.** Arcs in order: (1) LAUNCH_CHECKLIST §A+B+§C5+§D execution end-to-end → matrix flipped from 3/9 to **9/9 GREEN** (CRON_SECRET/Stripe webhook/Sentry token done in earlier sessions verified + Upstash signup + Turnstile signup + Supabase Auth CAPTCHA + PITR + smoke test passing + observability confirmed); (2) **CAPTCHA incident + hotfix** — sign-in lockout when CAPTCHA enabled because Session D only shipped Turnstile widget on /sign-up; hotfix `59b1360` added widget to /sign-in + /auth/forgot-password + updated LAUNCH_CHECKLIST §C with 5 new manual smoke rows; (3) **Brand consistency + theme-aware MarketingHeader** — swapped MarketingNav→MarketingHeader on 7 pages + fixed 6 inline brand strings + deleted 2 legacy components (commit `38645ec`); added theme="dark" prop to MarketingHeader + applied on /, /use-cases, /enterprise (commit `6e8b308`); (4) **Design system v1 end-to-end** — Master O provided detailed editorial-enterprise-data-platform spec with code samples; Phase A built 12 shared components (PageHero/Section/Container/CTASection + DataPanel/EditorialCard/WorkflowStrip/UseCasePlay/VerticalCard/PricingCard/StatStrip/CharterTermsPanel); Phase B refactored 8 marketing pages to use them (massive line reductions: /verticals 652→230, /enterprise 822→215, /methodology 869→220); Phase C deleted 3 unused legacy components; Master O reaction: "wow the preview looks amazing"; (5) **Seven16 Group Support widget unlock** — Master O confirmed seven16groupsupport.com built+ready, unlocked the "do not mount globally" rule; widget rewritten from null-render to live floating "💬 Chat with us" pill button + iframe panel + lazy-load + graceful fallback + CSP frame-src expanded; 3 UX iterations resolved double-X confusion, lazy-load Chromium quirk, and a setHasBeenOpened bug; (6) **Compliance pages** — Privacy + Terms with DRAFT banners (honest about counsel-review status) + functional self-serve /account/delete (soft-delete via is_active=false + audit_log entry + sign out + confirmation page) + footer "Delete my data" link. All work on `feat/design-system-v1` branch (8+ commits) → **PR #8 open** awaiting Master O Vercel-preview review + merge. Production main untouched at HEAD `1b57ee3` (SESSION_35 backfill); after merge production picks up everything. Full SESSION_36 close-out at [`docs/handoffs/SESSION_36_HANDOFF.md`](handoffs/SESSION_36_HANDOFF.md). Active arc flips to **domain cutover (directory.seven16group.com → agencysignal.co)** per Master O timing question at session close — recommended SESSION_37 first thing tomorrow morning (~30-60 min focused; Cloudflare DNS + Vercel domain + Stripe webhook re-register + 301 redirect + marketing copy scrub + family memory updates). Prior 2026-05-24 (**SESSION_35 PIVOT to charter-launch readiness (`docs/LAUNCH_CHECKLIST.md`) + family-doc backfill for 3 Master-O parallel sessions (D + E + F).** Discovered mid-SESSION_35-start that Master O had run 3 sessions today in a separate Claude Code instance that shipped Sessions D (security gates: CSP/rate-limit/Turnstile), E (data safety: Tier-1 classification + PITR runbook + scripted export), F (reliability: launch checklist + synthetic smoke test) — all squash-merged to main while my SESSION_34/35 conversation was live. None of D/E/F updated family-state docs (Rule 5 violation backfilled here). LAUNCH_CHECKLIST.md now the authoritative pre-launch doctrine: 12 dashboard actions + `npm run smoke` + ~10-min manual walkthrough → 9-row go/no-go matrix goes GREEN → charter outreach opens. Quick Search 4-tab is NOT on the matrix → parked. **BACKLOG #2 Slice 0-2 SHIPPED to feature branch `feat/quick-search-4tab` (commit `b3fccee` — SearchShell + URL state + WAI-ARIA Tab pattern + delete placeholder form, 3 files / +228/-115).** WIP-parked on origin; resume via `git checkout feat/quick-search-4tab && git rebase origin/main`. Full pivot record at [`docs/handoffs/SESSION_35_HANDOFF.md`](handoffs/SESSION_35_HANDOFF.md). Active arc flips to LAUNCH_CHECKLIST.md execution. Production HEAD `7c9202f` (Session F squash) with Vercel deploy READY on `dpl_FgDgctTxVAzeRE9jtUwCWVogwvTd`. Prior 2026-05-23 (**Sentry source-map upload VERIFIED WORKING in production — dashboard actions arc fully CLOSED.** SESSION_34 fresh-eyes pass on the SESSION_33 deferred item. Vercel MCP `get_deployment_build_logs` on latest production deploy `dpl_FgDgctTxVAzeRE9jtUwCWVogwvTd` (PR #7 squash-merge `be77c003`) found ZERO matches for `No auth token` / `Will not upload` / `Will not create release`, and POSITIVE matches for `> Uploaded files to Sentry` (2× — client + server compile), `> Release: be77c003cc3a2ed623bcec32a4ee6c279a0e3a7c`, and `Source Map Upload Report` with hundreds of `.js.map` entries. Fix was already live before SESSION_34 opened — whatever Master O changed between SESSION_33 close and SESSION_34 open took effect. All 3 dashboard actions from `MASTER_O_PENDING_DASHBOARD_ACTIONS_2026-05-22.md` now closed (CRON_SECRET + Stripe webhook + Sentry source-maps). Remaining build-log noise = 2 unrelated `@sentry/nextjs` DEPRECATION warnings (`disableLogger` + `reactComponentAnnotation` config keys to be moved under `webpack.*` paths in a future release — non-functional, safe to address opportunistically). Active arc flips to BACKLOG #2 (Quick Search 4-tab feature build, ~4–5.5 hrs); paste-ready opener at `docs/handoffs/SESSION_35_PROMPT.md`. Master O deleting 3 stale remote branches at laptop (`feat/foundations-sprint` + `feat/sentry-install` + `claude/session-30-recovery-D4xSt`, all content-merged) — verify via `git ls-remote --heads origin` at SESSION_35 open (should show only `refs/heads/main`). Full SESSION_34 close-out at [`docs/handoffs/SESSION_34_HANDOFF.md`](handoffs/SESSION_34_HANDOFF.md). Prior 2026-05-23 (**Dashboard actions arc CLOSED — 2 of 3 done cleanly + 1 deferred to 5-min fresh-eyes follow-up.** CRON_SECRET generated (browser DevTools console — PowerShell command in pending-actions doc failed on Windows PowerShell 5.1 .NET Framework 4.x; clean cross-platform fallback documented) + set in Vercel (Production+Preview scope, Sensitive). Stripe webhook discovered already wired (`Seven16-Agency-Directory` endpoint active with 18 events / 0% error rate / proven `STRIPE_WEBHOOK_SECRET` in Vercel via 200-response inference — BACKLOG / pending-actions doc was stale on this front). Sentry org token minted + Vercel env var attempted 3× but production build still emits "No auth token provided. Will not upload source maps." warning across all 8 redeploys monitored this session — non-blocking (site READY, customers unaffected, billing intact, cron will fire on next 04:00 UTC) but worth ~5 min fresh-eyes follow-up to filter env vars by Production scope + verify all 3 of `SENTRY_AUTH_TOKEN` + `SENTRY_ORG` + `SENTRY_PROJECT` exist there with non-empty values. Full close-out at [`docs/handoffs/SESSION_33_HANDOFF.md`](handoffs/SESSION_33_HANDOFF.md); pending-actions doc stamped SUPERSEDED. Production HEAD `708d3fd` with Vercel deploy READY on `dpl_6PpfA5ye4CjyyNZTkjBQ2NnJRReq` (8th of 8 redeploys this session). Prior 2026-05-22 (**3-PR merge sweep complete — PR #4 (/home v1 + epic cleanup) + PR #1 (ws@8.20.1) + PR #5 (qs@6.15.2) all SHIPPED to main.** Production HEAD `f7618aa` with Vercel deploy READY at https://directory.seven16group.com. Dependabot vulnerability count dropped 3 → 2 (ws resolved; remaining 2 — xlsx high + next/postcss moderate — documented as ACCEPT in `docs/security/dependabot-triage-2026-05-22.md`). Lint at **0 errors** (epic-level 42 → 0, -100%). **3 dashboard actions remain for Master O at laptop** (CRON_SECRET in Vercel, Stripe webhook registration, Sentry org-token rotation) — paste-ready checklist with PowerShell commands + exact URLs at `docs/handoffs/MASTER_O_PENDING_DASHBOARD_ACTIONS_2026-05-22.md`. These cannot be automated: secrets-never-in-chat standing rule + no Vercel env-var MCP + Stripe-webhook-dashboard-only memory rule + no Sentry MCP. Prior: **BACKLOG #1 SHIPPED — /home personalization v1 + post-login redirect flip.** First commit after Sessions 27-32 epic close. 2 files / +326/-58 — ~1.5 hrs vs original ~2-3 hr estimate; **6th consecutive scope-under-prompt delivery**. Replaced platform-wide KPI strip with 4 user-scoped MetricCards (Lists with updates / New agencies 7d / New contacts 30d / Total saved agencies, all RLS-scoped + clickable into /saved-lists deep-links); added NEW SignalsSection (auto-hides at zero; surfaces saved_lists WHERE has_updates=true as amber cards); added OnboardingChecklist for zero-saved-lists users (Apollo-style 3-step); demoted platform-inventory stats to a small strip below QuickActions. Flipped `app/auth/callback/route.ts` default `next` from `/build-list` → `/home`. **Smart-degradation design** = ships INDEPENDENT of cron snapshot data: today all 4 user-scoped counters render `0` truthfully (snapshots tables empty because CRON_SECRET still not set in Vercel — daily cron failing silently since SESSION_25); once CRON_SECRET is set + next 04:00 UTC cron fires, counters auto-light-up without further code changes. Lint stayed at 1 (no new; only pre-existing analytics/carriers:59 directive in untouched file). Build ✓ 16.1s. PR #4 draft open with Vercel preview READY on `ead0b71`. **Open dashboard action (Master O, ~3 min):** generate + set CRON_SECRET in Vercel production env — unblocks daily 04:00 UTC saved-list refresh cron + verifies BACKLOG #3 backend is fully live. Prior: **Sessions 27-32 internal-app redesign epic CLOSED end-to-end.** 6 of 6 slices shipped across ~14 hrs total wall (~60% under original estimate). 3 Path A descopes locked. Audit-first now load-bearing default discipline. Prior: **Session 31 COMPLETE — Data Coverage + Team chrome polish SHIPPED + missing Session 30 handoff recovered.** Sessions 27-32 epic slice 5 of 6. Recovery branch `claude/session-30-recovery-D4xSt` after the parent Session 30 conversation went silent before close-out. Two commits: `4fcaba4` writes the missing `SESSION_30_HANDOFF.md`; `597520b` ships Session 31 chrome polish — `/data-stats` Breadcrumbs + PageHeader + visible title "Data and Stats" → "Data Coverage" rename (caught a stale label like /ai-support in Session 30); `/team` Breadcrumbs + PageHeader (subtitle absorbs seat-counter + plan name + description), removed `← Back to /build-list` anti-slop link + dropped dead `owner` variable. Scope landed ~1 hr / 2 files / +29/-23 — ~80% under the SESSION_G_PROMPT estimate of 5 hrs / 15 files (4th consecutive session under estimate; pattern is now load-bearing). **Big audit finding (Path A descope):** `/methodology` (869 lines) + `/resources` (263 lines) are full marketing surfaces with hero designs, dual-rendered with `<MarketingNav>` (anonymous) or `<Sidebar>` wrap (authed). Stamping `<Breadcrumbs>` + `<PageHeader>` above an existing in-hero `<h1>` would clash with the marketing design and degrade the public-SEO surface. Same Path A pattern Session D locked for `/verticals` — preserve dual-purpose marketing surface; no app-chrome wrapper. PR #2 draft open. Lint stayed at 11 errors (no new; pre-existing `analytics/carriers/page.tsx:59` directive still queued). Build ✓ 14.0s. Prior: **Session 30 COMPLETE — Exports + Agency Search + AI Research Assistant chrome polish SHIPPED.** Sessions 27-32 epic slice 4 of 6. Scope landed ~2.5 hrs / 3 files, ~65% under SESSION_F_PROMPT estimate (6 hrs / 22 files) — audit revealed `/quick-search` is currently a placeholder form (not a 4-tab cross-entity search) and `/ai-support` already has rich functionality (KPI strip, FilterShortcuts sidebar, ParsePreview, RecentSearches). Shipped chrome alignment only: Breadcrumbs + PageHeader on /downloads, /quick-search, /ai-support; DataTable wrap on /downloads with typed `DownloadRow` interface (eliminates pre-existing `: any` cast); renamed /ai-support page title from "AI Support" → "AI Research Assistant" to match SESSION_27 sidebar rename. **Critical scope flag surfaced:** the "4-tab Agency Search (Agencies / Contacts / Carriers / Verticals)" mentioned in CMO brief 2 is a feature build, NOT a chrome polish — current /quick-search has a single placeholder form with a disabled Search button. Queued as a separate session-sized BACKLOG item. Lint stayed at 11 (no new errors; pre-existing pending). Build ✓ 7.6s. Prior: **Session 29 COMPLETE — Build Recruit List chrome polish + lint sweep SHIPPED.** Sessions 27-32 epic slice 3 of 6. Scope was 30% smaller than SESSION_E_PROMPT estimated after audit revealed `<ProgressStepper>` + `<RecordsCounter>` already exist (hardened instead of replaced); SWR fold-in deferred (no client fetcher in current architecture benefits). Shipped: (a) `<ProgressStepper>` a11y polish (nav landmark + aria-current + sr-only status); (b) Breadcrumbs added on `/build-list` + `/build-list/review` + `/build-list/download` + `/saved-lists`; (c) `<PageHeader>` chrome on all 3 build-list pages (Save + Edit Filters moved into review's PageHeader actions); (d) **removed non-functional Excel/PDF/Print SLOP buttons** from review + download tabs bar (anti-slop + D-024 — they had no onClick); (e) apply-on-touch lint sweep: defined `AgencyRpcRow` + `ContactRpcRow` + `CarrierRpcRow` typed interfaces in review/page.tsx, download/page.tsx, verticals/[slug]/page.tsx (replaces 9 `any` casts + removes 6 eslint-disable directives); fixed search-analytics/page.tsx Date.now() purity violation; fixed sign-up/form at_m cast. Lint: 20 → 11 errors net (-9; ZERO in Session 29 files). Build ✓ 9.0s. Prior: **Session D COMPLETE — SESSION_28 Path A SHIPPED.** New `/home` authed dashboard route + `VerticalOpportunityCard` primitive + `/verticals/[slug]` AppShell-wrap polish for authed users + sidebar Home link + brand link → `/home` + apply-on-touch D-024 lint cleanup (installed `eslint-plugin-react-hooks` + wired to flat config + added `.claude/**` to ignores + 3 real pre-existing lint errors fixed: `app/global-error.tsx` html-has-lang + 2× `app/sign-up/form.tsx` anchor-is-valid). MV refreshed mid-session — Texas DOI data now reflected in vertical counts (Agriculture 11,149; Transportation +37% to 1,603). Path A divergence from original SESSION_28_PROMPT slice 5 confirmed: preserved `/verticals` dual-purpose marketing surface for SEO; redesign delivered via NEW `/home` route + `/verticals/[slug]` polish instead. Lint: 42 → 20 errors net (zero in Session D files; remaining 20 = pre-existing `@typescript-eslint/no-explicit-any` rule-not-found + 1 new `react-hooks/set-state-in-effect` flag in `app/admin/verticals/[slug]/manager.tsx` surfaced by newly-installed plugin). Build smoke ✓ 12.3s; `/home` registered as dynamic function. Active arc now SESSION_29 (Build Recruit List Stepper + StickySummaryPanel + SWR client-cache fold-in) — paste-ready opener at SESSION_E_PROMPT.md. Prior: **Session C COMPLETE — Agency Signal Seven16 Group Support integration readiness SHIPPED.** 10 files / ~610 lines new: 6 `SUPPORT_*` env vars + `components/support/Seven16SupportWidget.tsx` null-render placeholder + `lib/support/{context,events}.ts` helpers + 4× `app/api/internal/support/{health,context,events,ticket-link}/route.ts` stubs + `docs/support-integration-readiness.md` full spec + CLAUDE.md awareness + cross-repo project memory at `~/.claude/projects/.../memory/project_seven16group_support_integration.md`. Architecture rule locked: "Agency Signal is support-INTEGRATABLE, not support-DEPENDENT". Lint+build smoke green (42 lint errors all pre-existing tech debt in unrelated files; my 7 new files lint clean; build succeeded 8.4s with all 4 routes registered). Active arc reverts to **Sessions 27-32 epic / SESSION_28 (Intelligence Home + Vertical Intelligence redesign)** — meaningfully stronger because `/verticals` now has real TX appointment density from Session B. SESSION_D_PROMPT.md = resume SESSION_28 per the original SESSION_28_PROMPT.md plan. Prior 2026-05-19 evening close: **Session B COMPLETE — Texas DOI 2026 appointment load LIVE.** Final state: `public.agency_carriers` 263,657 → 631,114 (+367,457 TX rows tagged source_type='state_doi_tx'); `public.agencies` 41,705 → 58,490 (+16,785 new TX agencies); `public.carriers` 1,369 → 2,007 (+638). Pillar 3 ground-truth data shipped for Texas. Load timings: bulk via Python loader 57s; Slices 4-6 ~5 min; total Session B wall ~90 min. Mid-session corrections: Gate 4c relaxed to accept 8-digit EINs (source spreadsheet stripped leading zeros, 6,975 legitimate rows recovered); appointment_status='confirmed' chosen for state-DOI rows (CHECK enum constraint); fuzzy_name_tx resolver tier skipped per conservative-fallback doctrine (52M comparisons too slow; accepted duplicate risk for ~500-1,000 overlapping agencies as future dedup). Validation: 27/367,484 rejected (0.007%, well under 1% kill-switch) — 25 Mexican border-state carriers with NULL US NAIC + 2 truly malformed EINs. Active arc now flips back to Sessions 27-32 epic with SESSION_28 (Intelligence Home + Vertical Intelligence) — meaningfully stronger because `/verticals` can now show real TX-state-resolved appointment density. SESSION_C_PROMPT.md updated to reflect Session C = resume SESSION_28. Prior Session A close: migrations 0093+0094. Prior SESSION_27 close: Foundation + Recruit Lists wrap.

> **How to use:** Read this file first on every session open (Rule 6). End every session with a Backlog edit — promote, defer, kill, or done. Each entry has enough context to act cold: what / why / file paths / next step.
>
> **Two handoff streams in this repo:**
> - `docs/handoffs/SESSION_<N>_HANDOFF.md` = **family-hub** work (pricing decisions, doctrine, cross-product migrations). Latest: `SESSION_21_PRICING_HANDOFF.md` (D-014 + D-015 locked, 2026-05-12).
> - `docs/handoffs/AGENCY_SIGNAL_SESSION_<N>_HANDOFF.md` = **Agency Signal** product work. Latest: `AGENCY_SIGNAL_SESSION_4_HANDOFF.md` (family reconciliation + `/enterprise` tier page + Next 16 hygiene, 2026-05-14). Session 5 kickoff queued at `AGENCY_SIGNAL_SESSION_5_KICKOFF.md`.
>
> Items below are tagged **[AS]** or **[HUB]** where the stream isn't obvious. Per Rule 1 the highest-numbered of *whichever stream is relevant* is current truth.

---

## Active arc

[ACTIVE] **[AS] Domain cutover — `directory.seven16group.com` → `agencysignal.co`.** Recommended SESSION_37, ~30-60 min focused. Choreography: (1) Cloudflare DNS A/CNAME records `agencysignal.co` → Vercel; (2) Vercel add `agencysignal.co` as production domain on `prj_w1SpwUzybi4hdbgHJNmMYjRLGHKV`; (3) Stripe re-register webhook endpoint URL + update STRIPE_WEBHOOK_SECRET in Vercel env vars (current endpoint bound to `directory.seven16group.com/api/stripe/webhook`); (4) Cloudflare Turnstile — already has `agencysignal.co` as hostname per Session D ✓ no change needed; (5) 301 redirect `directory.seven16group.com/*` → `agencysignal.co/*` so old links don't break; (6) scrub `directory.seven16group.com` inline mentions from marketing copy (`/charter` page + others); (7) update `.support/` knowledge base + family memory `reference_directory_admin_project_paths.md`. Trigger: do BEFORE charter outreach scales beyond hand-picked cohort. **Blocked on: PR #8 merge** (do that first so we cut over a clean production main, not main + open PR).

[PENDING] **[AS] PR #8 merge — design system v1 + support widget Stage 2 + compliance pages.** Master O review of Vercel preview on `feat/design-system-v1` branch. 8+ commits. Single merge brings 4 arcs to production. Once merged, domain cutover unblocks.

[CLOSED] **[AS] LAUNCH_CHECKLIST.md execution — charter-outreach readiness gating.** Matrix flipped to **9/9 GREEN** during SESSION_36. CRON_SECRET + Stripe webhook + Sentry token (done in SESSION_33/34/35) + Upstash signup + Turnstile signup + Supabase Auth CAPTCHA + PITR + Vercel redeploy + npm run smoke + ~10-min manual walkthrough + Sentry/Better Stack observability all verified. Charter outreach unblocked from the launch-readiness side. Original locked at SESSION_35 (2026-05-24) per Master-O pivot directive after discovery of parallel Sessions D + E + F (security gates / data safety / reliability). Single source of truth: [`docs/LAUNCH_CHECKLIST.md`](LAUNCH_CHECKLIST.md). Original entry below for audit:

[CLOSED — SESSION_36] **[AS] LAUNCH_CHECKLIST.md execution — charter-outreach readiness gating.** Locked at SESSION_35 (2026-05-24) per Master-O pivot directive after discovery of parallel Sessions D + E + F (security gates / data safety / reliability). Single source of truth: [`docs/LAUNCH_CHECKLIST.md`](LAUNCH_CHECKLIST.md). **9-row go/no-go matrix** — charter outreach opens ONLY when every row is GREEN. Currently **3 of 9 GREEN** (`.support/` knowledge done in Sessions A+C / voice+content done in Session B / vendor-naming P12 doctrine done) + **6 rows pending** behind 12 Master-O dashboard actions (~30 min total) + `npm run smoke` gate + ~10-min manual walkthrough.

**12 dashboard actions** broken into 4 sub-sections:
- **§A1 (4 items)** — Vercel env vars: CRON_SECRET + Stripe webhook endpoint+secret + Sentry token verify
- **§A2 (5 items)** — Security gates: Upstash signup + 2 env vars + Cloudflare Turnstile signup + 1 env var + Supabase Auth CAPTCHA
- **§A3 (2 items)** — Data safety: Supabase Pro confirm + PITR enable
- **§A4 (1 item)** — Trigger Vercel rebuild after env-var changes

**Recommended order (critical-path):** Upstash signup first (5 downstream items depend on its URL+token) → Turnstile signup → Supabase PITR (independent) → Vercel env var pastes (parallel after secrets land in clipboard) → trigger redeploy → `npm run smoke` against production → ~10-min manual walkthrough → matrix flips to GO.

**Claude verification surface:** Vercel MCP for env-var presence + build logs; Stripe MCP for webhook delivery; Supabase MCP for CAPTCHA flag + project tier + PITR availability. Upstash + Cloudflare Turnstile dashboards have no MCP → Master O at laptop with secrets-never-in-chat (clipboard → dashboard).

[PARKED] **[AS] BACKLOG #2 — Quick Search 4-tab feature build (Slices 3-9 remaining).** Slices 0-2 SHIPPED to `feat/quick-search-4tab` (origin), commit `b3fccee`. Resume kit: branch + [`docs/roadmaps/BACKLOG_2_QUICK_SEARCH_4TAB_PLAN.md`](roadmaps/BACKLOG_2_QUICK_SEARCH_4TAB_PLAN.md) + [`docs/handoffs/SESSION_35_PROMPT.md`](handoffs/SESSION_35_PROMPT.md). Reactivation trigger: charter outreach is live + revenue beginning to surface (then BACKLOG #2's "demo-able feature" value materializes). Estimated remaining: ~3-4 hrs (Agencies/Contacts/Carriers/Verticals tabs + D-024 DoD walk + final commit).

[CLOSED] **[AS] Sessions 27-32 — Internal app redesign epic — CLOSED end-to-end 2026-05-22.** Per Master O CMO brief 2 (pasted at SESSION_26 close 2026-05-18). **6 of 6 slices SHIPPED.** ~14 hrs total wall time vs original ~30-40 hr estimate (~60% under). Lint debt across the epic: 42 → 1 (-97%). 3 Path A descopes locked. Audit-first delivered every slice well under prompt estimate (28: -30%, 29: -30%, 30: -65%, 31: -80%, 32: -85%) — pattern is now load-bearing default discipline for this codebase. Full per-slice summary in [`SESSION_32_HANDOFF.md`](handoffs/SESSION_32_HANDOFF.md) close summary table. **Active arc flips back to backlog queue — Master O picks next.**

[available] **[AS] Active arc slot — open for next pick.** Sessions 27-32 epic closed. Next move options (priority order in Queued section below): #1 Session 29.5 `/home` personalization v1 + post-login redirect flip (~2-3 hrs), #2 Quick Search 4-tab feature build (~6-8 hrs), or pivot to first-paying-customer Stripe sandbox→live cutover when revenue surfaces (trigger-based, not session-blocked).

[completed-this-session] **[AS] State DOI appointment data load — Sessions A + B (~5 hrs total, single conversation 2026-05-19).** Pivot from SESSION_28 when Master O surfaced a Texas DOI appointment CSV (367k rows / 937 distinct carriers / regulatory ground-truth for Pillar 3 per D-023). Schema (migrations 0093+0094+0095) + ETL infrastructure (SECURITY DEFINER RPC bridge + Python loader) + carrier-side NAIC backfill + actual bulk load + validation + resolver + promotion all shipped end-to-end. Final: `agency_carriers` +367,457 TX rows; `agencies` +16,785 new TX; `carriers` +638. Loader ran 367k rows in 57s. Validation 27/367k rejected (0.007%). See Done log entry below for full detail.

**✅ Session A SHIPPED 2026-05-19 (~3 hrs).** Schema prep:
- Migration 0093 — `agencies.npn` + `agencies.ein` (non-unique B-tree indexes); `agency_carriers.appointment_active_date` + `agency_carriers.state_filed` + `agency_carriers.source_year`; composite index `agency_carriers(agency_id, carrier_id, state_filed)`.
- Migration 0094 — new schema `ax_staging` with 4 tables (`appointment_loads` ingest-run ledger, `appointment_load_pages` per-page checkpoint, `appointments_raw` landing zone with raw_json, `appointment_rows_rejected` validation fail bucket). RLS forced on all 4. Service-role only. NOT exposed via PostgREST.
- D-025 logged + `source_type` taxonomy locked at `docs/context/SOURCE_TYPE_TAXONOMY.md`.
- NAIC mapping CSV at `tmp/texas_naic_mapping.csv` (937 distinct Texas carriers → 300 high-confidence map / 301 review / 336 net-new). Master O spot-check before Session B start.
- Zero new advisor findings from 0093/0094.

**✅ Session B SHIPPED 2026-05-19 (~2 hrs).** Carrier-side prep (300 UPDATE + 638 INSERT carriers) → ingest run opened → migration 0095 (RPC bridge) → Python loader bulk-loaded 367,484 rows in 57s → validation gates (Gate 4c relaxed mid-flight for 8-digit EINs) → resolver (tiers a/b/c skipped since existing agencies had NULL NPN/EIN; tier d skipped per conservative-fallback; tier e created 16,785 new TX agencies) → promoted 367,457 rows to `public.agency_carriers` tagged `source_type='state_doi_tx'`. Spot-check passed (NPN 2331759 → "DAY, DEADRICK & MARSHALL, INC." with 5 carrier appointments). Full detail in [`SESSION_B_HANDOFF.md`](handoffs/SESSION_B_HANDOFF.md).

**Resolution flags locked at SESSION_26 close:**
- Product name = **Agency Signal** (D-004 holds; Seven16 Group = holding co owning AS + BindLab + DotIntel)
- Nav rename = **labels only**, keep existing URLs (Saved Lists → Recruit Lists, Downloads → Exports, Quick Search → Agency Search, Data and Stats → Data Coverage, AI Support → AI Research Assistant). DB tables stay `saved_lists`. Stripe webhook URLs untouched. Pillar 6 Edge Function URLs untouched.
- Tailwind palette = **REPLACE `brand-*` with #0F766E** (teal-700 anchor) + add new tokens `intel-cyan` (#0891B2) + `intel-dark` (#07111F). Visual ripple on every existing brand-styled CTA. Marketing homepage uses Tailwind `blue-600` so it's insulated.
- Session 27 wraps **Recruit Lists** (Saved Lists rename) as the proof-of-shell page.

**6-session structure (6 of 6 SHIPPED ✓ — epic CLOSED):**
- ✅ **Session 27 — Foundation + Recruit Lists wrap** SHIPPED 2026-05-19 (commit `0a8b2dc`, deploy `dpl_8SRxpyK5q3kwXVBnUzpmM5zdBn8s` READY). 10 files / 591/245. Live verified end-to-end (delete flow + sonner toast + KPI recompute). Notable scope adjustments vs original plan: StatusChip skipped (reused existing `components/ui/StatusPill` with 6 tones covering app-surface semantics); mobile sidebar drawer deferred to future session (mobile keeps existing sign-out strip; sidebar `hidden md:flex`).
- **Session 28 — Intelligence Home + Vertical Intelligence redesign** (~6 hrs, ~18 files). **NEW ACTIVE HEAD.** Scope: NEW `/home` authenticated dashboard route (KPI strip + recommended plays + recent activity + quick actions) + Vertical Intelligence (`/verticals`) redesign with `VerticalOpportunityCard` (live data from `mv_vertical_summary`) + `/verticals/[slug]` detail polish + add Home link to sidebar (now that route exists) + apply-on-touch D-024 cleanup on jsx-a11y errors flagged in SESSION_26 (`app/sign-up/form.tsx` + `app/global-error.tsx`). Paste-ready opener at [`SESSION_28_PROMPT.md`](handoffs/SESSION_28_PROMPT.md).
- Session 29 — Build Recruit List (Stepper + StickySummaryPanel) + Recruit Lists table polish + SWR client-cache fold-in (~6 hrs, ~20 files)
- Session 30 — Exports + Agency Search (4 tabs) + AI Research Assistant (~6 hrs, ~22 files)
- Session 31 — Data Coverage + Methodology + Resources + Team (~5 hrs, ~15 files)
- Session 32 — Admin polish: AdminSidebar (dark) + Master Control Room + System Health + Billing + Integrations (~6 hrs, ~25 files)

*Why active:* Master O CMO brief 2 pasted at SESSION_26 close 2026-05-18 — directive to bring the authenticated app surface up to the same intelligence-workspace bar as the marketing homepage. First major application of D-024 to internal product pages. Supersedes SESSION_5 Option A / Option B / Option C deferred-priority items below; SWR cache work folds into Sessions 28-30 as page redesigns happen.

[deferred-priority] **[AS Session 5] Option A — SWR client-cache on `/build-list` + `/saved-lists` (~2–2.5 hrs).**
Install `swr` (~5KB), wrap data loaders in `useSWR`, verify revalidation on focus + manual refresh, DOM only re-renders on diff. Pairs with the server-side `unstable_cache` shipped in AS Session 3 — closest user-facing match to "only new data loads on refresh." Files in scope: `package.json` / `package-lock.json`, `app/build-list/page.tsx`, `app/saved-lists/page.tsx`, possibly a small client wrapper if hooks need extraction.
*Why deferred:* Internal app redesign epic (Sessions 27-32) supersedes priority; SWR client-cache wiring is most naturally folded into Session 29 Build Recruit List redesign rather than shipped alone.

---

## Queued (priority order — pick the top one when active arc closes)

1. ~~**[AS] Session 29.5 — `/home` personalization v1 + flip post-login default**~~ → **SHIPPED 2026-05-22.** See [`SESSION_32_HANDOFF.md`](handoffs/SESSION_32_HANDOFF.md) + commit `ead0b71` + PR #4. 2 files / +326/-58. Smart-degradation design ships independent of cron snapshot data; counters auto-light-up post-CRON_SECRET without further code change. **Done.**

2. **[AS] Quick Search 4-tab feature build (~6-8 hrs, ~10-15 files).** [QUEUED 2026-05-20 after Session 30 audit revealed scope.] Current `/quick-search` is a placeholder form with a disabled Search button (gated until contact records are fully loaded). CMO brief 2 calls for a 4-tab cross-entity search: Agencies / Contacts / Carriers / Verticals. Real build = (a) client-side typing → debounced query → live results; (b) SWR client-cache fold-in (most natural insertion point for the AS Session 5 Option A deferred item); (c) per-tab sort + filter state in URL; (d) DataTable chrome on each tab's results; (e) RPC endpoints for each entity-type search (some already exist: `search_agencies_for_filters`, `search_contacts_for_filters`, `list_carriers_with_appointments`; verticals can use `mv_vertical_summary` directly); (f) deep-link support for cross-entity search results. Sequencing recommendation: queue after Session 32 epic closes — chrome epic finishes first, then dedicated product feature build with a clean slate. Or jump the queue if customer demos hinge on agency search functionality. Confidence: 80% on scope; the unknowns are pagination patterns + how/whether to surface cross-entity results in a single unified view vs. strict per-tab.
   *Source:* Session 30 audit — SESSION_F_PROMPT.md scope flag.

2.5. ~~**[AS] Refactor `app/admin/verticals/[slug]/manager.tsx` — eliminate `react-hooks/set-state-in-effect` violation**~~ → **SHIPPED 2026-05-19** (Session D close cleanup pass). Moved debounced carrier-search out of `useEffect` into an `onChange` event handler per react.dev/learn/you-might-not-need-an-effect; added a render-time `filteredResults` useMemo to drop already-mapped carriers (replaces the prior fetch-time filter, which was the reason the effect depended on `mappedIds` — that dep is now gone). Kept one cleanup-only effect to clear the pending debounce timer on unmount (no setState inside; safe). Removed the now-redundant `setResults` filter in `addCarrier` since `filteredResults` handles it at render. Lint: 20 → 19 errors (set-state-in-effect eliminated; remaining 19 are pre-existing `@typescript-eslint/no-explicit-any` rule-not-found in unrelated files). Build ✓ 14.8s.
   *Source:* Session D lint pass — flag #2. **Done.**

3. **[AS] Saved-lists update-detection backend** ~~(~4–5 hrs)~~ → **LIKELY SHIPPED Session 25 per family MEMORY index** (Pillar 6 Edge Function `recompute-saved-lists` v1 ACTIVE + Vercel cron + API routes + UI). Original ask: UI ready since AS Session 3 commit `e1bfc89` (row tint on `has_updates=true`); migration `0091_saved_list_snapshots.sql` + Vercel Cron → Edge Function recompute job + `markSavedListAcknowledged` server action + "Download Updates" delta button. **Action:** verify shipped state vs. this item before picking up; if confirmed shipped, move to Done with Session 25 date. Item #1 above assumes this backend is live (uses `saved_list_snapshots` + `saved_list_changes` for the personalization counters).
   *Source:* `AGENCY_SIGNAL_SESSION_4_HANDOFF.md` §5 #2 + §6 Option B; MEMORY index entry.

4. **[AS] Per-user data cache via `@vercel/kv` or Upstash Redis (~half day).** Cache `app_users` + `v_my_entitlement` per-user with short TTL — saves ~200ms/authed-page render. Free tier covers current scale. Needs invalidation strategy on user settings changes. Lower urgency than items 0–1 above; consider when scaling pressure becomes real.

5. **[HUB] Cascade WORKING_AGREEMENT.md Rule 2 + Rule 5 amendments to `dotintel2`** (~5 min, dotintel2 only since seven16-distribution spun out per D-022). SESSION_24 amended two rules in saas-agency-database only:
   - **Rule 2(b)** — new cross-repo prep artifact exception. A session in Repo A may produce paste-ready prep artifacts (next-session prompts, draft scripts, design layouts) for Repo B's next session under 4 clauses (no file writes / commits / migrations in Repo B; no active Repo B session running).
   - **Rule 5 step 1** — new "if material, refresh `FAMILY_HEALTH.md` at session close" sub-bullet per ANTI_DECAY_PROTOCOL Mechanism #1.

   Drop into dotintel2's next session as a 5-min open-the-session edit. Until cascaded: saas-agency-database family-hub sessions handle FAMILY_HEALTH refresh on dotintel2's behalf.

   *Source:* `WORKING_AGREEMENT.md` §Rule-2 + §Rule-5 footer note + SESSION_24_HANDOFF.md.

6. **[HUB] TIQ wind-down session** (~30 min, do when bandwidth allows). Per D-022. Tasks: delete Vercel project `prj_c6kzFEhpw6Uwb12TECUidKlBxOwr`, archive (don't delete) GitHub `gtminsightlab-cmd/seven16-distribution` repo, pause or delete Supabase project `yyuchyzmzzwbfoovsskz`, decide Cloudflare DNS fate for `thresholdiq.io`, clean any TIQ-tagged Stripe sandbox products, delete Desktop launcher `Open Claude — Threshold IQ.bat`. Not blocking anything; not urgent. Recommend archive over delete on GitHub — preserves git history if Master O wants to reference TIQ patterns for the separate CRM/AMS he's building outside the family.

7. **[HUB] Schema migration: `customer_entitlements` + `appointment_attributions` in `seven16-platform` satellite (`soqqmkfasufusoxxoqzx`).** State-level RLS enforcement + outcome SKU attribution tracking per D-015 §7. Required before Stripe catalog setup can wire end-to-end. *Source:* [`docs/context/PRICING_ENTERPRISE_LAYER.md`](context/PRICING_ENTERPRISE_LAYER.md) §11.3.

8. **[HUB] Schema migration: `credit_wallets`, `credit_ledger`, `credit_consumption_rates` in `seven16group` satellite (`sdlsdovuljuymgymarou`).** Per D-014. **Verify first:** scaffolding may exist — `credit_wallets` + `credit_ledger` showed up in `list_tables` during session 21; state unconfirmed. Run advisors after DDL.

9. ~~**[HUB] Stripe product catalog setup**~~ → **SHIPPED 2026-05-18.** Universal Credits + 6 DOT Alerts tiers + 4 Directory listings + Full Database SKU + Learning Center 6 pack tiers + Charter Member coupon (`L1Ngigfc`, 25% off forever) all created in sandbox `acct_1TLUF6HmqSDkUoqw`. 5 obsolete pre-D-021 products archived. Canonical SKU map at [`docs/context/PRICING_STRIPE_CATALOG.md`](context/PRICING_STRIPE_CATALOG.md). Still pending: Enterprise+ state SKUs (D-015 ~50 state SKUs + Distribution Suite bundle + Distribution+ outcome SKU) — its own session per D-015 complexity. Webhook event registration (Master O dashboard action, 5 events: checkout.session.completed / customer.subscription.created/updated/deleted / invoice.paid + invoice.payment_failed).

10. **[HUB] 5–8 Distribution Expander demos.** Pressure-test the $12,500 ceiling and the MI-is-Tier-1 / TX-is-Tier-2 / NH-is-Tier-3 surprises against live reactions before publishing prices broadly. ICP = VPs of Distribution at MGAs/wholesalers/carriers (Neilson-replacement buyers). `/enterprise` page now exists to drive the demo conversation.

11. **[AS] Wire `revalidateVerticalsRefs()` into admin "Refresh verticals" button.** Server action exists but no admin UI calls it. Needs a button that runs `REFRESH MATERIALIZED VIEW mv_vertical_summary` + invokes the revalidation.

12. **[AS] RLS advisory cleanup — `public._trucking_load_log` in `sdlsdovuljuymgymarou`.** RLS disabled (flagged session 21). Decision: enable + add super-admin-only policy, OR rename to confirm internal-only. Quick win.

13. **[AS] Carrier-table dedupe migration.** Catalog has 4× Cincinnati variants, 3× Selective, 3× Nationwide collapsing to same `normCarrier` key. First-match-wins via `Map.set` is deterministic but a future migration would tidy. Links resolve correctly today.

14. **[AS] ~150 long-tail novel carriers** in `data/_unmatched_carriers.tsv` still need `/admin/catalog` entries. Low priority but a clean polish task.

15. **[AS] Carry-from-older-sessions list (verify still needed before picking up — STATE.md §7):** contacts load for 8 session-12 vendor files, `account_type_id` backfill for 634 new agencies, WRB.xlsx vs AdList-17028.xlsx duplicate-file confirmation, MiEdge confidence-tiered fuzzy matcher, V5 parent grain remediation, retail trucking load (1,328 agents). Some of these may have been overtaken by the May AdList + DOT Intel sync work — confirm row counts before re-running.

---

## Deferred (parked, will surface on a trigger)

- **[AS] Pillar 9 — Future Market Discovery (ProgramBusiness-adjacent)** — explicitly parked per D-023 / ADR-023. Documented at [`docs/domains/domain-market-discovery.md`](domains/domain-market-discovery.md). **Trigger:** all three of (1) saved-list intelligence (Pillar 6 / BACKLOG #1) shipped + proven; (2) data trust signals visible in UI (Pillar 8 — verified badges, confidence scores, stale alerts); (3) Enterprise+ Distribution Expander demos (5–8) validate market-side demand. Until then ProgramBusiness is a benchmark, not a build target.

- **[FAMILY / AS] `/partners` stub page + Seven16 Group Partner Program integration prep** — locked 2026-05-18 family-wide doctrine ([memory: `project_seven16_partner_program.md`](C:\Users\GTMin\.claude\projects\C--Users-GTMin-Projects-saas-agency-database\memory\project_seven16_partner_program.md)). Parent partner hub at `partners.seven16group.com` not built yet. **DO NOT** build standalone referral/affiliate/commission systems inside Agency Signal. Future build items (defer until parent exists): (a) lightweight public `/partners` page explaining the program, primary CTA → `https://partners.seven16group.com/apply?product=agency_signal`; (b) sign-up `?ref=REFERRAL_CODE` param + `seven16_partner_ref` cookie + `product_key` metadata; (c) Stripe webhook forwarding of `invoice.paid` + `invoice.refunded` / `charge.dispute.*` to parent reward handler; (d) admin attribution view (read-only, no manual payouts); (e) RLS/role gates for partner demo access (seeded fake data only). Trigger to start build: parent hub exists at `partners.seven16group.com`. Cross-repo: same future-state requirements apply to DotIntel (PRODUCT_KEY=`dotintel`) and DotAgencies (PRODUCT_KEY=`dotagencies`).

- **[AS] Verified / claimed agency profile flow** — Pillar 1 + 8 cross-cut, NEW per D-023. Agencies claim and correct their own profile → inbound SEO + trust + future monetization. Tier 1.x, not active build. Schema preview in `0091_agency_signal_d023_tables_proposed.sql` (agency_profiles columns or extension table).

- **[AS] Verified / claimed producer profile flow** — Pillar 2 + 8 cross-cut, NEW per D-023. Tier 1.x. Schema preview in `0091_agency_signal_d023_tables_proposed.sql` (producer_profiles columns).

- **[AS] Producer-centric segmentation views** — Pillar 2, NEW per D-023. Existing UI is agency-first; need producer-first segmentation surfaces. Tier 1.x.

- **[AS] Confidence badges + stale alerts in UI** — Pillar 8, NEW per D-023. Surface confidence + freshness in `/agency-directory` + `/saved-lists` as product feature, not just backend plumbing. Tier 1.x.

- **[AS] Distribution Expander UX workflow build** — Pillar 7, the post-`/enterprise`-page workflow surfaces (state-targeting · vertical-segments · appointment-opportunities · export-jobs · data-quality dashboards). Trigger: schema migration for `customer_entitlements` + `appointment_attributions` lands in `seven16-platform` satellite (BACKLOG #5).

- **Stripe sandbox → production cutover** — trigger: first paying customer ready to convert. Account `acct_1TLUF6HmqSDkUoqw`, webhook endpoint `https://directory.seven16group.com/api/stripe/webhook`, events `checkout.session.completed`, `customer.subscription.updated`, `customer.subscription.deleted`, `invoice.paid`.

- **Hygiene Credit billing wiring** — trigger: Stripe production cutover above. Phased pricing: Growth Member auto-discounted 10% at month 6 + month 12 ($89.10 vs $99). Marketed as loyalty reward, NOT data-decay compensation. Mechanism: Stripe Subscription Schedule with phased pricing OR programmatic coupon at month 6+12.

- **Trust copy + Hygiene Credit rollout to live site** — trigger: data inventory catches up to copy claims (100k producers, 70% email density). Currently 41,705 agencies / 135,453 contacts (85.6% verified email). Locked language in STATE.md §8. Per DECISION_LOG §2 standing rule "pricing copy is placeholder until data inventory catches up" — consumer-tier marketing copy intentionally left stale (the `/enterprise` page uses fresh validated numbers; the homepage does not).

- **Phase 3 CMO rewrite (testimonials/customer logos)** — trigger: 2–3 paying customers.

- **A/B test sweep (3 tests)** — trigger: 500+ visitors/week.

- **Domain cutover `directory.seven16group.com` → `agencysignal.co`** — trigger: comfort with DNS choreography + a quiet week. Reserved but not wired.

- **Sprint 1C — shared JWT / Doppler / Sentry runbook across satellites** — trigger: `seven16-platform` shared-auth runbook lands. Partial credit already: Sentry is live on `directory-admin` Sentry project (AS Session 4 parallel `feat/sentry-install` merge) and Speed Insights/Analytics shipped on AS. Threshold IQ + dotintel2 join when the family runbook formalizes.

- **8 empty verticals (migration 0051)** — trigger: data + carrier mapping per vertical defined.

- **First Light + Maximum account_type reclassification** — trigger: bandwidth. Cosmetic, carry from session 11.

- **Pre-existing advisor cleanup (84 SECURITY DEFINER warnings, ext-in-public, pg_trgm-in-public, mv_vertical_summary-API-readable)** — trigger: bandwidth. Pre-existing, not session-introduced. Item 8 above (`_trucking_load_log`) is the only one worth fixing now.

- **xlsx (SheetJS) prototype-pollution + ReDoS** — no upstream patch. Used only in `scripts/`. Migrate to `exceljs` someday.

- **Contacts dedup edge case** (+16,273 contacts on AdList re-run in AS Session 2). Suggests NULL handling in `keyOf()` lets some duplicates through. Not blocking.

- **Pre-launch security gates (cross-product)** — trigger: any "launch," "tell my network," "we're going live with X." Memory `project_pre_launch_security_gates.md`. Mandatory pre-launch upgrades: GitHub Advanced Security ($49/u/mo for private-repo secret scanning — N/A here since this repo is public), Upstash rate limiting, PITR on satellites holding customer data ($100/mo), CSP headers, Better Stack uptime ($25/mo).

- **UI refresh by brand-systems designer (cross-repo, family-wide)** — trigger: designer engaged + brand system delivered. Sequenced AFTER seven16group.com apex build (which defines the brand). AI-generated UI smell across the family is a credibility tax; a polished, distinctive design system is a credibility multiplier worth ~$5–15k freelance investment. Deliverables expected: Figma design system + Tailwind tokens + component library + brand voice guidelines. Cascading rollout per product, not big-bang. See family memory `reference_seven16group_apex_plan.md` + `feedback_no_slop_in_copy.md`.

- **seven16group.com apex build (new repo + Vercel project)** — trigger: brand-systems designer delivered + services-arm scope locked (D-XXX) + bundle-location decision (D-XXX). Holding-company + marketing-agency front door. Architecturally separate from this repo. See family memory `reference_seven16group_apex_plan.md`.

---

## Killed (with reason + date)

*(empty — nothing killed yet under this system)*

---

## Done (audit trail, newest first)

- [2026-05-24] **SESSION_36 — 5-arc marathon: LAUNCH_CHECKLIST 9/9 + CAPTCHA hotfix + brand consistency + theme-aware header + design system v1 + support widget unlock + compliance pages.** Longest single session in this repo's history by arc count. Started continuous with SESSION_35 close (same conversation; arcs cleanly separated). **Arc 1 — LAUNCH_CHECKLIST §A+B+§C5+§D end-to-end:** Master O executed 12 dashboard actions (Upstash + Turnstile + Supabase Auth CAPTCHA + PITR) with Claude verifying via MCP after each; `vercel redeploy` fired; `npm run smoke` against production passed 19/19; Tier-1 baseline export ran clean (1,581 rows / 20 tables / 0 errors); Sentry + Better Stack verified. **9-row go/no-go matrix flipped from 3/9 to 9/9 GREEN.** Charter outreach unblocked from launch-readiness side. **Arc 2 — CAPTCHA incident + hotfix (commit `59b1360`):** sign-in lockout immediately after Master O enabled Supabase Auth CAPTCHA because Session D only shipped Turnstile on /sign-up. Rollback (30 sec dashboard toggle) restored sign-in. Hotfix added Turnstile widget + captchaToken handling to /sign-in + /auth/forgot-password (mirroring sign-up pattern); /auth/reset-password skipped (uses session not CAPTCHA-gated). Updated LAUNCH_CHECKLIST.md §C with 5 new manual smoke rows so the gap can't reappear. **Arc 3 — Brand consistency + theme-aware MarketingHeader (commits `38645ec` + `6e8b308`):** swapped MarketingNav→MarketingHeader on 7 pages + fixed 6 inline brand strings + deleted 2 legacy components → all 10 marketing surfaces report "Agency Signal: 1x | Seven16 Agency Directory: 0x"; extended rate-limit to 2 more endpoints; theme="dark" prop on MarketingHeader + applied to /, /use-cases, /enterprise eliminating white-header-on-dark-hero stitching. **Arc 4 — Design system v1 (commits `4ce418d` + `2818b06`):** Master O provided detailed editorial-enterprise-data-platform spec with 6 code samples. **Phase A** built 12 shared components: 4 layout (PageHero, Section, Container, CTASection) + 8 marketing (DataPanel, EditorialCard, WorkflowStrip, UseCasePlay, VerticalCard, PricingCard, StatStrip, CharterTermsPanel). **Phase B** refactored 8 marketing pages: /verticals (652→230), /use-cases (restructured), /charter (434→285), /resources (263→200), /enterprise (822→215), /methodology (869→220), homepage (preserved editorial richness per "homepage already strongest" feedback), /pricing (NEW placeholder for dedicated session). **Phase C** deleted 3 unused legacy components (HeroSection, PricingPreview, FinalCTA). Master O reaction after viewing Vercel preview: "wow the preview looks amazing." **Arc 5 — Seven16 Group Support widget unlock (commits `2b51b41` + `20c72a8` + `44d211a` + `5f5a6c6`):** Master O confirmed seven16groupsupport.com built+ready; doctrine "support-INTEGRATABLE not support-DEPENDENT" preserved but "do not mount globally" rule UNLOCKED. Widget rewritten from Stage-1 null-render placeholder to Stage 2 live floating "💬 Chat with us" pill button + 380x600 panel + iframe loading `seven16groupsupport.com/support/agencysignal` + lazy mount + graceful mailto fallback. CSP `frame-src` expanded. 3 UX iterations fixed: double-X confusion (button hides when panel open) + Chromium iframe lazy-load quirk in position:fixed containers (removed loading="lazy") + a setHasBeenOpened bug where setState nested in setOpen updater wasn't committing reliably (separated into independent calls). **Arc 6 — Compliance pages (commit `2818b06`):** /privacy + /terms with DRAFT banners (honest about counsel-review status; covers data collection, third-party processors, GDPR/CCPA rights, retention) + functional /account/delete page with type-DELETE-to-confirm pattern + server action that soft-deletes via app_users.is_active=false, logs to audit_log (schema verified via Supabase MCP before commit — caught bug where I'd assumed target_user_id column existed), signs out, redirects to confirmation page. Footer updated with Delete-my-data link. **All work on `feat/design-system-v1` branch (8+ commits) → PR #8 open** awaiting Master O Vercel-preview review + merge. Production main untouched at HEAD `1b57ee3` (SESSION_35 backfill). Once merged, production picks up new design system + support widget + compliance pages on every marketing surface. Full close-out at [`docs/handoffs/SESSION_36_HANDOFF.md`](handoffs/SESSION_36_HANDOFF.md). **Active arc flips to domain cutover** (`directory.seven16group.com` → `agencysignal.co`) for SESSION_37 first thing tomorrow morning per Master O timing question; do BEFORE charter outreach scales beyond hand-picked cohort.

- [2026-05-24] **SESSION_35 — Family-doc backfill + BACKLOG #2 Slices 0-2 PARKED + pivot to LAUNCH_CHECKLIST.md.** Opened per [`SESSION_35_PROMPT.md`](handoffs/SESSION_35_PROMPT.md) to start BACKLOG #2 (Quick Search 4-tab). Shipped Slices 0-2 cleanly: NEW `app/quick-search/search-shell.tsx` (~190 lines client component with full WAI-ARIA Tab pattern + URL state via useSearchParams + sticky search bar + ⌘K/Ctrl+K focus + 300ms debounce + render-prop ctx); modified `app/quick-search/page.tsx` (replaces QuickSearchForm with SearchShell, RecordsCounter via beforeTabs slot); deleted `app/quick-search/form.tsx` (placeholder). Then audit revealed Master O had run **3 parallel Claude sessions today (D + E + F)** in a separate instance — all squash-merged to main while my conversation was live. The parallel work produced `docs/LAUNCH_CHECKLIST.md` — new authoritative pre-launch doctrine that supersedes BACKLOG #2 as the next-to-revenue arc. **Pivot decision (confirmed by Master O): pivot to LAUNCH_CHECKLIST.md execution.** Branch surgery: stashed Slices 0-2 → hard-reset `feat/quick-search-4tab` from stale-base `f5895b6` onto current `origin/main` `7c9202f` → popped stash → dropped redundant `package-lock.json` delta (swr already in Session E) → committed `b3fccee` "wip(quick-search): Slice 0-2 — SearchShell + URL state + delete placeholder" → pushed branch to origin. WIP preserved on remote; resume via `git checkout feat/quick-search-4tab && git rebase origin/main`. **Family-doc backfill on main (this commit):** SESSION_35_HANDOFF.md + 4 new Done log entries (3 for D/E/F + this one) + Active Arc flipped + Items Requiring Attention restructured around the 12 LAUNCH_CHECKLIST actions. Full pivot record + Rule 2 collision postmortem at [`docs/handoffs/SESSION_35_HANDOFF.md`](handoffs/SESSION_35_HANDOFF.md). Pickup task: LAUNCH_CHECKLIST.md §A — start with Upstash signup (5 downstream items depend on its URL+token).

- [2026-05-23] **Session F SHIPPED (Master O parallel session, commit `7c9202f` squash from `f5895b6` PR) — Reliability: launch checklist + synthetic smoke test.** Closes the 3-session charter-launch readiness sprint (D + E + F) by consolidating every dashboard action from D + E + the pre-existing pending items into ONE authoritative checklist. **NEW [`docs/LAUNCH_CHECKLIST.md`](LAUNCH_CHECKLIST.md)** — 12 Master-O dashboard actions (~30 min) broken into A1 Vercel env vars / A2 Security gates (Upstash + Turnstile + Supabase Auth CAPTCHA) / A3 Data safety (Pro + PITR) / A4 trigger redeploy, plus `npm run smoke` (§B), manual end-to-end walkthrough (§C), observability verification (§D), final 9-row go/no-go matrix (§E), first-hour operating procedure (§F), rollback plan (§G), post-launch follow-up backlog (§H). **NEW `scripts/reliability/smoke-test.ts`** — pure fetch + tsx synthetic smoke against any `SMOKE_TARGET` URL (defaults to directory.seven16group.com); 5 check groups (public pages 200 / HTTP security headers present / CSP report endpoint 204 / API auth gates 401-400-429 / Stripe webhook unsigned 400); exits 0 on full pass; safe against production (unauthenticated only); cross-platform with no extra deps. `npm run smoke` wired into package.json. **What this Session F does NOT do:** run smoke against production (waits for post-deploy + Master O trigger); real signup smoke (manual step C); Sentry / Better Stack alert-rule configuration (Master O dashboard, verified manually per §D). Acceptance: lint clean + typecheck clean. After all 12 §A actions + §B smoke + §C manual walkthrough → §E matrix flips GO → charter outreach is live. Family-doc backfill in SESSION_35 (this entry preserves the close that Session F itself didn't write).

- [2026-05-23] **Session E SHIPPED (Master O parallel session, commit `608e135` squash from `53910e3` PR) — Data safety: Tier-1 classification + PITR runbook + scripted export.** Closes the data-safety gap before first paying-customer row writes. Customer-irreplaceable rows now have documented RTO/RPO targets, a Master-O dashboard-action checklist for PITR enablement, and a scripted defense-in-depth logical export. **NEW [`docs/DATA_SAFETY.md`](DATA_SAFETY.md)** — Tier-1 classification of 20 customer-irreplaceable tables (tenants / app_users / stripe_customers / billing_plans / user_entitlements / credit_wallets / credit_ledger / usage_logs / downloads_ledger / saved_lists + Pillar 6 snapshots/changes / distribution_expander_segments / top_agency_* / audit_log / tenant_limits / feature_flags / hygiene_events / saved_list_hygiene_flags); Tier-2 derived (agencies / contacts / carriers / agency_carriers); Tier-3 config/lookup. RTO/RPO targets: Tier-1 RPO ≤1h + RTO ≤4h via PITR + nightly logical export. 3 Master-O dashboard actions (confirm Pro plan / enable PITR 7-day retention / optional Branching). 3 recovery procedures documented (single bad migration via PITR rollback / catastrophic project loss / per-customer GDPR/termination export TBD). **NEW `scripts/data-safety/export-tier1.ts`** — scripted logical export of 20 Tier-1 tables to `./backups/<timestamp>/` with `_manifest.json` row counts; wired as `npm run export-tier1`. **NEW `scripts/data-safety/verify-critical-tables.sql`** — sanity-check SQL for post-PITR-rollback drills. Side effect: added `swr@^2.4.1` to package.json (incidental — likely added in anticipation of BACKLOG #2; turned out to make my SESSION_35 npm install a no-op duplicate). Follow-up backlog (per Master O notes): Vercel-Cron automation of the export; row-count drift alert; tenant-scoped GDPR export; quarterly recovery drill. Family-doc backfill in SESSION_35.

- [2026-05-23] **Session D SHIPPED (Master O parallel session, commit `e73eccb`) — Public-surface security gates: CSP (Report-Only) + Upstash rate-limit + Cloudflare Turnstile.** Three-pronged defense for public surfaces before charter outreach exposes them to bots/attackers. **CSP:** `next.config.mjs` now sets `Content-Security-Policy-Report-Only` header with allowed sources (`'self'` + Stripe + Cloudflare Turnstile + Sentry tunnel + Vercel Analytics + Upstash + Supabase). `'unsafe-inline'` retained for script-src + style-src as known compromise for Next 16 + Tailwind without nonce-injecting middleware (nonce upgrade tracked as follow-up). Master O flips header key from `-Report-Only` → enforcing after ~1 week of clean Sentry violation stream. **NEW `app/api/csp-report/route.ts`** — receives violation reports + forwards to Sentry tagged `source = "csp-report"`. **NEW `lib/security/rate-limit.ts`** — Upstash Redis-backed rate limiter wrapper (`@upstash/ratelimit` + `@upstash/redis` deps added). **Wrapped `app/api/team/invite/route.ts`** with `withRateLimit(11/hour/user)`. **EDIT `app/sign-up/form.tsx`** — Cloudflare Turnstile widget mounted (`@marsidev/react-turnstile` dep); submit gated until challenge passes. **NEW `docs/SECURITY.md`** — operating reference + future-CSP-enforcement path. 5 Master-O dashboard actions surfaced (consolidated into LAUNCH_CHECKLIST.md §A2): Upstash signup + 2 env vars + Turnstile signup + 1 env var + Supabase Auth CAPTCHA paste. Family-doc backfill in SESSION_35.

- [2026-05-23] **Sentry source-map upload VERIFIED WORKING in production — SESSION_34 close.** Single-task session closing the SESSION_33 deferred item. Fetched build log for latest production deploy `dpl_FgDgctTxVAzeRE9jtUwCWVogwvTd` (HEAD `be77c003` = PR #7 squash-merge) via Vercel MCP `get_deployment_build_logs`. **Failure signatures: zero matches** for `No auth token`, `Will not upload`, or `Will not create release`. **Success signatures present:** `> Uploaded files to Sentry` (×2 — client + server compile passes), `> Release: be77c003cc3a2ed623bcec32a4ee6c279a0e3a7c` (release tagged with merge SHA — error grouping in Sentry now auto-links to deploy), `Source Map Upload Report` (×2) with hundreds of `~/{chunk}.js (sourcemap at {hash}.js.map, debug id ...)` entries. Whatever Master O changed in Vercel env vars between SESSION_33 close and SESSION_34 open took effect immediately — no further dashboard action needed. **Dashboard actions arc fully closed:** CRON_SECRET ✅ (SESSION_33) + Stripe webhook ✅ (SESSION_33 pre-flight discovery — already wired) + Sentry source-maps ✅ (SESSION_34 verification). The `MASTER_O_PENDING_DASHBOARD_ACTIONS_2026-05-22.md` SUPERSEDED banner remains in place; pending-actions doctrine fully complete. **Remaining build-log noise:** 2 unrelated DEPRECATION warnings (`[@sentry/nextjs] DEPRECATION WARNING: disableLogger ...` + `reactComponentAnnotation ...` — both slated for migration to `webpack.treeshake.removeDebugLogging` + `webpack.reactComponentAnnotation` paths in a future @sentry/nextjs release). Non-functional, safe to address opportunistically next time `next.config.mjs` is edited. **3 stale remote branches** (`feat/foundations-sprint` + `feat/sentry-install` + `claude/session-30-recovery-D4xSt`, all content-merged) being deleted by Master O at laptop UI in same session window — `git ls-remote --heads origin` should show only `refs/heads/main` after. Full close-out at [`docs/handoffs/SESSION_34_HANDOFF.md`](handoffs/SESSION_34_HANDOFF.md). Next active arc = BACKLOG #2 (Quick Search 4-tab feature build); paste-ready opener at [`docs/handoffs/SESSION_35_PROMPT.md`](handoffs/SESSION_35_PROMPT.md).

- [2026-05-23] **Dashboard actions arc CLOSED — 2 of 3 done cleanly + Sentry source-map deferred to fresh-eyes follow-up.** Master O executed the 3-action checklist from [`docs/handoffs/MASTER_O_PENDING_DASHBOARD_ACTIONS_2026-05-22.md`](handoffs/MASTER_O_PENDING_DASHBOARD_ACTIONS_2026-05-22.md). **Action 1 ✅:** CRON_SECRET generated via browser DevTools console fallback (the pre-staged PowerShell command failed on Windows PowerShell 5.1 because `[RandomNumberGenerator]::Fill()` is a .NET 6+ API not available in the .NET Framework 4.x that backs Windows PowerShell 5.1; the JS one-liner `btoa(String.fromCharCode(...crypto.getRandomValues(new Uint8Array(32))))` + Chrome's `copy()` DevTools helper is the clean cross-platform fallback) → set in Vercel `CRON_SECRET` (Production+Preview, Sensitive). First proof point: 04:00 UTC cron after this session close should start populating `saved_list_snapshots` (currently 0 rows). **Action 2 ✅ (no action needed):** pre-flight discovered the Stripe webhook `Seven16-Agency-Directory` → `https://directory.seven16group.com/api/stripe/webhook` was ALREADY ACTIVE in Seven16 sandbox (acct_1TLUF6HmqSDkUoqw) with 18 events subscribed, 0% error rate, delivery activity visible in Workbench — caught BEFORE Master O clicked "Add destination" which would have created a duplicate. Webhook handler at `app/api/stripe/webhook/route.ts:84-87` returns 400 on missing/wrong `STRIPE_WEBHOOK_SECRET`; the 0% error rate is static proof the env var is already correctly wired. BACKLOG header + pending-actions doc were stale on this front. **Action 3 ⚠️ (deferred):** new Sentry Organization Auth Token minted with scopes `org:read` + `project:releases`. SENTRY_AUTH_TOKEN env var attempted 3 separate times in Vercel; production build STILL shows `[@sentry/nextjs - After Production Compile] Warning: No auth token provided. Will not upload source maps.` across all 8 production redeploys monitored this session via Vercel MCP. Most likely cause: the Production scope checkbox is not actually saving the new value, OR `SENTRY_ORG`+`SENTRY_PROJECT` (currently visible only in Development scope) also need Production scope for source-map upload to work end-to-end — the "no auth token" warning may be the first-of-many missing-config errors. **Non-blocking:** customer-facing site fully healthy across all 8 deploys (READY in 40s each); only impact is Sentry error stack traces will show minified code instead of original. Was the prior state any better? No — same warning appeared in builds going back to May 14 token creation; not a today-regression. Full close-out at [`docs/handoffs/SESSION_33_HANDOFF.md`](handoffs/SESSION_33_HANDOFF.md). Pickup task (~5 min fresh eyes): filter Vercel env vars by Production environment via "All Environments" dropdown → verify all 3 of SENTRY_AUTH_TOKEN+SENTRY_ORG+SENTRY_PROJECT exist there with non-empty values → trigger ONE redeploy → ping Claude to verify warning is gone via Vercel MCP build log fetch.

- [2026-05-22] **Dependabot ws + qs merges + lint=0 milestone.** Master O merged PR #1 (`chore(deps): bump ws from 8.20.0 to 8.20.1`, resolves the moderate ws "uninitialized memory disclosure" advisory) + PR #5 (`chore(deps): bump qs from 6.15.1 to 6.15.2`, transitive dep patch with stringify/parse bug fixes — bonus win opened by Dependabot mid-session). Both merged cleanly to production: main HEAD `f7618aa` via `363bd78` (ws merge) → `f7618aa` (qs squash merge). Vercel production deploy READY for both. **Dependabot vulnerability count dropped 3 → 2.** Remaining 2 (xlsx high + next/postcss moderate) documented as ACCEPT in [`docs/security/dependabot-triage-2026-05-22.md`](security/dependabot-triage-2026-05-22.md). Note on ws merge: the May-19 preview build had failed (pre-`.npmrc legacy-peer-deps=true` fix from SESSION_26 hotfix `f24b3f3`), but GitHub's three-way merge correctly combined the PR's lockfile delta with current main's `.npmrc`, so the merge commit's production build succeeded — preview-failed-but-merge-succeeded scenario worked as designed. **Also:** lint at 0 errors (epic-level 42 → 0, -100%) was the milestone closed during PR #4 (commit `13c9e5d`). **3 dashboard actions remain pending for Master O at laptop:** (1) CRON_SECRET in Vercel production env, (2) Stripe webhook registration at https://directory.seven16group.com/api/stripe/webhook for 6 events, (3) Sentry org token rotation. Paste-ready checklist with PowerShell commands + exact URLs at [`docs/handoffs/MASTER_O_PENDING_DASHBOARD_ACTIONS_2026-05-22.md`](handoffs/MASTER_O_PENDING_DASHBOARD_ACTIONS_2026-05-22.md). These cannot be automated by Claude — secrets-never-in-chat standing rule + no Vercel env-var MCP + Stripe-webhook-dashboard-only memory rule + no Sentry MCP available.

- [2026-05-22] **BACKLOG #1 COMPLETE — `/home` personalization v1 + post-login redirect flip SHIPPED.** First commit after Sessions 27-32 epic close. Audit-first revealed scope ~1.5 hrs / 2 files vs original ~2-3 hr / ~4 file estimate — 6th consecutive scope-under-prompt delivery. **Shipped:** (a) `<UserHeroSection>` replaces the platform-wide KPI strip above the fold with 4 user-scoped MetricCards (Lists with updates / New agencies 7d / New contacts 30d / Total saved agencies, all RLS-scoped + clickable into `/saved-lists` deep-links sorted by has_updates). (b) `<OnboardingChecklist>` — Apollo-style 3-step onboarding (Build first list → Save it → Updates land here) replaces the KPI strip when user has 0 saved_lists. (c) NEW `<SignalsSection>` — auto-hides at zero (common case until cron fires); surfaces up to 5 saved_lists WHERE has_updates=true as amber-tinted cards with "View updates" CTAs. (d) `<PlatformInventoryStrip>` — demoted from primary KPI position to compact 4-stat row below QuickActions (Agencies / Contacts / Verified appointments / Writing companies). Trust signal preserved for first-time visitors without competing for hierarchy. (e) Flipped `app/auth/callback/route.ts` default `next` from `/build-list` → `/home`. Explicit `?next=...` overrides still work. **Smart-degradation design** = redesign ships independent of cron snapshot data flowing. Today: saved_list_snapshots + saved_list_changes both empty (cron blocked on CRON_SECRET still not set in Vercel per SESSION_25 carry-forward); all 4 user-scoped counters render `0` truthfully + OnboardingChecklist surfaces correctly. Once CRON_SECRET is set + next 04:00 UTC cron fires, counters auto-light-up without further code change. **Pre-flight audit confirmed:** Pillar 6 backend code (recompute-saved-lists Edge Function writing snapshot + change diff via direct postgres.js) is fully shipped — only the cron auth secret is missing in Vercel. **Verification:** lint stayed at 1 (no new; only pre-existing analytics/carriers:59 directive in untouched file). Build ✓ 16.1s; /home + /auth/callback both register clean as dynamic functions. D-024 12-point DoD pass: 3 empty-state branches (Onboarding, no signals, no recent lists), focus-visible:ring on every Link, aria-labelledby on every section, semantic landmarks, mobile-responsive grid (grid-cols-2 sm:grid-cols-4 + onboarding sm:grid-cols-3 + signals sm:grid-cols-2 lg:grid-cols-3). Commit `ead0b71`. PR #4 draft open with Vercel preview READY. **Open dashboard action (Master O, ~3 min):** generate + set CRON_SECRET in Vercel production env. PowerShell on Windows: `$b = [byte[]]::new(32); [System.Security.Cryptography.RandomNumberGenerator]::Fill($b); [Convert]::ToBase64String($b) | Set-Clipboard` then paste into Vercel Settings → Environment Variables → Add (Sensitive, Production) → Redeploy. After this lands, BACKLOG #3 (Saved-lists update-detection backend) moves to Done as verification-only.

- [2026-05-22] **Session 32 COMPLETE — Admin polish + Sessions 27-32 epic CLOSE.** Slice 6 of 6, FINAL. Audit revealed admin surface was already at production polish quality with its own intentional dark-themed design system (`admin-*` Tailwind tokens with `intel-dark` anchor from SESSION_27, AdminSidebar + AdminTopbar + Master Control Room + System Health + Integrations + Billing all pre-existing). **Path A descope #3** — admin chrome preserved as-is; stamping customer-facing PageHeader/Breadcrumbs/AppShell would clash with established dark design. Same pattern Session D locked for `/verticals` + Session 31 for `/methodology`+`/resources`. Reframed session to polish-of-the-polish (lint debt apply-on-touch + dead-code cleanup + stale-copy fix). **Shipped:** (a) `/admin/_shell/topbar.tsx` — replaced ⌘K command-palette modal placeholder with `infoToast` hint (anti-slop: a v2 stub no longer pretends to be a real palette). Eliminates 5 jsx-a11y errors. ⌘K keystroke still fires but routes to sonner toast instead of modal. (b) `/admin/alerts/page.tsx` — Date.now() purity violation fixed via module-level `getServerNowMs()` helper (same pattern Session D + admin/search-analytics). Removed `_kept = { ShieldCheck }` silencer + unused ShieldCheck import + unused `ExportProbe` type. (c) `/admin/hygiene/email-domain-editor.tsx` — added `htmlFor` on 2 labels + dropped `autoFocus` (3 jsx-a11y errors fixed). (d) `/admin/usage/[tenantId]/limits-editor.tsx` — `htmlFor={\`${r.metric}-note\`}` + matching id on Override note label/input (1 jsx-a11y error fixed). (e) `/admin/page.tsx` — typed `VerticalSummaryRow` interface replaces `(v: any)` cast; removed `_kept = { Mail }` + unused Mail import; **updated stale "Next up:" footer copy** (previously referenced shipped modules; now points at Hygiene Credit Stripe wiring + first paying-customer cutover + outbound webhook publisher with Integrations deep-link). (f) `/admin/integrations/page.tsx` — removed `_kept = { Workflow, Building2, Server }` silencer + 3 unused imports. (g) `/admin/billing/page.tsx` — removed `_kept = { Crown }` silencer + Crown import + cleaned dead `userByWallet` for-loop block at pre-edit lines 624-631 (built a Map but never used it; comment admitted incompleteness). Scope: ~1.5 hrs / 7 files / +88/-124 — 85% under SESSION_H_PROMPT estimate. **Verification:** lint 11 → 1 errors (-10, -91%); only remaining = pre-existing `analytics/carriers/page.tsx:59` directive in untouched file. Build ✓ 14.7s; all 18 `/admin/*` routes register clean. **Sessions 27-32 epic CLOSED:** 6 sessions × ~14 hrs total wall (vs original ~30-40 hr estimate; ~60% under). Lint debt across epic: 42 → 1 (-97%). 3 Path A descopes locked (`/verticals`, `/methodology`+`/resources`, admin chrome). Active arc flips back to backlog queue — Master O picks next.

- [2026-05-22] **Session 31 COMPLETE — Data Coverage + Team chrome polish SHIPPED + missing Session 30 handoff recovered.** Sessions 27-32 epic slice 5 of 6. Recovery branch `claude/session-30-recovery-D4xSt` opened after the parent Session 30 conversation went silent before its Rule 5 close-out. **Commit 1 (`4fcaba4`):** wrote the missing `docs/handoffs/SESSION_30_HANDOFF.md` — exhaustive close-out doc (chronological log + D-024 DoD walk + skipped-scope rationale + cold-open guide). Code state from 2026-05-20 (`b5519a4` + `e23de63` + `33a17f7`) unchanged. **Commit 2 (`597520b`):** chrome polish on the two remaining bare authed surfaces, 2 files / +29/-23. (a) `/data-stats` — Breadcrumbs (Home → Data Coverage) + PageHeader (title "Data Coverage", subtitle "Row counts across every catalog and data table. Refreshed live from the directory."); **renamed visible title "Data and Stats" → "Data Coverage"** to match the SESSION_27 sidebar rename — caught the same stale-label class as /ai-support in Session 30. Kept the existing 10-tile KPI grid as-is (purpose-built inline render with brand-600 value treatment; consolidating to MetricCard would lose the color treatment + dense 3-col responsive grid). (b) `/team` — Breadcrumbs (Home → Team) + PageHeader (title "Team", subtitle absorbs seat-counter + plan-name chip + the previous inline description). **Removed the `← Back to /build-list` anti-slop link** — sidebar handles navigation now, "Back" without history context is the same UI-lie class as decorative buttons without onClick (per CLAUDE.md anti-slop rule). Dropped now-unused ArrowLeft icon import + a dead `owner` variable that was defined but never referenced anywhere in the JSX (apply-on-touch cleanup of pre-existing dead code). **Did NOT touch:** InviteForm, TeamRowActions, the members table internals, StatusPill, the 3-stat strip, or the seat RPCs (`get_my_seat_info` + `list_my_team`) — AS Session 9 / migration 0055 seat-invitation logic preserved untouched. **The big audit finding (Path A descope on /methodology + /resources):** both pages are full marketing surfaces (`/methodology` is 869 lines, `/resources` is 263 lines) with hero designs, dual-rendered using `<MarketingNav>` if anonymous or `<Sidebar>` wrap if authed (NOT `<AppShell>`). Stamping `<Breadcrumbs>` + `<PageHeader>` above the existing in-hero `<h1>` would clash with the marketing design and degrade the public-SEO surface — same Path A pattern Session D locked for `/verticals` (preserve dual-purpose marketing surface; no app-chrome wrapper). Decision logged in `597520b` commit message + carried into SESSION_31_HANDOFF. **Skipped from SESSION_G_PROMPT (with rationale):** /methodology + /resources chrome wrapper (Path A descope per above); DataTable wrap on /team members table (existing brand-styled section header + StatusPill rows already provide visual integration — same call as Session 29 /build-list/review); MetricCard consolidation on /data-stats (inline render purpose-built for dense 10-tile grid; MetricCard sized for 4-up dashboards with delta/icon/href slots that don't apply); SWR client-cache fold-in (no live client fetcher on either surface; most natural insertion remains BACKLOG #2 Quick Search 4-tab build). **Verification:** `npm run lint` stayed at 11 errors (no new; pre-existing `analytics/carriers/page.tsx:59` explicit-any directive remains queued — apply-on-touch when that file is next edited). `npm run build` ✓ Compiled successfully 14.0s; /data-stats + /team both register clean as dynamic functions. D-024 12-point DoD walk: pass on both polished surfaces. Behavior preserved end-to-end. PR #2 draft (https://github.com/gtminsightlab-cmd/saas-agency-database/pull/2) opened off the recovery branch with both commits + Vercel preview READY 38s on `4fcaba4` (Session 30 handoff) + preview build on `597520b` (Session 31 polish) at time of close. **Scope vs prompt:** ~1 hr / 2 files vs SESSION_G_PROMPT estimate of 5 hrs / 15 files = ~80% under. Fourth consecutive session where audit-first revealed less work than the original CMO brief 2 estimate (Session 28: 30% under; Session 29: 30% under; Session 30: 65% under; Session 31: ~80% under). Pattern is now load-bearing — treat SESSION_*_PROMPT.md estimates as ceilings, not floors. Next slice = SESSION_32 (Admin polish) per `SESSION_H_PROMPT.md`.

- [2026-05-20] **Session 30 COMPLETE — Exports + Agency Search + AI Research Assistant chrome polish SHIPPED.** Sessions 27-32 epic slice 4 of 6. Scope landed ~2.5 hrs / 3 files, ~65% under the SESSION_F_PROMPT estimate of 6 hrs / 22 files — third consecutive session where audit-first revealed less work than the original CMO brief 2 estimate (Session 28: 30% under; Session 29: 30% under; Session 30: 65% under). **The big audit finding:** the "4-tab Agency Search" mentioned in CMO brief 2 + SESSION_F_PROMPT is a feature build, not a chrome polish — `/quick-search` is currently a placeholder form with a disabled Search button (gated until contact records are fully loaded). Queued as a separate ~6-8 hr feature session at BACKLOG #2 with full scope (live typing → debounced query → DataTable per tab + SWR fold-in + URL-stateful sort/filter). Shipped: (a) `/downloads` — Breadcrumbs (Home → Exports) + PageHeader + DataTable chrome with proper empty-state CTA; defined typed `DownloadRow` interface eliminating the inline `: any` cast (line 43 pre-edit); added focus-visible:ring to download links + scope="col" on table headers. (b) `/quick-search` — Breadcrumbs (Home → Agency Search) + PageHeader; preserved the placeholder form + disabled Search button (honest gating, not slop); subtitle copy clarifies "Contact-record search will activate once the contact data set is fully loaded" so users know why it's disabled. (c) `/ai-support` — Breadcrumbs (Home → AI Research Assistant) + PageHeader + "Use classic Build List" link moved into PageHeader actions={}; **renamed page title "AI Support" → "AI Research Assistant"** to match the SESSION_27 sidebar rename (caught a stale label that didn't propagate). Dropped the decorative Sparkles icon on the h1 (PageHeader is more austere; the page is clearly labeled). Left KpiCard / FilterShortcuts / SearchForm / ParsePreview / RecentSearches sections in place — they're working surfaces, not D-024 violations. **Skipped from SESSION_F_PROMPT (with rationale):** the 4-tab Agency Search build (separate session — queued); SWR client-cache fold-in (no live fetcher to wrap until the 4-tab build); KpiCard → MetricCard consolidation on /ai-support (KpiCard is purpose-built for the 6-col compact layout; MetricCard sized for 4-up). Verification: lint stayed at 11 errors (no new; pre-existing `analytics/carriers/page.tsx` directive remains until that file is next touched). Build ✓ 7.6s. /downloads + /quick-search + /ai-support all register clean. Behavior preserved — form gating, AI parser flow, downloads listing, all functioning as before.

- [2026-05-20] **Session 29 COMPLETE — Build Recruit List chrome polish + apply-on-touch lint sweep SHIPPED.** Sessions 27-32 epic slice 3 of 6. Scope landed ~3 hrs / 8 files, 30% under the SESSION_E_PROMPT estimate of 6 hrs / 20 files. Audit revealed the proposed `<Stepper>` + `<StickySummaryPanel>` primitives were already in place as `components/build-list/progress-stepper.tsx` + `components/build-list/records-counter.tsx` — hardened in place instead of extracted to `components/app/*`. SWR client-cache fold-in skipped: no client-side fetcher in current `/build-list` architecture (reference data is server-side cached 1-hr; RPC calls are server-rendered). Shipped: (a) `<ProgressStepper>` a11y polish — wrapped in `<nav aria-label="Progress">`, added `aria-current="step"` on active, sr-only status text per step ("Completed"/"Current step"/"Upcoming"). (b) `<Breadcrumbs>` added on /build-list (Home → Build Recruit List), /build-list/review (... → Review), /build-list/download (Home → Recruit Lists → list name), /saved-lists (Home → Recruit Lists). (c) `<PageHeader>` chrome replaces manual h1+description on all 3 build-list pages; Save + Edit Filters CTAs moved into review's PageHeader actions={} slot. (d) **Removed non-functional Excel/PDF/Print SLOP buttons** from the brand-color tabs bar on review + download pages — they had no onClick handlers (decorative feature-lies). Real export path remains intact via the "Download & Export" sticky footer CTA on /build-list/download. Per anti-slop + D-024 standing rules. (e) **Apply-on-touch lint sweep:** defined `AgencyRpcRow` + `ContactRpcRow` typed interfaces in review/page.tsx and download/page.tsx (replaces 5+3 `as any[]` casts + removes 4 eslint-disable directives); declared `CarrierRpcRow` in verticals/[slug]/page.tsx (1 cast + 1 directive); inline `{ code: string }` typing for states-query lambda in save-button.tsx (1 cast + 1 directive); proper narrowing for `at_m` URL param in build-list/page.tsx (eliminates `as any`). Also caught a `Date.now()` purity violation in app/admin/search-analytics/page.tsx (newly-surfaced by Session D's react-hooks plugin install) — fixed via the same module-level `getServerNow()` helper pattern as `/home/page.tsx`. URLSearchParams casts in review/page.tsx (3×) simplified from `as any` to direct passing (TypeScript accepts `Record<string, string>` natively). (f) Decisions logged for the lint backlog: `app/analytics/carriers/page.tsx` explicit-any directive remains (1 file untouched this session — apply-on-touch when next edited). Lint: 20 → 11 errors net (-9; ZERO new errors introduced; ZERO in Session 29 touched files). Build ✓ 9.0s; all /build-list/* + /saved-lists + /verticals/[slug] + /admin/search-analytics routes compile clean. Behavior preserved: Build → Review → Download flow + Save modal + tab navigation + sortable headers + filter chips + soft-cap warning + entitlement gating all functioning as before. UX wins: Save button visible at top in PageHeader (used to be only at bottom of review page); broken buttons gone (less visual confusion); Breadcrumbs on every page (better orientation when entering via deep link). **Skipped from SESSION_E_PROMPT (with rationale, queued for SESSION_F if priorities flip):** generic `<Stepper>` extraction to components/app/* (no second consumer); generic `<StickySummaryPanel>` right-rail (RecordsCounter top-sticky is functionally equivalent + visually integrated with existing layout); DataTable wrap on review + download tables (visual integration with brand-color tabs bar > primitive purity); SWR client-cache (no client fetcher to wrap in current architecture — most natural insertion is when /build-list match-counts go dynamic).

- [2026-05-19] **Flag 2 — react-hooks/set-state-in-effect refactor SHIPPED** (~30 min, 1 file, post-Session-D cleanup). `app/admin/verticals/[slug]/manager.tsx` — moved debounced carrier-search out of useEffect into an onChange event handler per react.dev/learn/you-might-not-need-an-effect; added render-time `filteredResults` useMemo for the already-mapped-carrier filter (replaces the prior fetch-time filter that was the reason the effect depended on `mappedIds`); kept one cleanup-only useEffect (clears pending debounce timer on unmount; no setState inside; safe). Removed now-redundant `setResults` filter in addCarrier. Lint: 20 → 19 errors (set-state-in-effect eliminated; remaining 19 = pre-existing @typescript-eslint/no-explicit-any rule-not-found in unrelated files — out of scope, queued for whichever future session touches each file). Build ✓ 14.8s. Behavior: identical user-facing UX (debounced search, dropdown filters by mapped carriers, add/remove updates the dropdown immediately) but no longer causes cascading renders + saves a refetch round-trip on every add/remove.

- [2026-05-19] **Session D COMPLETE — SESSION_28 Path A SHIPPED.** Sessions 27-32 epic slice 2 of 6. Path A preserved `/verticals` dual-purpose marketing surface (SEO) and delivered the dashboard experience via NEW `/home` route + `/verticals/[slug]` AppShell polish for authed users — divergence from original SESSION_28_PROMPT slice 5 (chrome replacement) confirmed with Master O before writes. Shipped: (a) `app/home/page.tsx` — server-component dashboard inside `<AppShell>`, 4-card KPI strip (Total Agencies / Total Contacts / Verified Appointments / Writing Companies, live from base tables), Top Vertical Opportunities grid (top 4 by agency_count from `mv_vertical_summary`), Recent Recruit Lists table (top 5 from saved_lists, wrapped in `<DataTable>` with empty-state CTA), 4-card Quick Actions panel; (b) `components/app/vertical-opportunity-card.tsx` — new primitive composing icon + density `<StatusPill>` (Specialist hub / Strong coverage / Emerging / Niche) + 3-stat dl + CTA, plus `<VerticalOpportunityGrid>` wrapper with skeleton + empty-state handling; (c) `app/verticals/[slug]/page.tsx` — authed branch now uses `<AppShell>` + `<Breadcrumbs>` + `<PageHeader>` (replaces marketing hero) + 4-card `<MetricCard>` row from `mv_vertical_summary` + sister-product cross-ref + segment-groups grid (extracted to reusable JSX const) + `<DataTable state="empty">` fallback for 0-carriers; anonymous body preserved verbatim for SEO. (d) `components/app/sidebar.tsx` — added Home link as first Intelligence nav item; flipped brand `<Link href="/build-list">` → `/home`. (e) Mid-session: `REFRESH MATERIALIZED VIEW mv_vertical_summary` via Supabase MCP — TX DOI data now reflected (Agriculture surged to 11,149 agencies; Transportation +37% to 1,603; Real Estate jumped from previous low to 2,521). (f) Apply-on-touch D-024 lint cleanup: installed `eslint-plugin-react-hooks` dev dep + wired into flat config + added `.claude/**` to ignores list (eliminates worktree-duplicate noise that contributed ~20 of the original 42 errors); fixed 3 real pre-existing errors (`app/global-error.tsx` html-has-lang → `<html lang="en">`; `app/sign-up/form.tsx` 2× anchor-is-valid `<a href="#">` placeholders → `<span>`); suppressed 1 new `react-hooks/set-state-in-effect` flag in sign-up/form.tsx useEffect (pre-existing pattern surfaced by the newly-installed plugin) with explicit refactor-candidate comment. Verification: `npm run lint` 42 → 20 errors net (zero in Session D files; remaining 20 = 8× `@typescript-eslint/no-explicit-any` rule-not-found in build-list/analytics/verticals files + 1 new set-state-in-effect in admin/verticals manager.tsx + 11 various pre-existing); `npm run build` ✓ 12.3s; `/home` registered as dynamic function. D-024 12-point DoD walk: pass on /home + /verticals/[slug] authed branch (per-route error.tsx boundaries deferred — falls back to global-error.tsx). 7 files modified, ~770 lines added.

- [2026-05-19] **Session C COMPLETE — Agency Signal Seven16 Group Support integration readiness SHIPPED.** Stage-1 readiness scaffolding per Master O's paste-into-every-product directive (source brief: `docs/Customer Support and Sales AI Agent.txt`). 10 files / ~610 lines new — non-mounting widget placeholder, safe-context lib (allowlist-only type + sign stub), event helper (type-safe enum + fire-and-forget POST), 4× internal API route stubs (`/health` returns 200 with readiness payload, other 3 return 501 with Bearer-auth gate pre-wired), full-spec readiness doc, CLAUDE.md awareness section, cross-repo project memory. Architecture rule locked: "Agency Signal is support-INTEGRATABLE, not support-DEPENDENT" — verified by zero runtime calls to seven16groupsupport.com (which doesn't exist yet); widget renders null; events `console.debug` in dev when unconfigured; API routes don't fan out. Lint smoke: 42 pre-existing errors in unrelated files, 0 in Session C files. Build smoke: ✓ Compiled successfully 8.4s; all 4 internal/support routes registered as dynamic functions. Side-finding: `.claude/worktrees/intelligent-leavitt-9243ef/` subdirectory is being lint-scanned (eslint.config.mjs ignore list doesn't cover it) — queueable hygiene fix, not Session C scope. Active arc reverts to Sessions 27-32 epic / SESSION_28 (Intelligence Home + Vertical Intelligence redesign). SESSION_D_PROMPT.md = pointer to original SESSION_28_PROMPT.md + note that `/verticals` now has real TX appointment density.

- [2026-05-19] **Session B COMPLETE — Texas 2026 DOI appointment load LIVE end-to-end.** Final delta in `sdlsdovuljuymgymarou`: `public.agency_carriers` 263,657 → 631,114 (+367,457 TX rows tagged `source_type='state_doi_tx'`, `state_filed='TX'`, `source_year=2026`, `appointment_status='confirmed'`, `verified=true`, `lines_of_business` canonicalized per appointment_type); `public.agencies` 41,705 → 58,490 (+16,785 distinct new TX agencies with NPN+EIN populated, `profile_status='unclaimed'`); `public.carriers` 1,369 → 2,007 (+638 from Slice 1 carrier-side prep). Loader at scripts/load-tx-appointments.py pushed 367,484 rows in 57s via SECURITY DEFINER RPC bridge (migration 0095) — 74 chunks of 5k rows averaging ~0.8s each. Validation: 27/367,484 rejected (0.007%) — 25 Mexican border-state carriers with NULL US NAIC (Mapfre Mexico, Grupo Nacional Provincial, HDI Seguros, Qualitas) legitimately un-loadable through NAIC-keyed pipeline + 2 truly malformed EINs. **Mid-session corrections:** (1) Gate 4c relaxed from 9-digit-only to `^[0-9]{8,9}$` after initially rejecting 6,975 legitimate 8-digit EINs (source spreadsheet stripped leading zeros); resolver pads to 9 via `'0' || ein` at compare/insert time. (2) Resolver tiers (a) NPN+EIN exact / (b) NPN exact / (c) EIN exact all skipped — existing agencies had NULL NPN/EIN at session start; tiers would match 0. (3) Tier (d) fuzzy_name_tx skipped per conservative-fallback doctrine ([[feedback_conservative_fallback_fuzzy_match_loads]]) — 52M comparisons too slow; accepted ~500-1,000 duplicate-risk against existing 3,086 TX agencies as future dedup work. (4) `appointment_status='confirmed'` chosen because CHECK constraint requires one of `observed|confirmed|lapsed|unknown` (NOT 'active'); state DOI files are regulator-filed = confirmed. (5) `agencies.profile_status='unclaimed'` per CHECK constraint enum. Spot-check passed: NPN 2331759 = "DAY, DEADRICK & MARSHALL, INC." (TX, EIN 520800754) with 5 distinct TX carrier appointments visible (Employers Assurance, Employers Compensation, Employers Preferred, Great American, Liberty Mutual) — matches CSV row 1. Service-role key refreshed mid-session via PowerShell clipboard-paste (key value never entered chat per "Secrets never in chat" rule). Active load_id `1b83ad57-e3dc-4e50-b673-4722ac612d1c` `status='completed'`. **5 queued follow-ups:** (a) dedup pass on pre-existing 3,086 TX agencies vs the 16,785 newly-inserted; (b) NPN backfill on the original 41,705 NULL-NPN agencies via future state-DOI ingests; (c) 263,657 pre-existing source_type-NULL agency_carriers rows still need a decision; (d) NPN UNIQUE promotion verification; (e) ax_staging cleanup of forensics tables. Active arc reverts to **Sessions 27-32 epic / SESSION_28** with `/verticals` redesign meaningfully stronger via real TX appointment density.
- [2026-05-19] **Session B PARTIAL — Texas 2026 DOI appointment load (carrier-side + ETL infra SHIPPED; bulk load BLOCKED on key refresh).** Pivot from "stop after Session A" lock when Master O said "lets roll" + "keep going" + set threshold at 70-80% context. **Slice 1 (carrier-side prep):** UPDATE 300 existing carriers SET naic_code (map band, score ≥0.92) — caught a false-positive where NAIC 10946 "Arch Property Casualty" (0.930) and NAIC 20699 "Ace P&C" (1.000) both mapped to the same carrier_id; Postgres UPDATE-FROM-VALUES kept the last write (NAIC 20699), then inserted Arch as net-new to fix. INSERT 637 net-new carriers from review + create bands via 4 chunked SQL files (200 + 200 + 200 + 37). Final: 2,007 carriers (1,369 + 638), 937 with NAIC. **Slice 2 (open ingest run):** load_id `1b83ad57-e3dc-4e50-b673-4722ac612d1c` opened with source_type='state_doi_tx', source_state='TX', source_year=2026, SHA-256 captured. **Slice 3 infrastructure:** migration 0095 adds 2 SECURITY DEFINER RPCs (`public.staging_insert_appointment_rows` + `public.staging_finalize_appointment_load`) as PostgREST bridges to the locked ax_staging schema. Python loader `scripts/load-tx-appointments.py` reads CSV in 5k-row chunks + verifies SHA-256 + POSTs to RPC + handles --start-page resume + --dry-run validation. **Slices 4-6 pre-staged:** `scripts/session-c-slices-4-5-6.sql` covers 6 validation gates + 5-tier agency resolver (npn_ein_exact/npn_exact/ein_exact/fuzzy_name_tx@0.85/created_new) + promotion to public.agency_carriers tagged source_type='state_doi_tx'. Kill-switches: validation reason >1% (3,670) STOPs; created_new agencies >30k STOPs. **Blocker:** SUPABASE_SERVICE_ROLE_KEY in .env.local returns 401 (rotated again post-Session-A); MCP execute_sql cannot serve as bulk-load path (367k rows × ~250KB SQL per chunk = ~18MB total = millions of context tokens). Session C unblocker = Master O refreshes key (5 min dashboard) → runs loader (~10 min) → runs Slices 4-6 (~30 min). Detail in SESSION_B_HANDOFF.md.
- [2026-05-19] **Session A — State DOI appointment ETL schema prep SHIPPED.** Pivot from SESSION_28 UI epic after Master O surfaced a Texas DOI appointment CSV (367k rows / 937 distinct carriers — Pillar 3 regulatory ground-truth per D-023). **Migration 0093** applied via Supabase MCP — `agencies.npn` + `agencies.ein` (text, non-unique B-tree indexes WHERE NOT NULL; branches may share parent NPN); `agency_carriers.appointment_active_date` (date) + `agency_carriers.state_filed` (text two-letter USPS) + `agency_carriers.source_year` (smallint); composite index `agency_carriers(agency_id, carrier_id, state_filed)` + secondary `agency_carriers(state_filed)`. **Migration 0094** applied — new schema `ax_staging` (REVOKE from anon + authenticated; service-role only; NOT exposed via PostgREST) with 4 tables: `appointment_loads` ingest-run ledger with CHECK status (running/completed/failed/rolled_back), `appointment_load_pages` per-page checkpoint with UNIQUE(load_id, page_no), `appointments_raw` landing zone (all-text columns + raw_json jsonb + BIGSERIAL id), `appointment_rows_rejected` validation fail bucket with reason_code + raw_json. RLS enabled + forced on all 4; service_role policy each; foreign keys with ON DELETE CASCADE; indexes on load_id + npn/naic for resolver perf. **D-025 logged** to DECISION_LOG (Agency identity keyed by NPN+EIN; state DOI feeds authoritative for appointment data). **SOURCE_TYPE_TAXONOMY.md** created at `docs/context/` locking `state_doi_<state>` lowercase convention + reserved values + out-of-scope rules. **NAIC mapping CSV** at `tmp/texas_naic_mapping.csv` (NOT committed; 937 distinct Texas carriers → 300 high-confidence map ≥0.92 / 301 review 0.75-0.92 / 336 net-new <0.75) generated by `scripts/texas-naic-mapping.py` (committed; stdlib `difflib` only — no PyO3 hassle on Python 3.14). **Zero new advisor findings** from 0093/0094 verified via grep of advisor output for new column/table names. **Pre-existing 263,657 `agency_carriers` rows** have `source_type IS NULL` and ZERO have 'TX' in `states` — they predate D-025 taxonomy; backfill / replace strategy queued for Session B or later (separate decision). **Texas DOI source unknown** per Master O's "variety of sources" — likely Texas DOI public records but not vendor-bought.
- [2026-05-19] **SESSION_27 — Internal-app foundation shell + Recruit Lists wrap SHIPPED** at directory.seven16group.com. 1 commit on main: `0a8b2dc` (`feat(d-024,intel-app): foundation shell + Recruit Lists wrap (Session 27)` — 10 files / +591/-245). Tailwind palette swap: brand-* replaced with #0F766E (teal-700) anchor + full 50-900 ramp + intel-cyan (#0891B2) + intel-dark (#07111F) flat tokens. 256 brand-* occurrences across 40 files visually re-tinted. 5 new `components/app/*` primitives: PageHeader, Breadcrumbs, TopBar, MetricCard, DataTable. StatusChip skipped (reused existing `components/ui/StatusPill`). AppShell refactored (skip-to-content link + semantic main landmark + TopBar render above main). Sidebar relabeled per Master O lock: brand "Agency Signal / Distribution Intelligence"; 4-section regroup (Intelligence / Recruit Workflow / Trust & Reference / Account + super-admin Admin); 5 label renames (Saved Lists → Recruit Lists, Downloads → Exports, Quick Search → Agency Search, Data and Stats → Data Coverage, AI Support → AI Research Assistant); `aria-current="page"` on active nav. Recruit Lists page rewritten inside new chrome: PageHeader with "Build a Recruit List" CTA + 5-KPI MetricCard row (Total Lists / Agencies Saved / Contacts Saved / Ready to Export / Last Refresh) + DataTable wrapper with EmptyState fallback + StatusPill on Updates column + dead search input removed. row-actions D-024 pass: 3× alert() → sonner errorToast, successToast on delete + delta download, ErrorBoundary wrap with "Actions unavailable" fallback, Loader2 spinner during async, aria-label + focus-visible rings on every interactive element. Deploy `dpl_8SRxpyK5q3kwXVBnUzpmM5zdBn8s` READY (42s build). Live verified end-to-end (delete flow + toast + router refresh + KPI recompute 4→3 rows). Side finding: GitHub Dependabot flagged 3 pre-existing vulnerabilities (1 high, 2 moderate) on `main` push — not session-27-introduced, queue for triage.
- [2026-05-18] **SESSION_26 Path A — Marketing homepage redesign SHIPPED** at directory.seven16group.com. 2 commits on main: `b8defba` (`feat(d-024,homepage): marketing homepage overhaul per CMO brief` — 24 files / +1939/-1334) + `f24b3f3` (`fix(d-024): add .npmrc legacy-peer-deps for Vercel install` — hot-fix after first Vercel build errored on eslint-plugin-jsx-a11y@6.10.2 peer-cap). 6 D-024 shared primitives at `components/ui/` (LoadingState, EmptyState, ErrorState, SuccessToast, ErrorBoundary, StatusPill). 13 marketing section components at `components/marketing/` (MarketingHeader sticky mobile-burger, HeroSection dark-navy with live trust strip, AppointmentSearchMockup centerpiece, Problem, Comparison, HowItWorks, VerticalCardsSection consuming live `mv_vertical_summary`, RecruitPlays, Methodology with Agency Recruit Score formula visual, DataTrust 6-tile, PricingPreview 3-tier, FinalCTA, MarketingFooter). Tooling locks: `sonner` Toaster mounted in `app/layout.tsx`, `eslint-plugin-jsx-a11y` wired via flat config `eslint.config.mjs`, `@typescript-eslint/parser` for TSX parsing, `@eslint/eslintrc` FlatCompat shim, lint script changed `next lint` → `eslint .` (Next.js 16 deprecated `next lint`). Live data audit replaced brief's hardcoded numbers (36,212 / 87,000 / 400 / 60) with stronger actuals (41,700+ agencies / 135K+ contacts / 1,300+ writing companies / 260K+ verified appointments / All 50 + DC) plus STAT_FALLBACK defensive-UI constants. Vercel deploy `dpl_u5ATG82juVK1dikTz7gkhqk5HrEN` READY. **Pre-existing tech debt surfaced by jsx-a11y wire-up (apply-on-touch follow-ups):** `app/sign-up/form.tsx` 2× anchor-is-valid + 1× missing react-hooks plugin, `app/global-error.tsx` html-has-lang. **Brand naming clarified:** Agency Signal = canonical product (D-004); Seven16 Group = holding co owning Agency Signal + BindLab + DotIntel (DECISION_LOG §4 amendment queued in FAMILY_HEALTH).
- [2026-05-15] **Family-hub SESSION_24 (close)** — D-022 locked: Threshold IQ spun out of Seven16 family. Two final commits: `fe57bce` (`/charter` page rebuilt without TIQ — surfaces grid drops from 7 to 6, savings example rebuilt with DOT-heavy 15-truck scenario, baseline drops from $5,580/yr to ~$1,512/yr, anti-slop honest math) + the D-022 family-doc cleanup commit (DECISION_LOG D-022 entry, D-020 marked superseded, FAMILY_HEALTH TIQ row spun out, PRODUCT_REPO_INDEX TIQ row removed, MEMORY.md TIQ refs archived, BACKLOG TIQ cascade #3 reduced to dotintel2 only, NEW BACKLOG #4 TIQ wind-down queued, PRICING_THRESHOLD_IQ banner-archived per Rule 4).
- [2026-05-15] **Family-hub SESSION_24** — Anti-Decay Protocol Mechanism #1 shipped + Rule 2(b) cross-repo prep exception added + dotintel2 SESSION_30 prep artifact drafted under the new rule + `/charter` marketing page shipped on directory.seven16group.com + `PRODUCT_REPO_INDEX.md` canonical lookup created. Six commits: `29b3cfc` (SESSION_23 housekeeping — `.vercel` ignore + revised SESSION_24_PROMPT stricter Step 0 guard), `db9731e` (SESSION_24 body — FAMILY_HEALTH.md v1 + Rule 5 step 1 amendment + SESSION_25 prompt), `f5fc464` (tail amendment — Rule 2(b) cross-repo prep artifact exception), `822c192` (`docs/cross-repo/dotintel2_SESSION_30_ARTIFACT.md` — ~870 lines: paste-ready SESSION_30 prompt + full `csa-ingest-soda.ts` v1 draft + 2 migration drafts + testing protocol + 10 known gotchas, zero writes to dotintel2 tree), `9c0e000` (`feat(charter)` — `app/charter/page.tsx` + nav update, first live render of D-018/D-021 Charter Member offer on directory.seven16group.com, Vercel auto-deploy `READY` in 38 sec), and the post-charter index commit (`docs/context/PRODUCT_REPO_INDEX.md`).
- [2026-05-15] Backlog system bootstrapped — `docs/BACKLOG.md` + `docs/WORKING_AGREEMENT.md` seeded from AS Session 4 handoff + Session 5 kickoff + STATE.md + family memory.
- [2026-05-14] **AS Session 4** — Family DECISION_LOG reconciliation (added D-016 three-domain brand split, D-017 no source-attribution in `directory.*`, 2 new §6 standing rules — "always recommend next path as CTO/PM" + "no paid services until DOT Intel revenue"). **`/enterprise` tier page shipped** (D-015 Distribution Expander ICP, 832 lines, commit `e77c29d`). Next 16 hygiene greens (`force-dynamic` stripped from auth pages, `middleware.ts` → `proxy.ts`, commit `f52b38b`). Parallel `feat/sentry-install` session merged `899b47d` — Sentry pilot live via `@sentry/nextjs` on `directory-admin` Sentry project, tunnel route `/monitoring`.
- [2026-05-14] **Vercel Speed Insights + Analytics + security headers** merged via parallel `feat/foundations-sprint` session (`fea5b34`) — HSTS, X-CTO nosniff, X-Frame SAMEORIGIN, Referrer strict-origin-when-cross-origin, Permissions (camera/mic/geo/FLoC off), DNS prefetch on.
- [2026-05-12] **Family-hub Session 21** — D-014 (consumption engine: 50/400 + 100/600 monthly credits, $5-increment top-up slider with 0–45% bonus thresholds, hybrid expiry) + D-015 (Enterprise+ state-based slider on verified email contacts: Tier 1 $2k × 5 states, Tier 2 $1.5k × 15 states, Tier 3 $1k × 28 states, $12,500 all-50 ceiling, Distribution+ outcome SKU $300–500/qualified appointment) locked. MASTER_CONTEXT data snapshot refreshed (commits `915e4fe` / `f11287b` / `769400d`).
- [2026-05-12] **AS Session 3** — Next 14 → 16 + React 18 → 19 upgrade closing 24 CVEs, first real caching layer on `/build-list` + `/verticals` (`lib/cache/build-list-refs.ts`), Neilson UX-parity polish.
- [2026-05-09] **AS Session 2 (dedicated)** — DOT Intel → AS sync (17,638 agencies + 13,914 carrier appts + 53 UIIA affiliations) + AdList vendor load (31,746 contacts — the contact gap-fill, 3,328 agencies, 7,263 carrier appts, 1,065 affiliations, 6,807 SIC links). **100% canary scrub success across 16 patterns** — 0 live Neilson watermark hits post-load. Migrations 0084–0087.

---

## Cross-product carry-forwards (next time the relevant repo's session opens)

- **dotintel2 STATE.md still uses superseded local D-numbers** from its `SESSION_26_HANDOFF.md`. Reconciliation map locked in family `DECISION_LOG.md` header. Specifically: local D-012 brand split → family **D-016**; local D-013 Option B host → already family **D-012** ✓; local D-014 no paid services → §6 standing rule (not numbered); local D-015 recommend next path → §6 standing rule (not numbered); local D-016 no source attribution → family **D-017**. **Carry-forward for dotintel2's next session.** (Added to dotintel2 BACKLOG separately.)

- **dotintel2 minted D-017a through D-017g** in its `SESSION_28_HANDOFF.md` for Carrier Software parity launch decisions. These are product-tactical (DOT Intel feature priority), not family-architectural; they don't collide with the new family-ledger D-017 (different content), but renaming D-017a-g → `DI-CSP-a` through `g` would prevent future confusion if/when family logs another D-017.X.

---

## Source-of-truth pointers (read these on session open)

- This file (read first per Rule 6)
- **D-023 / ADR-023 — Agency Signal positioning lock (2026-05-18):** [`docs/decisions/adr-023-neilson-programbusiness-agency-signal-boundary.md`](decisions/adr-023-neilson-programbusiness-agency-signal-boundary.md) + [`docs/strategy/agency-signal-positioning.md`](strategy/agency-signal-positioning.md) (core thesis) + [`docs/strategy/agency-signal-product-boundaries.md`](strategy/agency-signal-product-boundaries.md) (owns / does NOT own) + [`docs/strategy/neilson-competitive-boundary.md`](strategy/neilson-competitive-boundary.md) + [`docs/strategy/programbusiness-competitive-boundary.md`](strategy/programbusiness-competitive-boundary.md) + [`docs/strategy/distribution-expander-thesis.md`](strategy/distribution-expander-thesis.md). 8 domain docs at [`docs/domains/`](domains/). Status dashboard at [`docs/agency-signal-status.html`](agency-signal-status.html). Proposed schema at [`supabase/migrations/0091_agency_signal_d023_tables_proposed.sql`](../supabase/migrations/0091_agency_signal_d023_tables_proposed.sql) (NOT YET APPLIED).
- Latest family-hub handoff: [`docs/handoffs/SESSION_21_PRICING_HANDOFF.md`](handoffs/SESSION_21_PRICING_HANDOFF.md)
- Latest Agency Signal product handoff: [`docs/handoffs/AGENCY_SIGNAL_SESSION_4_HANDOFF.md`](handoffs/AGENCY_SIGNAL_SESSION_4_HANDOFF.md)
- Next Agency Signal kickoff: [`docs/handoffs/AGENCY_SIGNAL_SESSION_5_KICKOFF.md`](handoffs/AGENCY_SIGNAL_SESSION_5_KICKOFF.md)
- Working agreement: [`docs/WORKING_AGREEMENT.md`](WORKING_AGREEMENT.md)
- Family hub (this repo): [`docs/context/MASTER_CONTEXT.md`](context/MASTER_CONTEXT.md), [`DECISION_LOG.md`](context/DECISION_LOG.md) (D-001 through D-021 + §6 standing rules), [`SESSION_STATE.md`](context/SESSION_STATE.md), [`ENGINEERING_DOCTRINE.md`](context/ENGINEERING_DOCTRINE.md), [`ANTI_DECAY_PROTOCOL.md`](context/ANTI_DECAY_PROTOCOL.md), [`FAMILY_HEALTH.md`](context/FAMILY_HEALTH.md) (cross-product snapshot, updated at session close), [`PRODUCT_REPO_INDEX.md`](context/PRODUCT_REPO_INDEX.md) (canonical repo/project/URL lookup per product + per-product paste-ready session openers)
- Inside-view: [`docs/STATE.md`](STATE.md) *(last updated end of AS Session 3 — Session 4 deltas are in `AGENCY_SIGNAL_SESSION_4_HANDOFF.md` §5)*
- Pricing briefs locked 2026-05-12: [`docs/context/PRICING_CREDITS_AND_TOPUPS.md`](context/PRICING_CREDITS_AND_TOPUPS.md), [`docs/context/PRICING_ENTERPRISE_LAYER.md`](context/PRICING_ENTERPRISE_LAYER.md)
