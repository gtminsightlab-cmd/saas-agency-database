# Decision Log — Seven16 Group

**Last updated:** 2026-05-26 (**D-034 locked: Family pricing + Charter Member program KILLED. Per-product pricing autonomy adopted; Agency Signal moves to 4-tier transparent buying model (Sample $75 + Monthly Subscription $299/$599/$999 + Bulk Build-Your-File + National Founder License $12,500/yr). Supersedes D-014 / D-015 / D-018 / D-021 — all four marked SUPERSEDED with cross-references. Stripe sandbox catalog scheduled for archive; `/charter` page retired to "program ended" notice. Each Seven16 family product now decides its own pricing model independently — no shared credit currency, no Charter Member cap.**) Prior 2026-05-19 (**D-025 locked: Agency identity keyed by NPN+EIN; state DOI feeds authoritative for appointment data. Schema 0093 + 0094 applied to `sdlsdovuljuymgymarou` — agencies gains `npn` + `ein` (non-unique B-tree indexes); agency_carriers gains `appointment_active_date` + `state_filed` + `source_year`; new `ax_staging` schema for ETL doctrine compliance (4 tables, service-role only, NOT exposed via PostgREST). `source_type` taxonomy locked at `docs/context/SOURCE_TYPE_TAXONOMY.md`. Texas 2026 NAIC mapping CSV generated for Session B spot-check (937 distinct carriers in file: 300 high-confidence map, 301 review, 336 net-new).**) Prior 2026-05-18 (**D-024 locked: Front-End Production Standard — every screen meets 10 standards + 12-point Definition of Done before "done." Tooling locks: `sonner` toast lib + `eslint-plugin-jsx-a11y`. Shared `components/ui/*` primitives queued. Apply-on-touch policy. New §6 standing rule + full doctrine in ENGINEERING_DOCTRINE.md §"Front-end production standard."**) Prior 2026-05-18 (**D-023 locked: Agency Signal positioning sharpened to "Distribution intelligence for commercial insurance" + 7-pillar product taxonomy + Neilson + ProgramBusiness as adjacent competitive references + verified/claimed profile flow queued to Tier 1.x. Architect strategy review ratifies ~80% of existing family doctrine; D-023 codifies the sharpening so future sessions can't relitigate.**) Prior 2026-05-15 (SESSION_24 tail — **D-022 locked: Threshold IQ spun out of Seven16 family.** Family-active products consolidate to DOT Intel cluster + Agency Signal + seven16-platform control plane. TIQ-specific decisions D-009/D-019/D-020 preserved for history per Rule 4 but no longer active; `PRICING_THRESHOLD_IQ.md` archived (banner added, file kept). Charter Member offer surface count drops from 7 to 6 (TIQ surface removed). `/charter` marketing page on directory.seven16group.com rebuilt with TIQ-free savings example (commit `fe57bce`). seven16-distribution repo + Vercel `prj_c6kzFEhpw6Uwb12TECUidKlBxOwr` + Supabase `yyuchyzmzzwbfoovsskz` + Cloudflare `thresholdiq.io` scheduled for wind-down in a follow-up session. Prior 2026-05-15: POST-SESSION-22 pricing refinements — Learning Center LOCKED [$29.95/seat + 1-25 volume toggle: 5=12% / 6-14=20% / 15-19=30% / 20-25=35%, single-course v1]; Directory Listings REFINED [DOT carriers FREE, retail agents $120/yr annual-only, NEW wholesaler/insurance-carrier $1k/mo tier]. New family memory `reference_account_support_tooling.md` with Scribe-anchored 3-phase customer-success tooling plan. Full SESSION_22 handoff at `docs/handoffs/SESSION_22_PRICING_HANDOFF.md` covers the prior charter-integration locks; THIS pricing refinement is a post-handoff continuation in the same family-hub session). Prior 2026-05-15: SESSION_22 family-hub pricing marathon close — Charter Member integrations locked across TIQ + Alerts + Directory at 25% off [option (a) confirmed by Master O]; 3 new operational pricing docs shipped: `PRICING_DIRECTORY_LISTINGS.md`, `PRICING_LEAD_DOWNLOADS.md`, `PRICING_LEARNING_CENTER.md` [placeholder, pricing model pending Master O at time of SESSION_22 close — now locked]. Prior 2026-05-15: D-021 numeric refinements (TIQ tiers in PRICING_THRESHOLD_IQ.md; DOT Alerts flat tier bands in PRICING_DOT_ALERTS.md). Prior 2026-05-15 AM: D-021 Seven16 Credits & Usage Pricing Architecture locked — supersedes D-019 TIQ tier pricing; amends D-018 Charter Member program; amends D-014 base credit price. Prior 2026-05-15: D-021 Seven16 Credits & Usage Pricing Architecture locked — supersedes D-019 TIQ tier pricing same-day; amends D-018 Charter Member program to lock "best pricing tier" model; amends D-014 base credit price from $0.29 to $0.15. Prior 2026-05-15: D-019 (now superseded) + D-020 (still valid). Prior 2026-05-15 AM: D-018 Charter Member program locked. Prior 2026-05-14: D-016 three-domain brand split + D-017 no-source-attribution-in-`directory.*` logged from dotintel2 sessions 25-27; §6 standing rule "Always recommend next path as CTO/PM" added.
**Companion to:** [MASTER_CONTEXT.md](MASTER_CONTEXT.md)

> This doc is the **frozen list of decisions Master O has already made**. If a future Claude session is about to relitigate something on this list, stop — the answer is here.
>
> When a new decision lands, add it under the right category with a date and a "Why" line so the reasoning travels with the verdict.

---

## 1. Brand architecture (D-001 through D-011)

| # | Decision | Date | Why |
|---|---|---|---|
| **D-001** | Brand architecture = **Option C**. Separate brands for the market, shared infrastructure for the business, single login for the customer. | 2026-04-30 | Lets each product earn its own audience without rebuilding plumbing. Required for the 2027 bundle. |
| **D-002** | Pricing locked, "little guy first" — every consumer tier under the $500 P-card threshold so a producer can expense without manager sign-off. | 2026-04-30 | $19 Producer is the wedge. Everything follows from giving the working producer a yes without bureaucracy. |
| **D-003** | **Seven16 Group is not a product.** It's the trust/authority layer. The page a VP lands on after reading a LinkedIn post. | 2026-04-30 | Avoids brand confusion — products carry the value prop, Seven16 carries the credibility. |
| **D-004** | **Agency Signal** is the new market name for the product previously called "SaaS Agency Database" / "Agency Data Seven16". The existing build stays in production. | 2026-04-30 | Foundation is good, no rebuild needed. Older docs that say "SaaS Agency Database" map to this. |
| **D-005** | **2027 bundle:** $179/mo Seven16 Intelligence = DOT Intel Business + Agency Signal Growth. | 2026-04-30 | Anchors the bundle math: more than either single tier, less than the sum. |
| **D-006** | Build shared auth + RLS day one. One Supabase org, separate schemas. One Stripe customer per user across products. | 2026-04-30 | The 2027 bundle requires it. Retrofitting is expensive — and will be more expensive then. |
| **D-007** | **DOT Intel = full rebuild on Seven16 stack.** Same URL (dotintel.io), new bones. Old DOT Intel project is reference-only for learnings; code is NOT migrated. | 2026-04-30 | Old project's architecture isn't compatible with the shared-auth bundle requirement. Rebuild is cheaper than migrating. |
| **D-008** | **Supabase multi-project topology** — `seven16-platform` (auth + tenants + entitlements + master control center) plus Seven16-built product satellites: Agency Signal, DOT Intel, Threshold IQ, Seven16Recruit. (Growtheon does NOT get a satellite — it's a reseller arrangement on third-party infrastructure per D-010.) Supersedes the "one project, separate schemas" reading of D-006. **Cost note (2026-05-02):** real per-satellite cost is $10/mo on Pro plan, not the $25/mo originally estimated. Full family at maturity = 4 satellites + 1 platform = +$20/mo over today, not the ~$50/mo originally noted. | 2026-05-01 | M&A optionality + cross-sell + master control center are all required. Multi-project is the only architecture that honors all three, in exchange for clean blast-radius separation and a viable carve-out path if any Seven16-built product is ever sold. |
| **D-009** | **Threshold IQ** is the locked market name for the third platform product (working name: `seven16-distribution`; descriptive: MGU/wholesaler operating CRM). Domain `thresholdiq.io` secured on Cloudflare 2026-05-02. Brand surface: single-word **Threshold** with `IQ` suffix on URL/wordmark. Triple meaning — doorway (founder crossing into the market), underwriting threshold (appetite/premium/loss-ratio thresholds are native MGU vocabulary), growth threshold ("crossing into scale"). Scope: producer lifecycle, contracting, licensing, E&O tracking, appointments, exception-based compliance. Tenant zero = Seven16 Group. | 2026-05-02 | Native vocabulary for the ICP — insurance pros recognize "threshold" as their daily language. Editorial/utilitarian positioning differentiates from generic SaaS. Pricing already set in the build session, family-integration pricing collaboration deferred to a future session. |
| **D-010** | **Standalone-capable add-on architecture (two delivery models).** Two products are sold as standalone OR attachable add-ons — neither is bundled inside a platform — but their delivery models differ: <br>**(a) Growtheon — third-party SaaS reseller.** Seven16 resells a GHL-like platform under the Growtheon white-label brand. No Seven16-owned Supabase satellite; the underlying CRM runs on the third-party vendor's infrastructure. Tracked in `seven16-platform.products` and `seven16-platform.entitlements` so we can bill via Stripe and show entitlements in the master control center; provisioning creates a sub-account on the upstream vendor and stores the reference in the entitlement metadata. Reseller margin model is open question #2 in §9. <br>**(b) Seven16Recruit — Seven16-built AI tool.** Own Supabase satellite per D-008. Two delivery surfaces: standalone (own UI for non-customers) and plug-in (API/integration into Threshold IQ or external customer CRM). Stealth, attorney-gated. <br>Both share the same entitlement pattern: a tenant can subscribe to either without any platform entitlement, and any platform tenant can attach either. The existing `unique(tenant_id, product_id)` constraint in `entitlements` allows multiple distinct products per tenant. | 2026-05-02 | Maximises addressable market — customers who want one capability shouldn't be forced into a platform commitment. Different delivery models reflect business reality: Growtheon's value is reseller economics on an existing platform; Seven16Recruit's value is Seven16-built AI capability. Same entitlement plumbing, different infrastructure shape. |
| **D-011** | **Target market = emerging / startup small firms.** The product family deliberately fills the gap where competitors (legacy carrier-grade systems, large-broker tools, GHL+enterprise CRMs) are too robust or too expensive. Our stack and capabilities aren't as deep — that's intentional. Pricing tiers and onboarding flows are designed for the firm that can't justify procurement, can't afford a $5k/mo seat license, and needs to start producing quickly. | 2026-05-02 | Codifies what's been implicit since D-002. Every future scope, pricing, and feature decision should pass the "would an emerging firm with one P-card actually buy this today?" test before adding cost or complexity. Larger customers welcome but never the design target. |
| **D-012** | **Directory build host = `dotintel2` repo + `vbhlacdrcqdqnvftqtin` Supabase satellite.** The five new schemas from the directory build spec (`directory.agent_*`, `wholesaler.*`, `carrier_market.*`, `content.*`, `marketplace.*`) live in dotintel2's Supabase satellite, not saas-agency-database's. Spec + kickoff + status tracker archived at `dotintel2/docs/specs/dotintel-directory-build-{spec,kickoff,status}.md`. Agency-side tables (`directory.agencies` / `contacts` / `appointments`) currently live in saas-agency-database's `sdlsdovuljuymgymarou` and will mirror over to `vbhlacdrcqdqnvftqtin` via a future Task 1.F when integration is needed. **Note:** the wrap-up from the dotintel2 session 25 referred to this as "D-009" — that number was already taken (Threshold IQ name lock); it's logged here as D-012. | 2026-05-10 | Spec §4 diagrammed all five schemas under one "seven16-app" Supabase project, but D-008 deliberately splits satellites per product. Putting the directory build in dotintel2 keeps it co-located with the heavier data spine (`carriers` 50K rows, `inshist_raw` 7.4M rows, `carrier_insurance_current` 19.7K rows). Mirroring the 30K-ish agency rows from saas-agency-database into dotintel2 is mechanically simpler than mirroring 7.4M+ rows the other direction. URL-strategy question for the new agent/wholesaler directories (sub-path of directory.seven16group.com vs. dotintel.io paths vs. new TLD) remains §9 open question #1 — this decision is host-only, not URL-strategy. |
| **D-013** | **Directory mirror cadence = nightly refresh; architecture = Vercel-Cron-→-Supabase-Edge-Function proxy.** Open Q #8 from the directory build spec is resolved. Task 1.F splits into two phases: **Phase A (1.F-schema)** one-time initial copy of the 1,887 transportation agency rows + 23,546 contacts (mirror filter = agencies appointed to ≥1 transportation-vertical carrier via `carrier_verticals`); **Phase B (1.F-cron)** nightly idempotent UPSERT at `0 4 * * *` UTC via `app/api/cron/agency-mirror/route.ts` (Vercel Cron) → `supabase/functions/mirror-agency-signal/index.ts` (Supabase Edge Function with direct `postgres.js` connection — bypasses PostgREST's stuck schema cache, see `memory/feedback_postgrest_schema_cache_stuck.md`). UPSERT key = `id` (= source uuid, identical in both projects). Mirror all 1,887 location-level agency rows (NOT the parent-deduped 1,168 the public mv shows). Neilson contacts excluded per hard rule §6.7. Both phases shipped 2026-05-12 (session 27). | 2026-05-12 | Producer data churns monthly, not minute-by-minute → nightly is fresh enough for Phase 2 email-blast freshness without the ops surface of CDC. Vercel Cron is already the family scheduler pattern (Task 1.C-cron) → reuses CRON_SECRET handling, monolingual stack. Edge Function escape hatch was forced by PostgREST's stuck schema cache (4+ hours after `notify pgrst, 'reload schema'` it still reported `directory.*` not exposed) — direct `SUPABASE_DB_URL` postgres.js connection from inside Supabase's runtime was the only viable write path. The proxy pattern (Vercel route → Edge Function) keeps scheduler + executor concerns cleanly separated and is now the family template for any cron that writes to a non-public schema. |
| **D-014** | **⚠️ SUPERSEDED 2026-05-26 by D-034 — Family pricing + universal credit currency killed. AS moves to 4-tier transparent buying model.** Original content preserved per Rule 4. <br><br>**Original D-014 (now superseded):** Consumption engine = monthly credit allotments inside locked subscription tiers + $5-increment top-up slider (uncapped position, bonus-credit ladder 0% → +45% at $1,000+) + variable consumption for AI/upload tools.** All locked subscription tiers from D-002 / D-005 / D-011 stay exactly as published. Each paid tier ships with a monthly credit allotment (DOT Intel: 100 / 600; Agency Signal: 50 / 400). **Credit reference value: 1 credit ≈ $0.29 face** ($29 Pro → 100 credits anchor). Subscription credits rollover one month at 2× cap then expire; top-up credits never expire. **Top-up slider** snaps in $5 increments with bonus thresholds: $5 (+0%), $10–$15 (+10%), $20–$45 (+20%), $50–$95 (+30%), $100–$195 (+35%), $200–$495 (+40%), $500–$1,000+ (+45% cap). Past $1,000 the slider remains available but +45% is the ceiling — bigger commitments route to a quoted Enterprise contract (preserves contract-tier value). **Variable consumption**: PDF upload + AI analysis = 5 credits base + 1/5MB over baseline (cap 25 credits/doc); image OCR = 2 credits base + 1/5MB over (cap 10/image). Estimated cost shown client-side before upload — user never gets surprised. In-product slider surfaces at 10%-remaining (persistent header chip) and inline at the moment of value capture (modal); never force-upgrades the user, never hard-paywalls (out-of-credit users still see basic public-data tier output). **Per-tool credit costs locked in `PRICING_CREDITS_AND_TOPUPS.md` §3.2 (DOT Intel) and §3.5 (Agency Signal)** — anchor example: DOT lookup + PDF export = 2 credits ($0.58 face), so $29 Pro = 50 of those at the locked rate. Schema (`credit_wallets`, `credit_ledger`, `credit_consumption_rates`) + Stripe metered billing wiring deferred to next implementation session. Enterprise+ state-ladder (Neilson-replacement layer) and Distribution+ outcome SKU explicitly deferred to a separate brief (`PRICING_ENTERPRISE_LAYER.md`, second-ICP scope, outside D-011's design target). | 2026-05-12 | The locked small-firm wedge is the right entry; the consumption engine is how we capture the heavy user's actual willingness-to-pay without forcing a tier upgrade or a procurement conversation. Master O's lived experience with Twin Agents (spent $5 ten times in a week without noticing; slider goes to $1,000 with 45% off) validates both the $5-increment baseline and the uncapped-slider ceiling. The trends-report "126% YoY credit-model surge" is this exact pattern at industry scale (Cursor, Replit, Lovable, Bolt all run variants). Bonus credits framed as "more fuel" not "discount" preserves abundance psychology. Hybrid expiry is the cleanest customer story. Variable consumption on uploads protects unit economics — PDF analysis really does cost more (Claude API + OCR), and clearly metering it lets us add high-value AI features without margin risk. Zero conflict with D-002 / D-011 because no subscription price moves; the $500 P-card threshold is preserved (top-ups are micro-discretionary, not subscription); and the +45% bonus cap at $1,000 ensures heavy self-serve users get warmed-up for Enterprise rather than staying on the slider forever. |
| **D-015** | **⚠️ SUPERSEDED 2026-05-26 by D-034 — State-based slider replaced by flat $12,500/yr National Founder License + transparent 4-tier model. Distribution+ outcome SKU retired with the rest of the Enterprise+ layer.** Original content preserved per Rule 4. <br><br>**Original D-015 (now superseded):** Enterprise+ pricing layer for the second ICP (Distribution Expanders) — FULLY LOCKED 2026-05-12. Agency Signal state-based slider ($1,000–$2,000/state, $12,500 all-50 ceiling, 50% undercut of Neilson $25k); DOT Intel volume packs ($499/$1,499/quote); Distribution+ outcome SKU ($300–$500 per qualified appointment, layered on either Enterprise+).** Sits ABOVE the locked subscription tiers, inside the existing $399+ (Agency Signal) and $499+ (DOT Intel) Enterprise bands — published packaging of those bands, not new tiers. **Second-ICP buyer = VP of Distribution at MGAs/wholesalers/carriers** — explicitly outside D-011's small-firm design target. State pricing formula: `state_price = max($1,000, min($2,000, $500 + contacts_with_email × $0.20))` — anchored on **verified email contacts** (buyer's reachable-people KPI), not agency count. **Validated against live data 2026-05-12** (41,705 agencies / 135,453 contacts / 115,892 verified emails in Agency Signal satellite `sdlsdovuljuymgymarou`). **Locked state tiers** (5/15/28 + 3 territorial add-ons): **Tier 1 $2,000** (≥5,000 emails) = CA, MI, NY, OH, PA; **Tier 2 $1,500** (2,000–4,999 emails) = CO, FL, GA, IA, IL, IN, KY, LA, MA, MN, MO, NC, NJ, TX, WI; **Tier 3 $1,000** (<2,000 emails) = 28 states; **$0 territorial add-ons** (<50 emails) = AK, DC, HI (included with any purchase). Notable: MI is Tier 1 by data weight (7.2 contacts/agency) despite ranking #7 by agency count; TX/FL are Tier 2 not Tier 1 (high agency count but low contacts/agency). Bundle discount ladder: 1–4 states full, 5–9 −15%, 10–24 −30%, 25–49 flat $9,500, all-50 flat $12,500 — **with mandatory overflow protection** `final_price = min(computed_bundle, $12,500)` so the ladder never exceeds the ceiling (arbitrage becomes conversion event: UI shows "Your 9 states cost $13,600 — upgrade to all 50 for $12,500"). Included credits scale with purchase (200/state à la carte; 5,000/yr at all-50). **Distribution+ outcome SKU** prices per qualified appointment (MGA's actual KPI); qualification standard mutually defined at contract; Neilson's cost model precludes following this pricing motion. **Distribution Suite bundle = $22,500/yr** (Agency Signal all-50 + DOT Intel Enterprise Volume Pro + 10,000 unified credits) — coexists with locked $179/mo 2027 Seven16 Intelligence bundle (D-005) since they target different ICPs. New schema required in `seven16-platform`: `customer_entitlements (customer_id, product_id, scope_type, scope_value, ...)` for state-level RLS and `appointment_attributions` for outcome-SKU tracking. À la carte all-50 totals $64,500 — bundle ceiling $12,500 = **81% effective discount**. Full spec: `PRICING_ENTERPRISE_LAYER.md`. | 2026-05-12 | D-011's small-firm wedge cannot serve the Distribution Expander; published $399+/$499+ Enterprise floors had no packaging definition. Neilson trained this buyer ($10k entry, $1,500/state, $25k all-50 — see `memory/reference_neilson_pricing_mechanics.md`) — meeting that expectation with a 50% undercut at the ceiling is the wedge. Pricing on verified email contacts (not agency count) aligns vendor revenue with buyer value: agencies vary 4×-10× in contacts-per-record, so agency-count pricing structurally undercharges high-density states. Tier rule (≥5,000 / 2,000–4,999 / <2,000) gives a defensible "your state's price is based on the people you can reach" answer when challenged. Overflow protection on bundle ladder solves arbitrage cleanly — buyer always sees the cheapest path to their goal. Outcome SKU captures the buyer's actual KPI (agent appointments) in a way Neilson's cost structure forbids — that's the moat. Sits cleanly alongside D-014 (consumption engine for self-serve overflow) without touching D-002/D-011 locked subscriptions; $500 P-card threshold preserved at consumer tier because Enterprise+ is annual-contract/procurement-routed by design. |
| **D-016** | **Three-domain brand split (not subdomains)** — DOT Intel territory now spans three Cloudflare-DNSed brands routed through a single Vercel project (`prj_4tnPATJjP4Bahg2RFw4DwlTuNa8S`, repo `dotintel2`): **`dotcarriers.io`** (carrier-facing directory — ~50,298 public unclaimed-shell listings auto-seeded from `public.carriers`, claim CTAs, paginated/filterable by state / PU band / claim status, profile detail at `/:dot`), **`dotagencies.io`** (agent + wholesaler directory — proper chrome, empty `directory.agent_profiles` state by design, profile detail at `/agents/transportation/:slug`), **`dotintel.io`** (intelligence dashboard + 2027 bundle hub — unchanged from existing). Host-header-based path rewriting in `dotintel2/middleware.ts` splits routes; runbook at `dotintel2/docs/runbooks/domain-split-dns.md`. **Reverted an earlier "let's use subdomains" path** after one round-trip. | 2026-05-12 | Single canonical URL per audience improves SEO over subdomain pages competing for the same brand juice; cleaner brand boundary for the carrier-claim flow (a fleet owner shouldn't need to grok "DOT Intel" before claiming their own DOT); same codebase keeps shipping cost flat. Aligns with D-001 brand architecture (separate brands, shared infrastructure, one customer login). **Note on numbering:** this decision was logged in `dotintel2/docs/SESSION_26_HANDOFF.md` as a local "D-012" — that local number conflicted with family ledger D-012 (Directory build host = dotintel2). Reconciled here on 2026-05-14 with the correct family-ledger ID **D-016**. |
| **D-017** | **No agency-data-source attribution in `directory.*` schemas.** Hard rule §6.8 from the directory build spec, enforced via rename + drop migration on `vbhlacdrcqdqnvftqtin`: `agency_signal_id` columns renamed to `upstream_id` (so the directory row knows where it came from internally, without leaking the source brand to any reader of the row); all upstream account-type, management-system, and social-URL columns DROPPED from `directory.agencies` + `directory.contacts`. The Edge Function mirror (`mirror-agency-signal` per D-013) strips these on every nightly UPSERT, not just the initial backfill. | 2026-05-12 | The directory build is a public-facing surface; readers of a `directory.agencies` row must not be able to infer that the data came from Agency Signal (would expose the cross-product dependency Master O wants kept opaque to outsiders). Neilson exclusion (per `data_load_denylist`) is separately enforced; this rule is about attribution, not about which rows mirror. **Note on numbering:** logged in `dotintel2/docs/SESSION_26_HANDOFF.md` as a local "D-016" — reconciled here on 2026-05-14 with the correct family-ledger ID **D-017**. |
| **D-018** | **⚠️ SUPERSEDED 2026-05-26 by D-034 — Charter Member program KILLED entirely. No more cap, lifetime-locked pricing, or value-exchange-for-discount cohort across the family. Each product prices independently.** Original AMENDED + Original D-018 content preserved per Rule 4. <br><br>**Prior status (now superseded):** AMENDED 2026-05-15 by D-021 — Charter Member offer reshaped from "50% off Charter Bundle" → "Best pricing tier permanently on everything they buy."** Cap (50–75 accounts total), 60–90 day enrollment window, required value-exchange (1+ case study within 12 months + quarterly feedback calls + anonymized usage data + multi-product use), active-subscription rule (6+mo cancellation forfeits charter), M&A-only transferability — **all unchanged.** Charter Members occupy: max credit bonus tier (+40% on top-ups, effective ~$0.107/credit), best directory-listing pricing, best TIQ submission band rate, best alerts per-unit rate — all locked for life. **Original "Charter Bundle" SKU dissolved** — Charter Members get charter pricing across every product they buy, not on a fixed bundle slice. This is a STRONGER offer than the original — scales with everything they consume, compounds across multi-product adoption. **Original D-018 (now amended):** Charter Member program — 50% off lifetime-locked on the Charter Bundle, capped 50–75 accounts total across the Seven16 family, 60–90 day enrollment window, structured value-exchange.** Single offer for network + non-network charter members (no two-tier complexity). **Scope = the Charter Bundle, NOT every future SKU.** Phase 1 bundle (at launch): D-005 Seven16 Intelligence bundle (DOT Intel Business + Agency Signal Growth, $179/mo standard → $89.50/mo charter) + Learning Center access included. **Phase 2 (charter members get first right of refusal at 50% for a time-bounded 60–90 day window from each SKU's GA, NOT automatic lifetime inclusion):** Threshold IQ Starter seats (per **D-019** below — $99/mo standard → $49.50/mo charter ROFR), DOTCarriers premium directory upgrade (when directory monetization ships), DOTAgencies premium directory upgrade (when directory monetization ships). **Explicit exclusions (charter members pay standard):** Agency Signal one-time Snapshot ($125), Distribution+ outcome SKU ($300–500/qualified appointment, performance-based), Enterprise+ tier (D-015 second-ICP scope), and any product launched after the Charter program closes. **Free-trial period:** 30 days free, no credit card required, charter pricing kicks in month 2 — single universal entry (no two-tier network/non-network logic at launch; reserve direct-to-charter path for pre-qualified intros if NETWORK conversions materially drop). **Active-subscription rule:** if an account fully cancels and leaves for 6+ months, Seven16 reserves the right not to re-issue charter pricing on reactivation (prevents pause-and-restart gaming). **Transferability:** locked to the customer/org; transferable only if the agency/MGU is acquired via M&A (prevents secondary market). **Required value-exchange (structured, not optional, made explicit at signup):** (1) at least one case study or testimonial within first 12 months, (2) feedback sessions / product interviews several times per year (quarterly cadence default), (3) anonymized usage data to guide roadmap + messaging, (4) try more than one product in the Charter Bundle (validates the ecosystem thesis — single-product chartering misses the point). **What charter members get:** 50% off the Charter Bundle locked for life, early access to new features (charter-only beta channel), direct line to product team for feedback + roadmap input, voice in what gets built next, Charter Member badge visible on dotagencies.io directory listing (where applicable). **Fallback ranking** (if economics force compromise): preferred = locked terms above; acceptable = 50% off for 12 months then 25% off for life; least preferred = 50% off for 12 months then standard. **NOT considered:** "free forever" — heavy B2B tools with real data/support/infra costs shouldn't go free; lifetime-deal best-practice consensus is bounded + paid + with upgrade path. Full memorialization in family memory `project_charter_member_program.md`. | 2026-05-15 | Sits alongside D-014 (consumption engine) + D-015 (Enterprise+) as the third leg of the locked pricing stool: D-014 captures heavy-user willingness-to-pay above subscription floor; D-015 captures the Distribution Expander second-ICP; D-018 captures the highest-LTV early-adopter cohort with a "you will never see this price again" founding-member offer. **Strategic rationale:** (1) Network charter members are the highest-LTV cohort Seven16 will ever have — they buy multiple products over time, refer others, sit for case studies, feed roadmap. Long-term network yield outweighs year-1 full-price revenue. (2) Lifetime-locked is THE differentiated charter signal — proven pattern (GoHighLevel, Notion, Linear early-supporter models). (3) Avoids the worst SaaS moment: the "your discount is ending" email. Don't send that to network members you'll see at industry conferences for the next decade. (4) Math is acceptable as growth spend: ~50 members × $90/mo (bundle at 50%) × 5 years ≈ $270k "discount" relative to full list = the growth budget for acquiring evangelists, not dead margin. (5) Risk is controlled by **cap + scope**, not by shrinking the discount — lifetime deals get dangerous when uncapped in quantity OR unclear on scope OR treated as "all future products forever." Charter Member program addresses all three explicitly. (6) Aligns with D-002 small-firm wedge + D-011 ICP framing — these are the emerging-firm operators we designed for. (7) Time-bounded Phase 2 right-of-refusal (60–90 days post-GA, not lifetime-inclusion) keeps the lifetime commitment scope-bound — charter members are rewarded for adopting new modules without overcommitting Seven16 to discount every future SKU forever. **Sources Master O cited at lock time:** earlystagemarketing.com (acquiring early SaaS adopters), reddit.com/r/SaaS founder discussion on lifetime deals, freemius.com SaaS lifetime deals analysis, thebootstrappedfounder.com lifetime deals + SaaS businesses. |
| **D-019** | **⚠️ SUPERSEDED same-day by D-021 (2026-05-15).** Original lock attempted tiered subscription pricing for Threshold IQ (Starter $99 / Growth $299 / Scale $799 with per-seat + included-submissions hybrid). Master O recognized within hours that the tier model didn't match the actual product architecture — Seven16's products are à la carte by nature (DOT Lookups separate from lead downloads separate from directory listings separate from alerts separate from training separate from TIQ CRM) with cross-sell discounts emerging organically. D-021 replaces the tier structure with: (a) universal credit currency at $0.15/credit base + volume bonus bands, (b) per-organization TIQ Core license + submission bands, (c) directory listing prices by entity type + size, (d) DOT alerts per-unit pricing. See D-021 below for the locked replacement model. **D-020 (TIQ positioning lock) remains valid** — positioning is independent of pricing structure. Original D-019 entry preserved below for historical context per Rule 4 + the standing "log gets fuller, not cleaner" principle. <br><br>**Original D-019 (now superseded):** Threshold IQ subscription pricing — 3 tiers (Starter / Growth / Scale) with hybrid per-seat + included-volume pricing, soft caps on overage, 30-day free trial no credit card.** Pricing: **Starter $99/mo** (3 seats, 100 submissions/mo, all Phase E features); **Growth $299/mo** (10 seats, 500 submissions/mo, + advanced reporting + multi-tenant); **Scale $799/mo** (25 seats, 2,000 submissions/mo, + dedicated onboarding). **Overage:** $25/seat over included, $5/submission over included; soft caps at 80% (alert, not paywall). **Free trial:** 30 days of Starter, no credit card required (matches D-018 Charter Member program). **Charter Member pricing per D-018 Phase 2 ROFR:** 50% off Starter = $49.50/mo for charter members in the 60–90 day window from TIQ GA; ROFR is first-right-of-refusal, NOT automatic lifetime inclusion. **Annual prepay discount:** explicitly deferred (not in v1) — keeps launch billing simple; add later via Stripe catalog work. **Add-ons:** future Phase E modules + Recruit module + Phase 2 features priced separately when shipped. **Sits as the 6th leg of the Seven16 pricing stool:** D-002 (consumer tiers under $500 P-card), D-005 (Seven16 Intelligence Bundle $179), D-014 (credit consumption engine), D-015 (Enterprise+ Distribution Expander), D-018 (Charter Member program), D-019 (Threshold IQ tiers). | 2026-05-15 | Threshold IQ buyer = startup wholesalers / MGAs / MGUs per D-011 — distinct from producer-focused consumer tiers; MGU owner has budget authority, so the $500 P-card threshold relaxes. Salesforce-26-year-CTO playbook applied: **3 tiers (not 2 or 5)** because two leaves money on the table and five creates choice paralysis; **per-seat + included-volume hybrid** because pure-per-seat undercharges high-volume MGUs and pure-per-submission scares budget-conscious startups; **soft caps on overage** because hard paywalls break trust with startup MGUs who are exactly the relationship Seven16 is trying to honor. $99 / $299 / $799 is competitive vs. EZLynx enterprise pricing ($1000s/mo) AND vs. generic CRMs ($50–200/seat/mo) while remaining insurance-shaped. Starter at $99 stays P-card-friendly annually ($1,188/yr); charter price $49.50 is psychologically a "yes." **Growth at $299 will be the dominant tier** — covers ~70% of MGUs (5–10 producer team, 200–400 submissions/mo at Tenant Zero's observed rate of 4,853 submissions / 4 years ≈ 100/mo per MGU baseline). **Unblocks D-018 Charter Bundle Phase 2 ROFR** which previously hand-waved "TIQ starter seats when priced." Implementation: Stripe SKU + entitlement plumbing in seven16-platform satellite (deferred to existing family-hub Stripe catalog work). |
| **D-021** | **⚠️ SUPERSEDED 2026-05-26 by D-034 — Universal credit currency, family-wide à la carte SKU architecture, and cross-sell discount mechanics all retired. Per-product pricing autonomy adopted; no shared credit wallet across the family.** Original content preserved per Rule 4. <br><br>**Original D-021 (now superseded):** Seven16 Credits & Usage Pricing Architecture — universal credit currency + à la carte SKUs + cross-sell discounts via volume bonus bands. Supersedes D-019 tier pricing; amends D-014 base credit price; amends D-018 Charter Member offer shape. **(1) Universal credit model:** ONE credit currency across DOT Intel + Agency Signal + DOTCarriers + future alert/RFQ actions. Credits owned at **account (org) level**, not per-user — any user under the account spends from the shared wallet. **Base list price: $0.15/credit** (supersedes D-014's $0.29 anchor — substantive economics change). Standard lead/contact download = 1 credit. Top-up bonus bands: $5–$49 no bonus → $50–$199 +10% → $200–$499 +20% → $500–$999 +30% → $1,000+ +40%. Effective per-credit price drops with volume from $0.15 baseline; high-spend tier approaches "database deal" economics. Separate full-database SKU (~$12,500 for ~130k contacts) remains available as large prepaid credit pack. **(2) Credit consumption rules:** lead/contact download = 1 credit ($0.15 list). DOT lookup view = negligible or fractional credit (in-app utility, kept frictionless). **DOT lookup + PDF export = ~3 credits ($0.45 list)** aligning with $0.50 reference price. Account-wide wallet; all users in account draw from shared pool; usage tracked at account level. **(3) Lookup loyalty bumps:** monthly account lookup volume drives bonus credit bumps on the *next* top-up (without user needing to understand the table): 0–49 lookups/mo standard, 50–99 +10% on next purchase, 100–249 +15%, 250+ +20%. **(4) TIQ pricing:** **per-organization license**, not per-seat/per-submission. **3 named tiers locked 2026-05-15 in operational doc `PRICING_THRESHOLD_IQ.md`:** **Launch $500/mo** (100 submissions/mo included, $3/sub overage, 250 GB storage), **Growth $1,500/mo** (500 submissions/mo, $2/sub overage, 1 TB), **Scale $4,000/mo** (2,000 submissions/mo, $1/sub overage, 3 TB). Storage overage: $50/$40/$30 per additional 250 GB by tier. **Onboarding model:** DIY included; Assisted Onboarding $1,500–$3,500 one-time; Custom Implementation/Migration $5,000 fixed-scope OR $150–$250/hr. **Premium support retainer:** optional $500–$2,000/mo. **Charter Member integration mechanics PENDING** (§10 of TIQ doc surfaces 3 options; CTO recommendation = 25% off subscription + always Scale-tier overage rate). Principle preserved from D-021: protect small MGAs (Launch tier is the gateway), charge proportionally for high-volume AI/storage consumption, never nickel-and-dime per transaction. **(5) Directory listings:** ⚠️ **REFINED 2026-05-15 — see `PRICING_DIRECTORY_LISTINGS.md` for current locked rates.** Refined model: **DOT carrier listings are FREE** (carriers as free side of three-sided marketplace; monetization on agent + wholesaler sides). **Retail producer** $120/year annual-only (was $20/mo). **Agency** $120/yr first location + $60/yr per additional location. **NEW Wholesaler / Insurance Carrier listing** $1,000/mo (premium recruiting surface — capabilities directory + email blast inclusion + missed-opportunity dashboard). Charter Member 25% off all paid tiers. Original D-021 carrier-paid model preserved here for history: 1–3 PU $20/mo, 4–25 PU $50/mo, 50+ PU $65/mo. **(6) DOT alerts:** **AMENDED 2026-05-15 to flat tier bands locked in `PRICING_DOT_ALERTS.md`** (replaces the D-021 original "$20 base + tiered per-unit add-on" rough model). New structure: Owner-Operator (1 driver) **$25/mo**; Small Fleet (2–5) **$50/mo**; Growing Fleet (6–15) **$90/mo**; Regional Fleet (16–40) **$175/mo**; Large Fleet (41–100) **$350/mo**; Enterprise (101+) **starting $500/mo per DOT number, custom**. No per-alert or per-message charges; reasonable SMS + email volume included. Structural shift from per-unit math → flat bands chosen for invoice predictability + tier-boundary friction reduction + alignment with how fleet operators actually budget safety/compliance spend. Charter Member alerts integration PENDING (3 options in §8 of new doc). Original D-021 rough alerts model preserved here for history: solo $20/mo, 2–10 units +$2/unit, 11–50 +$1.50/unit. **(7) Cross-sell discounts (volume drives discount, not bundle SKUs):** the more an account spends across products, the better their effective rates — implemented via credit bonus bands + tier-locking for directory/alerts/TIQ submission bands. No fixed "bundle SKU" beyond what emerges from add-on math. (D-005 bundle reframed: $179/mo as a packaged discount price on the DOT Intel Business + Agency Signal Growth combination, NOT a fixed-tier SKU.) **(8) Charter Member integration (amends D-018):** Charter Members occupy the **best pricing tier permanently** — locked at +40% credit bonus (effective ~$0.107/credit), best directory pricing, best TIQ submission band rate, best alerts per-unit rate. Cap (50–75 accounts), enrollment window (60–90 days), exchange terms (case study + feedback + multi-product use) all unchanged from D-018. Charter Member shape changes from "$89.50/mo Charter Bundle" → "Charter pricing tier on everything you buy, locked for life." **(9) Numeric refinement:** specific submission bands + alerts per-unit values + cross-sell discount math need cost-modeling refinement; structure is locked, numbers may flex within the structure as observed unit economics inform. | 2026-05-15 | The tier-based D-019 lock made the same mistake every SaaS founder makes once: treating modular products as if they were monolithic tiers. Seven16's actual product architecture is à la carte by nature — a retail agent buying DOT Lookups + PDF Loads is a fundamentally different buyer than one downloading 500 leads/month than one running an MGU CRM. Tiers force these distinct buyers into the same package; à la carte + universal credits + cross-sell discounts let each buyer compose their own package while sharing the same credit wallet economics. **Why universal credits:** lets a TIQ-licensed MGU buy Agency Signal lead-download credits from the same wallet they bought DOT Intel lookup credits — natural cross-sell mechanic, no separate currency conversion friction. **Why account-level wallets:** producers + account managers + admins in the same agency draw from one pool — matches how the buyer thinks about their team's collective consumption, not individual seat budgets. **Why the lookup loyalty bump:** rewards heavy users without requiring them to understand bonus tables (the +10/15/20% just shows up on their next purchase). **Why per-org TIQ license + submission bands:** AI + storage costs scale with actual submission volume; protects small MGAs from per-transaction friction while charging proportionally for the shops actually consuming the compute. **Why directory pricing by entity + size:** $20 producer / $50–$25 agency by location / $20–$65 carrier by PU band — each reflects the value asymmetry (a 50-PU carrier extracts ~3× the value of a 1-PU operator from the same directory presence). **Why $20 alerts for solo operator:** market-feedback-validated reference price; per-unit math tunable from there. **Charter Member reshape:** "best tier permanently" is actually a STRONGER offer than "50% off the bundle" because it scales with everything they buy — a charter member running TIQ + Agency Signal + DOT Intel is getting compound discount across all three, not just the bundle slice. The cap (50–75) + scope (Charter Bundle defined as "everything Seven16 ships under the family architecture") still protects future pricing flexibility. **Sources implicitly drawn upon:** Salesforce-26y CTO playbook (modular SKUs with cross-sell discounts beats fixed tiers for emerging operators), AWS pricing (pay-per-action with volume discounts), Stripe pricing (transparent unit economics with no surprise charges), GoHighLevel reseller model context (per `reference_growtheon_platform.md` for the services arm side of the family). |
| **D-020** | **⚠️ SUPERSEDED 2026-05-15 by D-022 — Threshold IQ spun out of Seven16 family; positioning lock no longer family-active.** Original entry preserved per Rule 4. <br><br>**Original D-020:** **Threshold IQ positioning lock — "Run your MGU like a bigger shop." Target ICP: emerging MGUs / wholesalers stuck between generic CRM and heavy AMS, who want to operate at scale before they ARE at scale.** Locked in marketing v2 commits `541f69d` ("Marketing landing at apex") + `010cf4b` ("Marketing page v2: wholesaler audience + system-of-reference framing") on seven16-distribution; memorialized here to defend against future drift per Rule 4. **What this means concretely:** TIQ is **NOT** positioned as a CRM, **NOT** as an AMS, **NOT** as a generic workflow tool. It IS positioned as a **system of reference** — the spine that ties producer recruiting, submission intake, UW workflow, commission lifecycle, and reporting together for the startup MGU. **Tone discipline (per family `feedback_no_slop_in_copy.md`):** direct, operator-built, anti-enterprise-SaaS-jargon; NO "unlock the power of," NO "revolutionary platform," NO generic three-tile feature copy. **Sister-product alignment:** TIQ is the third leg of the Seven16 Trucking + Distribution Intelligence portfolio narrative (DOT Intel = data + Agency Signal = distribution + Threshold IQ = operating spine). Used together in the Charter Member deck slide 9 (Desktop file `seven16_charter_member_deck_FINAL.md`). **What this locks against:** future marketing pivots that drift back to "MGU CRM" / "AMS for MGAs" / generic-SaaS positioning. Any pivot of the positioning requires a superseding D-XXX per Rule 4. | 2026-05-15 | "MGU CRM" was always accurate-but-flat — it described the category, not the claim. "Run your MGU like a bigger shop" is a CLAIM addressed to a specific audience (wholesalers + MGUs scaling out of spreadsheets) with a specific promise (operate at scale before you ARE at scale). The "system of reference" framing positions TIQ as the spine other tools cluster around, not just another point product in the stack. This aligns with Seven16's broader thesis from D-011 — give under-tooled operators better weapons, not try to out-enterprise the incumbents on day one. Memorializing in DECISION_LOG protects against drift back to generic framing under future marketing pressure — every future "let's reframe TIQ as X" conversation now starts with "we'd need to supersede D-020 first." Pre-empts the most common SaaS-marketing failure mode: positioning entropy. |
| **D-024** | **Front-End Production Standard locked — every screen meets a 10-point standard + 12-point Definition of Done before "done."** Master O directive 2026-05-18: "We are not building a demo. We are building a production-grade application." **10 standards** (responsive layout · loading states · error states · empty states · success states · accessibility · error boundaries · state management discipline · performance · defensive UI). **12-point Definition of Done** (loading · empty · error · retry · success · mobile · keyboard navigation · screen readers · partial data · slow connection · user leaving mid-request · malformed or missing response fields). **Tooling locks:** `sonner` toast library + `eslint-plugin-jsx-a11y` ESLint plugin. **Shared primitives** `components/ui/{LoadingState,EmptyState,ErrorState,SuccessToast,ErrorBoundary,StatusPill}.tsx` built per repo. **Apply-on-touch policy:** existing screens that don't meet the standard get brought up to it whenever a session edits them; new screens hit the bar before merge. Full doctrine at `docs/context/ENGINEERING_DOCTRINE.md` §"Front-end production standard (D-024)." | 2026-05-18 | The front end is part of the product architecture, not just the visual layer. We're not building demos. Existing AS screens (sign-in, dashboard, build-list, verticals, admin, even today's Pillar 6 UI commit `b8ac2f5` with its `alert()` calls + missing visible loading indicator + `title=` instead of `aria-label`) fall below this bar. Codifying as D-024 makes the bar un-relitigable per Rule 4 and gives future Claude sessions a concrete checklist to ship against. Apply-on-touch avoids a backfill bloat session while bringing every actively-developed screen up to standard. Shared primitives prevent "every page rolls its own LoadingState" inconsistency. Tooling locks (`sonner`, `eslint-plugin-jsx-a11y`) chosen for modern UX + automated a11y enforcement at lint time. **First implementation queued for next session** as Pillar 6 UI hardening pass: replace `alert()` with toast, add visible loading spinner, add success confirmation, add error boundary, replace `title=` with `aria-label`. |
| **D-025** | **Agency identity keyed by NPN+EIN; state DOI feeds are authoritative for appointment data.** Schema migrations 0093 + 0094 applied to `sdlsdovuljuymgymarou` 2026-05-19. **0093** extends `public.agencies` with `npn` (text, non-unique B-tree index where not null) + `ein` (text, non-unique B-tree index where not null) for agency identity, AND extends `public.agency_carriers` with `appointment_active_date` (date) + `state_filed` (text, two-letter USPS code) + `source_year` (smallint) for state-filed appointment provenance, AND adds composite B-tree on `agency_carriers(agency_id, carrier_id, state_filed)` for the dominant query pattern. NPN/EIN are non-unique because branches may share a parent NPN; promote to unique only if load data proves the right shape (deferred decision per Session A scoping conversation). **0094** creates new schema `ax_staging` with 4 tables (`appointment_loads` ingest-run ledger, `appointment_load_pages` per-page checkpoint, `appointments_raw` landing zone with all-text columns + raw_json, `appointment_rows_rejected` validation fail bucket) — RLS enabled + forced on all 4, service_role policy each, **NOT** exposed via PostgREST (internal ETL only). Schema follows family `feedback_bulk_etl_architecture_rules.md` for any ingest >100k rows. Implication for existing 263,657 `agency_carriers` rows: all have `source_type IS NULL` and ZERO have 'TX' in `states` — they predate this taxonomy. Backfill / replace strategy for those rows is a separate decision (not covered by D-025; queued for Session B or later). `source_type` taxonomy locked at `docs/context/SOURCE_TYPE_TAXONOMY.md` with `state_doi_<state>` lowercase convention. First production use: Texas 2026 load (Session B). | 2026-05-19 | NPN (National Producer Number) + EIN are federal IDs published by every state DOI's public appointment files — they are the only reliable join key across state feeds, since fuzzy-matching agency names across 50 state files would compound resolution error catastrophically. The Texas 2026 file (367k rows / 937 distinct carriers / tens of thousands of distinct agencies) is the first ingest to exercise the pattern; future state DOI feeds (FL/CA/NY/etc.) reuse the same schema + ETL doctrine. **State DOI feeds are authoritative** because they ARE the regulator's record — preferred over vendor-scraped, modeled, or self-reported data when both are available for the same (agency, carrier, state, LOB). This is a structural defensibility moat per the family memory note `reference_neilson_pricing_mechanics.md` — Neilson charges $1,500/state for similar data; ingesting state files at $0 cost firsthand at the regulator-grade level beats any vendor-licensed equivalent on freshness, cost, and provenance. Carrier-side resolution requires NAIC backfill on the existing 1,369 `carriers` rows (all `naic_code IS NULL` today) — Session A produced a 937-row mapping CSV at `tmp/texas_naic_mapping.csv` (300 high-confidence map / 301 review / 336 net-new) for Master O spot-check before Session B writes any `carriers.naic_code` values. The non-unique NPN/EIN indexes are the conservative choice — promoting to unique would block ingest if any existing agency duplicates surface and the conservative-now/strict-later path costs almost nothing in query time at our scale. **First implementation queued for Session B** (the actual Texas load: ax_staging.appointments_raw → resolver pass → ax_staging promote → agency_carriers append). |
| **D-043** | **Seven16 family design coordination standard locked — branded product family model with shared design DNA + product flexibility.** Master O directive 2026-05-28: same arc as D-042 — "push this down to the family products so we can all be on the same page." External (GPT) design coordination memo reviewed, reconciled against locked doctrine (D-024 production standard + `feedback_no_slop_in_copy` + `feedback_frontend_production_standard` + the seven16-group-site S6.5 Sanity-editorial design system), locked as family-wide design standard. **Operating principle:** "One Seven16 operating system. Multiple product brands." Goal is not identical-looking products; goal is products that feel related — same skeleton + skin-level product personality. **Two flex rules LOCKED:** (1) **Logged-in app experience = 80% consistent / 20% product-specific** (users don't relearn the software when moving between products); (2) **Public marketing surface = 30% family DNA / 70% product personality** (each product earns its own ICP audience). **REQUIRED-CONSISTENCY MATRIX (operational reference):** Very High consistency on billing / support / security / compliance pages (parent-brand trust surfaces; drift costs the most). High on app shell / login / signup / dashboard structure / typography / button styles / tables / forms / AI assistant layout. Medium on dashboard structure / marketing homepage / product colors / product logos. Low / High flex on hero graphics / product terminology. **SKELETON LOCKED (consistent across all products):** app shell (left-side nav OR consistent primary nav; top header with logo + product-switcher + org-switcher + notifications + user-menu; consistent breadcrumbs + search); nav logic recommended pattern (Dashboard / Intelligence / Records / Workflows / Reports / AI Assistant / Settings / Billing / Support — products rename per industry but structure stays); typography = Fraunces (serif headlines) + Inter (sans body/UI) + JetBrains Mono (code), per S6.5 token system; shared component system (~40 named components: core SaaS billing/usage/role/audit/notification + AI assistant/confidence/source citation/usage meter + dashboard KPI row/work queue/alerts/trend/recommended-action); dashboard 4-question test (What is happening / What matters / What needs attention / What action next); AI assistant family-wide consistency (clear name + product role + confidence + suggested prompts + escalation + source citation + usage tracking tied to D-042 ai.usage event + hallucination guardrails + AI-not-replacing-pro-judgment disclosure); auth + billing + support + security UX near-zero product flex. **SKIN (per-product personalities LOCKED, accent colors TBD-confirm):** Bind Lab = Confident / Operational / Structured / Serious / Workflow-driven / Enterprise-grade (NOT lightweight CRM template); DOT Intel = Analytical / Signal-based / Risk-aware / Data-driven / Fast / Investigative; Agency Signal = Growth-oriented / Sales-focused / Data-rich / Actionable / Clean / Prospecting-friendly; Seven16 Survey = Clear / Simple / Helpful / Conversion-focused / Insightful / Easy to launch; DotCarriers = Practical / Searchable / Trustworthy / Logistics-aware / Verification-oriented; DotAgencies = Professional / Searchable / Local-market aware / Trustworthy / Referral-friendly; Seven16 Support = Calm / Helpful / Organized / Reliable / Clear / Human-centered. Per-product accent colors captured as STARTING POINTS only (GPT directional: Bind Lab deep blue/slate; DOT Intel dark navy/signal green/risk amber; Agency Signal growth green/data blue; Survey conversion tones; DotCarriers logistics/fleet; DotAgencies pro insurance directory; Support calm service tones) — Master O confirms each before code commits. Seven16 Email / Seven16 Academy / Seven16 Command personalities + accents = Master O dump pending. Seven16 Group Hub = the parent-trust personality (Authoritative / Calm / Operational / Enterprise-grade) — sets the tone every product borrows from. **5 FAMILY DESIGN RULES LOCKED:** (1) No random one-off designs (no random buttons/cards/fonts/dashboards/nav/colors/AI graphics); (2) Use shared components first (5-step order); (3) Product personality lives in the skin not the skeleton; (4) Every product looks enterprise-grade (15-item avoid list: gradient blobs, generic purple SaaS, fake metrics, weak dashboards, low-contrast text, etc.); (5) Design for cross-sell (shared nav patterns, product switcher, unified account identity, shared billing center, parent support, "Also available from Seven16 Group" cards). **SHARED DESIGN SYSTEM PHYSICAL HOME LOCKED:** Phase 1 (now) — seven16-group-site repo S6.5 work is the canonical reference; other repos copy-paste with `// SOURCE: seven16-group-site/...` comment marking drift visibility. Phase 2 (when second consumer needs cross-product reuse) — extract to `@seven16/design-system` npm package, versioned/semver'd. Phase 3 (mature) — Storybook + design tokens + Figma + per-product theming via token override + CI drift lint. Phase 1 → Phase 2 trigger is the pull (second consumer asks), NOT preemptive push (avoids extraction overhead pre-need per `feedback_no_paid_services_until_revenue` discipline). **20-item per-product launch DoD** (extends D-024 12-point production standard): consistent spacing token-based / responsive / mobile-friendly / WCAG AA contrast / clear CTAs / no broken states / no placeholder copy / no fake metrics / no generic AI-UI feel / clear empty/error/loading states / professional icon usage / consistent page headers / consistent form behavior / consistent table design / clean type hierarchy / clear support+billing+security paths. **Cross-repo prep at `saas-agency-database/docs/cross-repo/family-design-coordination-prep.md`** carries paste-ready BACKLOG entries for all 10 satellites (3 sub-arcs each: token import / shell+dashboard align / accent color confirm). Sequencing: seven16-group-site documents tokens first (~0.5 session) → Master O confirms parent palette + ready-to-lock accents (~0.25 session each) → satellites run alignment in parallel (~1-2 each). **Claude implementation instruction (paste-ready into every product session prompt that touches UI):** "Build using Seven16 Group product family design system per family memory project_seven16_family_design_coordination_standard.md (D-043). Maintain consistency in app shell, nav, typography (Fraunces/Inter/JetBrains Mono), dashboard structure, cards, tables, forms, billing, settings, support, AI assistant UX. Allow product-specific flex in accent color, logo, hero graphics, terminology, market-specific dashboards, marketing personality. Logged-in 80% consistent / 20% product-specific. Marketing 30% family DNA / 70% product. Avoid generic AI SaaS design. Enterprise-grade." **Final standard locked:** "Users should know they are inside a Seven16 Group product without every product looking the same." Full spec at family memory `project_seven16_family_design_coordination_standard.md`. | 2026-05-28 | (1) **External validation pattern repeats** — GPT independently re-derived the design coordination model already implied by S6.5 + the production standard + the no-slop rule. The genuine new value: the 80/20 + 70/30 flex rules + the consistency matrix + the per-product personality framings + the paste-ready Claude instruction. Three GPT reviews in 48 hours all landing on the same architectural foundation (D-040 platform / D-042 command center / D-043 design) = strong external validation. (2) **Design coordination IS family-wide doctrine, not nice-to-have** — without it, every product session re-derives skeleton patterns independently and the family fragments into 10 separately-designed products with no cross-sell-trust-transfer. The compounding cost of redesign-debt across 10 products is far worse than locking the standard once. (3) **The skeleton/skin split is the cleanest mental model for product family design** — it lets each product earn its own ICP (skin) without breaking the family operating-system feel (skeleton). Matches D-034 per-product pricing autonomy: each product chooses its own pricing model, but they all run on the same Stripe rails. Same here: each product has its own personality, but they all run on the same design rails. (4) **Phase-1-then-extract on the component library** avoids premature extraction — pulling a shared library into an npm package before the second consumer exists is over-engineering. Copy-paste with `// SOURCE` comments + drift visibility is enough until the second consumer creates real demand for versioned extraction. (5) **Master-O-confirms-each-accent-color discipline** prevents Claude sessions from quietly committing brand decisions. Personality is locked authoritatively (it's product positioning, already covered in product catalog); accent colors are brand-design decisions Master O owns. The standard ships; the colors get a clean confirmation pass before any code lands. (6) **Locking three family-wide standards in 48 hours** (D-040 platform / D-042 command center / D-043 design) creates the foundational operating-system framework before the family scales — every greenfield product (Bind Lab Phase B, Seven16 Academy v1, DotCarriers/Agencies decouple) bakes the standards in from day one; every shipped product runs an alignment pass in their next session. The sequence is correct: platform first, then command center, then design. |
| **D-042** | **Seven16 Group Master Command Center locked — parent-company internal operating system at `seven16group.com` for monitoring + controlling the family of products.** Master O directive 2026-05-28: "push this down to the other products so that they build their own command centers... ping them so they start to work on it as soon as possible." External (GPT) architecture spec reviewed, reconciled against locked doctrine (D-026 / D-027 / D-040 + security baseline), locked as the family-wide build. **CRITICAL ROLE-SPLIT (locked to prevent drift):** three central-ish surfaces with explicit roles — (1) **Group Hub** (`seven16group.com`) = INTERNAL ops/finance/security command plane; OWNS product registry + provider integrations + cost ledger + AI usage ledger + security event ledger + alert engine + audit log + kill switches + admin UI; MIRRORS (read-only) Stripe records + tenant identity + entitlements from Command; (2) **Seven16 Command** (`seven16command.com`) = EXTERNAL customer-facing CRM + customer-state system-of-record per D-027; OWNS authoritative tenants / tenant_memberships / tenant_product_entitlements + customer profile + cross-product customer state; (3) **saas-agency-database** = INTERIM home for central billing/entitlement truth per D-040 phased path until Command matures. Group Hub does NOT own tenant identity or entitlement data — same pattern as Stripe (Stripe is SoR for billing; Group Hub mirrors for ops dashboards). **GROUP HUB OWNS (tables in its own Supabase project):** `products` (family product registry) · `provider_accounts` + `provider_resources` (Stripe / OpenAI / Anthropic / Vercel / Supabase / Sentry / GitHub / Cloudflare / Resend / etc.) · `cost_events` (every billable provider call tagged by product + tenant + user) · `ai_usage_events` (every Claude/OpenAI call with tokens + cost + latency + abuse flags) · `security_events` (every authorization denial + cross-tenant attempt + WAF event) · `alert_rules` + `alerts` (alert engine config + open/resolved) · `audit_log` (every admin write with before/after diff). Full schema per GPT plan §10. **FAMILY-WIDE ROLLOUT — every satellite MUST do two things:** (A) wire HMAC-signed telemetry POSTer to `${SEVEN16_COMMAND_CENTER_URL}/api/internal/events` emitting `ai.usage` + `cost.usage` + `security.event` + `product.health` + `billing.event` events (canonical shape captured in `docs/cross-repo/family-master-command-center-prep.md` so every satellite copies the same wire); (B) build a per-product control room at `/admin/products/<product_key>` with §20 tabs (Overview / Revenue / Costs / AI usage / Security / Infrastructure / Deployments / Alerts / Tenants / Settings). Cross-repo prep artifact at `saas-agency-database/docs/cross-repo/family-master-command-center-prep.md` carries paste-ready BACKLOG entries for DOT Intel / Agency Signal / Seven16 Email / Seven16 Survey / Seven16 Command (SPECIAL bidirectional role) / Bind Lab / Seven16 Academy / DotCarriers / DotAgencies / Seven16 Group Support — each satellite's next session reads its block + opens the work. **BUILD PHASES (Group Hub itself, in seven16-group-site repo):** Phase 1 foundation (product registry + provider accounts + Stripe read-only sync + the 7 ledger tables + `/api/internal/events` HMAC endpoint + basic portfolio dashboard + RLS per security baseline + Edge Function workers NOT Railway/Render/Fly per D-040) ~3 sessions; Phase 2 billing + cost control (full Stripe sync + provider connectors + cost dashboards + tenant 360 mirror + gross margin formula) ~2-3 sessions; Phase 3 security + abuse (authorization logging + cross-tenant detection + AI abuse signals + Cloudflare WAF + Sentry integration + kill switches at product/tenant/user/agent levels) ~2 sessions; Phase 4 advanced ops (alert rules engine + seed rules per GPT §19 + incident management + cost forecasting + product health scoring + Stripe admin-writes with two-person approval on production pricing) ~2-3 sessions; Phase 5 enterprise-grade (SOC-style control + evidence collection + compliance + vendor risk register + product readiness scorecards) ongoing. ~10-15 dedicated seven16-group-site sessions to reach Phase 4 production-ready; each phase independently valuable + shippable in-place. **15 non-negotiable engineering rules** per GPT §23 all apply + reconciled with locked doctrine (audit log on every write per D-040; HMAC on every internal endpoint per D-027 Practice 8; provider secrets in Supabase Vault not plaintext; RLS everywhere per security baseline; no AI call without usage logging — the NEW family rule; no production provider sync without rate-limit handling; alert dedupe + cooldown mandatory). **Definition of done (Phase 4):** Seven16 can answer 20 ops/finance/security questions in under 30 seconds from Group Hub dashboards (per GPT §24). Full spec at family memory `project_seven16_group_master_command_center.md`. | 2026-05-28 | (1) **External validation again** — GPT's spec independently re-derived the multi-tenant + entitlements + RLS + audit + HMAC architecture locked in D-040 yesterday; the value is the NEW layers (cost ledger / AI usage ledger / provider connectors / alert engine / kill switches). Two model reviews 24 hours apart landing on the same foundation is high-confidence validation. (2) **Role-split locked now because three "central" surfaces is the drift trap** — Group Hub + Command + saas-agency-database all have plausible claims to "the central system." Without explicit roles, every product's entitlement check could end up calling a different home and the system fragments. The split (internal ops = Group Hub; customer-state SoR = Command per D-027; interim billing host = saas-agency-database per D-040) keeps doctrine consistent. (3) **Cost + AI accounting was the genuinely missing layer** — D-040 covered runtime auth/entitlement; D-042 covers ops/cost/abuse. Together they form the full operating standard. The family is about to ship Stripe enablement (the saas-agency-database SKU work coming next) — landing the cost/AI ledger BEFORE customers + AI agents scale up means we never operate blind on margin or abuse. (4) **Per-satellite responsibilities push down via cross-repo prep, not direct ping** — each satellite's next session opens, reads `docs/cross-repo/family-master-command-center-prep.md`, finds its block, opens the work. No Master O coordination overhead per satellite. (5) **15-rule discipline** prevents the operating-system from becoming yet another monitoring tool nobody trusts — every rule has a teeth-in-doctrine reason (audit log = D-040; HMAC = D-027 Practice 8; etc.). (6) **Phased build** lets seven16-group-site session ship Phase 1 (the endpoint + foundations) within ~3 sessions and immediately satellites can start wiring telemetry — no big-bang multi-month wait for value. |
| **D-041** | **Seven16 Survey / Activator pricing v1 locked — Solo/Agency/MGA tiers + standalone $297 vertical packs. NO founder tier.** Per D-034 per-product autonomy. **Subscription tiers:** Solo $49/mo ($470/yr) 1 seat / Agency $149/mo ($1,428/yr) 5 seats (+$15/mo each extra) / MGA $499+/mo annual-contract 25 seats (volume negotiated). Every tier includes ALL released vertical packs (Trucking today; Restaurant + HVAC next; Workers Comp / Contractor / Commercial Auto / GL-BOP / Property / Umbrella / Garage queued). **Standalone vertical pack:** $297 lifetime, one-time (no subscription), single vertical; validation-experiment SKU; 30-day refund if <10 surveys sent; switch-to-subscription keeps the pack + adds others at tier with no double-charge. **Refunds:** subscriptions 7-day pro-rated; pack 30-day-if-<10-sent. **No setup fees.** Annual = 2 months free. Entry point: Solo $49/mo or the $297 one-time pack. Referral attribution via `?ref=CODE` routes to partners.seven16group.com (commission lives in the Partner Program, not this product). Regulatory guardrail on page: "does not bind insurance coverage, generate quotes, or constitute regulated insurance advice." Full copy at family memory `project_seven16_survey_pricing_v1.md`. **FOUNDER TIER REMOVED:** the initial draft included a Founder Tier ($29/mo lifetime, first 50, lifetime-locked rate carrying through every future feature). Master O REMOVED it 2026-05-27 because it was structurally the Charter Member program killed by D-034 + rolled back by D-039 — a capped early cohort with a permanently-locked recurring rate + "every future feature included forever." Do NOT re-introduce a founder/charter/lifetime-locked-recurring cohort for Survey (or any product) without an explicit decision superseding D-034. The standalone $297 one-time pack STAYS — it's a buy-it-outright SKU (pay once, own one vertical), not a capped cohort with a locked recurring subscription rate; the two are categorically different. **Status change:** Survey/Activator moved pre-code → partially-shipped + selling (Lane B shipped 2026-05-26); supersedes the earlier pre-code status in the catalog + security baseline; prohibited-claims P11 now satisfied for shipped surfaces (keep queued-feature labels clear). **Naming flag:** customer copy uses "Seven16 Activator"; domain is seven16survey.com; catalog says "Seven16 Survey / Activator" — dual-naming queued for a consistency decision. | 2026-05-27 | (1) **Per-product autonomy in action** — D-034 decentralized pricing so each product ships its GTM cadence without family coordination; Survey's transparent 3-tier + one-time pack is exactly that. (2) **Founder tier removed for D-034 consistency** — even a single-product founder cohort reproduces the exact shape D-034 retired (capped lifetime-locked recurring rate + every-future-feature-forever promise + the operational drag of a permanent cohort flag + per-feature cohort price variant). Master O's call: not worth carrying the same drag D-034 just eliminated. The $297 one-time pack survives because one-time lifetime ownership of a single feature ≠ a recurring-rate cohort. (3) **Transparent competitive framing** — generic vertical CRMs $97-249/mo (no activation system); lead-gen agencies $500-2,000/mo (no kanban/scoring transparency); Solo undercuts CRM band, Agency competes with lead-gen band but ships a working pipeline, MGA is where wholesalers expect to pay for submission tooling. (4) **Hybrid one-time + subscription** — the $297 pack is a deliberate validation experiment (one-time, lifetime, single vertical) that de-risks the subscription motion by proving willingness-to-pay per vertical. (5) Locked so the next Survey pricing-page + Stripe-SKU session has tiers, annual Prices, per-seat overflow, and one-time-pack `payment`-mode all spec'd — with no founder-cap logic to build. |
| **D-040** | **Family platform architecture standard locked — multi-tenant isolation + centralized billing/entitlement truth + per-product DBs that never trust the UI.** Triggered by an external (GPT) architecture review Master O brought in 2026-05-27; review independently re-derived ~90% of locked doctrine (D-026 core thesis verbatim: "Stripe decides what someone PAID for; entitlement tables decide what they CAN USE; RLS decides what rows they CAN SEE; app code only PRESENTS"). This decision locks the standard that reconciles the review with existing doctrine + adds the genuinely-new pieces. **Already-locked + re-validated:** one Stripe account (D-026), entitlements-table-as-runtime-authorization (D-026), central idempotent webhook handler (D-026/D-028), 4-tier RLS + anon-revoke gotcha + pre-launch audit recipe (`reference_family_supabase_security_baseline.md`), Command as system-of-record (D-027), keep Supabase over Neon. **NEW family standards locked by D-040:** (1) **Multi-tenant layer** — `tenant_id` on every customer-owned business table (future-proofing even for solo-prosumer products; solo user = 1 tenant), `tenant_memberships` with roles (owner/admin/member/viewer), tenant-membership-based RLS policies with mandatory `with check` on writes + both `using`+`with check` on updates (prevents cross-tenant insert + record-move leaks). Tenant DEPTH varies by product: full membership UI for Bind Lab / Command / Email (genuine teams); light for Agency Signal / DOT Intel (prosumer w/ seats on Pro tiers); `tenant_id` everywhere regardless. (2) **3-level access model** — tenant membership AND product entitlement AND user role, checked server-side via `has_product_access()` SECURITY DEFINER fn; UI hide + API 403 + RLS block = three independent gates. (3) **`tenant_entitlements` schema** extends D-026 billing schema with tenant + feature_id granularity. (4) **Index-on-RLS standard** — every column used in an RLS policy / join / filter / export / search / dashboard gets an intentional index (Supabase-documented perf gain). (5) **Worker-runtime standard** — background jobs run in Supabase Edge Functions (already in stack via `EDGE_FN_AUTH_SECRET`); do NOT add Railway/Render/Fly (violates `feedback_no_paid_services_until_revenue` + ops burden). (6) **Dev/staging/prod separation** — never test against prod; prod DB creds unavailable to dev experiments (reinforces D-027 Practice 5 + `feedback_supabase_dashboard_via_master_o_not_chrome_mcp`). (7) **Per-product rollout audit checklist** (access-control + billing + environment) baked into the standard. **CENTRAL-TRUTH HOME DECISION (the one fork only Master O could call):** Seven16 Command is the DESTINATION system-of-record; saas-agency-database HOSTS the central billing/entitlement truth in the interim (already built + working for Agency Signal); phased migration to Command as it matures (greenfield Phase 1, baking the tenant + entitlement schema in from day one to match the canonical shapes so the migration is a data move, not a re-model). Rejected the external review's "dedicated 4th standalone central DB" — Command already IS the SoR per D-027; a separate central DB = a 2nd source of truth, which D-027 forbids. Canonical standard at family memory `reference_family_platform_architecture.md`. | 2026-05-27 | (1) **External validation de-risks the architecture** — an independent model arriving at the same Stripe/entitlements/RLS split as D-026 is confirmation the foundation is sound, not a signal to change course. The value of the review was the NEW layers it surfaced (tenancy, 3-level access, index standard), not the 90% it re-derived. (2) **The tenant layer is the real gap** — the security baseline's 4-tier model is user-ownership (`user_id = auth.uid()`); it has no team/company tenancy with seats + roles. Bind Lab (wholesaler w/ many producers) + Command (CRM) + Email (agency senders) are genuinely team-multi-tenant and cannot ship correctly without it. Putting `tenant_id` everywhere now — even on solo-prosumer products — costs one column; retrofitting it onto live data later costs a full migration. (3) **Central-truth home: phased Command migration beats both alternatives.** Keeping it permanently in saas-agency-database would demote Command from system-of-record to consumer, contradicting D-027. Building a dedicated 4th DB now would create throwaway work (the Stripe webhook would move twice) + a 2nd source of truth. The phased path keeps today's working billing in place + routes to Command as the canonical destination D-027 already named — no throwaway, no contradiction. (4) **Worker runtime = Edge Functions, not new infra** — the review listed Railway/Render/Fly as options; adopting them would violate the family's no-paid-services-pre-revenue rule + add ops surface. Edge Functions are already proven in Agency Signal's saved-list refresh. (5) **Locking as a written standard means every product session reads ONE doc** to stay on the same page — prevents each product independently re-deriving tenancy/access patterns + drifting. Surface on ANY schema / auth / billing / entitlement / multi-tenant work in ANY family repo. |
| **D-039** | **Charter Member code rollback — executes D-034 family-wide kill in Agency Signal codebase.** D-034 (locked 2026-05-26) killed the Charter Member program family-wide ("we are not doing charter member program anymore or family pricing. Its too confusing."). D-039 executes the code-level rollback in the Agency Signal repo specifically. **Removed:** (a) `app/api/stripe/checkout/route.ts` — `charterIntent` query-param parsing, customer-metadata tagging (`charter_member="true"` + `charter_enrolled_at`), Charter Coupon auto-attach (`STRIPE_CHARTER_COUPON_ID` env var + `discounts` payload), `allow_promotion_codes` Charter-conditional flip, `charter_member` field in `baseMetadata`, charter-preserving sign-up redirect. Checkout route restored to plain plan-code-only flow. (b) `app/api/stripe/webhook/route.ts` — `ensureCharterCouponAttached` helper deleted; every call-site removed from `checkout.session.completed` (subscription branch) and `customer.subscription.updated`; `charter_member_enrolled` Command CRM push removed from `checkout.session.completed` (subscription branch); `charter_member` field removed from event_data payloads in `pushCommandFromSession` + `pushCommandFromSubscription`; welcome-email template selection collapsed from 3-way (charter / growth / snapshot) to 2-way (subscription → `growth_member_welcome`, payment → `snapshot_purchase_welcome`). (c) `lib/family-integrations/email.ts` — `charter_member_welcome` removed from `EmailTemplateKey` union. (d) `lib/family-integrations/command.ts` — `charter_member_enrolled` removed from `CommandEventType` union + doc comments + example idempotency-key list. **Predecessor handoff marked SUPERSEDED:** `docs/handoffs/SESSION_38_CHARTER_DOCTRINE_HANDOFF.md` prepended with SUPERSEDED banner (body preserved as historical record). **Family memory marked PARTIALLY SUPERSEDED:** `reference_stripe_bundling_flow.md` prepended with banner explaining which sections are dead (Charter Member mechanics) vs still valid (4-layer architecture, 3 bundle SKU patterns, lifecycle signal triggers). **Master O dashboard cleanup pending** (NOT Claude work): deactivate Stripe Coupon `L1Ngigfc` in dashboard; archive any test Charter SKUs in `acct_1TLUF6HmqSDkUoqw` sandbox catalog; `agencysignal.io/charter` page rewrite to "program ended" notice (if still live). **What survives:** the welcome-email infrastructure (`sendEmail` client + `sendWelcomeForSession` helper) and the Command CRM push infrastructure (`pushCommandEvent` client + `pushCommandFromSession` + `pushCommandFromSubscription`) both retained — they work for the non-Charter flow. Five remaining Command event types (`customer_signup` / `subscription_started` / `subscription_changed` / `subscription_cancelled` / `snapshot_purchased`) + two remaining email templates (`growth_member_welcome` / `snapshot_purchase_welcome`) carry the post-Charter checkout flow forward. | 2026-05-27 | (1) **Stripe-files cleanliness for the new 4-tier SKU build.** Master O is about to spin up Stripe enablements for the family products on the one parent LLC account (per D-026). Doing the rollback NOW while in the cutover session leaves `app/api/stripe/checkout/route.ts` + `app/api/stripe/webhook/route.ts` at a known-clean baseline for whoever wires the new SKUs — they don't have to first reverse-engineer which Charter branches to delete before adding the new 4-tier branches. (2) **Avoiding stale-doctrine code shipping.** D-034 has been locked since 2026-05-26; every day the Charter code stays in `main` is a day the next session reading it might believe it's current. The SUPERSEDED banner on `SESSION_38_CHARTER_DOCTRINE_HANDOFF.md` + the deletion of the actual branches together create the no-ambiguity state. (3) **Preserves replaceable infrastructure, removes dead specifics.** The welcome-email + Command-push scaffolding has value beyond Charter (every paid signup gets a welcome + every paid signup pushes a `customer_signup` event); deleting that infrastructure would be over-rotation. The Charter-specific branches were narrowly scoped enough that removing them is mechanical, not architectural. (4) **One-commit-with-D-038 because both are pricing-related cleanup.** D-038 (Seven16 Email pricing lock) + D-039 (Charter rollback) are both responses to the same underlying theme: "post-D-034 per-product autonomy needs explicit per-product pricing locks + dead-code cleanup of the killed family-wide model." Committing together creates one clean audit trail entry for "post-D-034 cleanup pass." |
| **D-038** | **Seven16 Email pricing v1 locked — 7-tier subscription ladder + Enterprise.** Per D-034 per-product-pricing-autonomy, each Seven16 family product picks its own pricing model. This locks Seven16 Email's: **Starter $59/mo · Growth $119/mo · Pro $179/mo · Advanced $349/mo · Agency $499/mo · Scale $799/mo · Enterprise custom starting at $1,500/mo** (REFINED 2026-05-28 per the email repo's D-E029: Starter $49→$59, Agency $449→$499; all other rows unchanged). **Annual = 15% off** (Starter $50/mo, Growth $101, Pro $152, Advanced $297, Agency $424, Scale $679). **14-day free trial** (100 contacts / 500 sends). **Per-tier gates (locked D-E029):** contacts + monthly-send caps scale by tier (Starter 1k contacts/10k sends → Scale 100k/1M); custom domains → Growth+; AI agent full-send → Pro+; dedicated IP → Scale+; overage +$10/1k contacts + $5/10k sends. Positioning locked: "AI-powered email campaigns, prospect nurturing, and compliant outreach for commercial insurance teams." Target buyers: agencies, wholesalers, MGAs, recruiters, insurance sales teams. Buyer comparison frame: cost of wasted producer time / poor list quality / bad follow-up / missed revenue — NOT primarily Mailchimp/ConstantContact commodity tier comparison. The $59/mo Starter floor is ~4.5x higher than Mailchimp's ~$13/mo entry because Seven16 Email is priced as a vertical operating tool, not as postage volume. **Billing rule locked verbatim:** "Plans include fair-use sending limits. High-bounce, purchased, scraped, or non-permissioned lists may require review, throttling, suppression cleanup, or dedicated sending infrastructure." Compliance posture, not just billing rule — protects the shared deliverability surface (`notify.seven16email.com` + `mail.seven16email.com`) for every consuming family satellite from a single tenant's reputation-damaging behavior. **Tier feature/quota differentiation NOW LOCKED** (was TBD at original D-038 lock; resolved 2026-05-28 in the email repo's v1.1 session, D-E029/D-E030/D-E031). Per-tier contacts + send caps + the custom-domain/AI-agent/dedicated-IP gates + overage rates are all defined (see above). Anchoring: prices set to Resend marketing-contact cost ÷ 0.65 (35% gross margin) + $25/mo infra allocation per active paid account. **Authoritative detail now lives in the email repo's `project_seven16_email_pricing_locked.md`** (D-E029/030/031); the family-memory `project_seven16_email_pricing_v1.md` mirrors the final numbers. Implementation notes for the future pricing-page build (positioning, tier-card structure, fair-use clause placement, Enterprise CTA, no-Charter-language, Stripe SKU pattern) captured in family memory `project_seven16_email_pricing_v1.md`. | 2026-05-27 | Master O directive 2026-05-27: pricing model + positioning + billing rule landed mid-cutover-session as a capture-now-before-it-gets-lost moment. Three rationale threads: (1) **Premium-vs-commodity positioning** — Mailchimp/ConstantContact at $13–$25/mo entry tiers treat email as postage; that's the wrong frame for a vertical operating tool serving commercial-insurance distribution where one missed renewal exceeds the year's email subscription cost by 100x. Per-product autonomy under D-034 lets Seven16 Email choose its own price/positioning shape without coordinating across the family. (2) **Tier ladder depth (7 tiers) vs Agency Signal (4 tiers) vs DOT Intel (3+credits)** — different products earn different ladder shapes because their value metrics differ. Email serves teams of widely varying size (solo producer at Starter $49 → MGA fleet at Scale $799 → carrier program at Enterprise $1,500+); a flatter ladder would force-fit personas into tiers that don't match their actual usage. (3) **Fair-use billing rule** is mandatory because Seven16 Email runs on shared sender domains (per `reference_seven16_email_platform.md` invariants 1 + 10 — "warmup stages enforced Stage 0 unverified → Stage 5 mature"); a single bad tenant can damage platform-wide deliverability across every family satellite that consumes the API. The billing-rule language is the contract that lets the platform throttle / require dedicated IPs / require suppression cleanup BEFORE sending — protecting downstream products. Aligned with industry-standard practice (Mailchimp / SendGrid / Postmark all enforce similar list-hygiene gates on shared-IP infrastructure). Captures Master O's recommendation verbatim so the next pricing-page session has the hero copy + tier ladder + compliance posture all locked. |
| **D-037** | **Family URL + brand updates 2026-05-27.** Three coordinated changes: (1) **Agency Signal canonical domain rotated from `agencysignal.co` → `agencysignal.io`** — second cutover (first was `directory.seven16group.com` → `agencysignal.co` per SESSION_38). DNS + Vercel + SSL already live on `.io` (Master O confirmed); code surfaces updated this session — `next.config.mjs` 301 redirect chain updated to point both legacy hosts directly at `.io` (skipping the `.co` middle hop to avoid redirect chaining); `lib/stripe/server.ts` APP_URL fallback flipped to `.io`; `.env.local.example` template + comments updated; `app/privacy/page.tsx` legal-policy hostname text updated to list both `.co` and `directory.seven16group.com` as legacy 301 sources; `app/admin/integrations/page.tsx` `PUBLIC_INTEGRATIONS_URL` flipped; `scripts/reliability/smoke-test.ts` default + comment flipped; `platform/migrations/0003_platform_seed.sql` seed value updated for fresh installs; new migration `0007_agency_signal_domain_cutover.sql` UPDATEs the existing production row idempotently (the 0003 seed is not re-run by Supabase). All 4 `.support/*.md` knowledge files swept so the support widget LLM quotes the new domain. 18 operator-facing docs swept (AGENTS, CLAUDE, STATE, SECURITY, LAUNCH_CHECKLIST, BACKLOG, FAMILY_HEALTH, SESSION_STATE, MASTER_CONTEXT, PRODUCT_REPO_INDEX, PRICING_LEAD_DOWNLOADS, ENGINEERING_DOCTRINE, agency-signal-status.html, support-integration-readiness, dotintel_demo_walkthrough). Historical session handoffs intentionally NOT swept (snapshot-in-time records). (2) **Seven16 Email canonical URL normalized from `api.seven16email.com` → `seven16email.com`** — root-domain pattern matches seven16command.com / seven16survey.com / agencysignal.io (family convention). Sender domains `notify.seven16email.com` (transactional) + `mail.seven16email.com` (marketing) unchanged. Wire impact: Vercel env var `SEVEN16EMAIL_API_URL` in every consumer satellite should now point at `https://seven16email.com` (path `/api/send` unchanged). `lib/family-integrations/email.ts` doc comments updated. (3) **Bind Lab Academy renamed to Seven16 Academy at `seven16academy.com`** (supersedes D-029 academy-domain portion). Brand moves out from under the Bind Lab sub-brand and under the holdco umbrella (matching the Seven16 Email / Survey / Command convention). Product scope unchanged — only customer-facing brand + domain move. Stripe slug + source_product slug + GitHub repo + folder-path renames PENDING (queued for the dedicated greenfield-build session for Seven16 Academy v1, so disk + GitHub + Stripe rename in lockstep with code build, no half-renamed state on `main`). Family memory `reference_family_product_catalog.md` updated this session with the new brand + URL + Cascade-work-pending table. **Master O dashboard actions pending:** (a) Stripe webhook endpoint URL rotation from `https://agencysignal.co/api/stripe/webhook` → `https://agencysignal.io/api/stripe/webhook` (signing-secret preserved on URL-only edit per SESSION_38 pattern); (b) Vercel: confirm `agencysignal.io` is the PRIMARY domain (not just an added domain — the Primary flag controls canonical-URL emission); (c) eventual `seven16academy.com` DNS + Vercel + SSL provisioning when the Seven16 Academy greenfield session lands. | 2026-05-27 | (1) **`agencysignal.io` is the long-term canonical** — `.io` reads as the production-grade brand domain (more globally-recognizable than `.co` in commercial-insurance distribution markets); the `.co` was a fast cutover from `directory.seven16group.com` in SESSION_38 but never the final home. Doing the second cutover NOW unblocks Master O's "create Stripe enablements for products using the one parent account ASAP" directive — Stripe Checkout success_url + webhook + receipts emit the canonical URL, so any Stripe SKU shipped with `.co` baked in would need to be re-shipped later. (2) **Seven16 Email URL normalization** aligns with family-wide naming pattern; the original `api.seven16email.com` plan would have been an outlier vs. seven16command.com / seven16survey.com / etc. Cheap fix to make NOW before satellites start hardcoding the old URL in env-var examples and onboarding docs. (3) **Seven16 Academy rebrand** puts the credentialing layer at the same hierarchical level as the other Seven16-named shared services — consistent with how Master O thinks about the holdco vs. sub-brand split. Bind Lab Academy was a 2026-05-25 working name; today's correction is the proper Seven16-umbrella positioning. Critically: the rebrand was caught BEFORE the dedicated greenfield-build session for the Academy started, so no code rename needed today — only the brand + domain + catalog entry, which is a 5-line change. The technical-identifier cascade (folder + repo + Stripe slug + source_product slug + env var prefix) is queued for the actual build session so disk + GitHub + Stripe move in lockstep with v1 code. **Cutover atomicity pattern locked:** SESSION_38 established that Stripe-webhook URL rotation preserves the signing-secret on URL-only edit; this session re-uses the same pattern, so STRIPE_WEBHOOK_SECRET in Vercel stays unchanged across both cutovers (no re-rotation needed). Future cutovers should follow the same "edit URL in Stripe dashboard, leave signing-secret alone" path. **Historical handoffs intentionally NOT swept:** SESSION_<NN>_HANDOFF.md / SESSION_<NN>_PROMPT.md / AGENCY_SIGNAL_SESSION_<N>_*.md files are snapshot-in-time records of what was true when the session ran; rewriting them to say `.io` would falsify the historical claim that SESSION_38 cut over to `.co`. The 301 redirect chain handles inbound link rot from those historical surfaces. |
| **D-034** | **Family pricing + Charter Member program KILLED. Per-product pricing autonomy adopted. Agency Signal moves to 4-tier transparent buying model. Supersedes D-014 / D-015 / D-018 / D-021.** Master O directive 2026-05-26: "we are not doing charter member program anymore or family pricing. Its too confusing." **What's killed:** (a) **Universal credit currency** ($0.15/credit base + cross-product bonus bands per D-021) — no more shared credit wallet across DOT Intel / Agency Signal / DotCarriers / etc. (b) **Charter Member program** (cap 50–75 / 50% lifetime / "best pricing tier permanently" per D-018 + D-021 amendment) — no more lifetime-locked early-adopter cohort across the family. (c) **Enterprise+ state-based slider** for AS ($1,000–$2,000/state, $12,500 all-50 ceiling, 50% Neilson undercut per D-015) — replaced by flat **$12,500/yr National Founder License**. (d) **Cross-product bundle discount mechanics** — products no longer coordinate pricing; each family member prices independently. **What replaces it for Agency Signal:** transparent 4-tier model targeting Neilson-style opaque-quote vendors via lower friction + self-service. **(1) Sample — $75 one-time** = 50 filtered commercial-insurance contacts + full records + one CSV export. Purpose: paid trial; intentionally per-contact-more-expensive than bulk so it doesn't cannibalize real deals. **(2) Monthly Subscription — Starter $299/mo / Growth $599/mo / Pro $999/mo** with annual discount ($249/$499/$849) = ongoing access via export credits (250 / 1,000 / 3,000 per month), seats (1 / 3 / 5), filters/saved-lists/segmentation by tier. **(3) Bulk Build-Your-File — starts at $500** with sliding per-record pricing: 100–499 records @ $1.20 / 500–1,999 @ $0.90 / 2,000–4,999 @ $0.70 / 5,000–14,999 @ $0.50 / 15,000+ @ $0.40 or sales. **(4) National Founder License — $12,500/year** = full U.S. database access, annual license, founder-rate positioning. Full proposal copy lives in family memory `project_agencysignal_pricing_v2_neilson_alternative.md` (queued — see Phase E task). **Per-product autonomy implications across the family:** (i) DOT Intel decides its own pricing — Producer + Business tier framing per D-002 is the natural baseline but no longer family-coordinated. (ii) Bind Lab + Bind Lab Academy + DotCarriers + DotAgencies each price independently when their dedicated build sessions land. (iii) D-026 (Stripe billing architecture — ONE account, slug metadata) SURVIVES — payment-processing infrastructure is product-agnostic and works with any pricing model. (iv) D-027 (family API integration mesh) SURVIVES — Command CRM integration is not pricing. (v) D-028 (Stripe bundling flow + schematic) SURVIVES as an available pattern but no longer the family default — bundles can still be built per-product via Pattern A when natural, but no Family Pack SKU is on the roadmap. (vi) D-005 ($179/mo Seven16 Intelligence bundle = DOT Intel Business + Agency Signal Growth) is also implicitly retired — the AS Growth tier referenced no longer exists in the new model. **Strands to clean up (deferred to a follow-up "pricing migration" session):** Stripe sandbox catalog `acct_1TLUF6HmqSDkUoqw` has 6 D-021 surfaces shipped SESSION_25 (2026-05-18); each Product needs to be archived in dashboard or via Stripe MCP. Charter coupon `L1Ngigfc` needs deactivation. `agencysignal.co/charter` page retired in this session to "program ended" notice. `app/pricing/page.tsx` placeholder needs rebuild against the new 4-tier model. Family memory entries citing D-014/D-018/D-021 need supersession notes. `project_charter_member_program.md` marked superseded. `PRICING_CREDITS_AND_TOPUPS.md` + `PRICING_ENTERPRISE_LAYER.md` + `PRICING_STRIPE_CATALOG.md` + `PRICING_DIRECTORY_LISTINGS.md` + `PRICING_LEAD_DOWNLOADS.md` + `PRICING_LEARNING_CENTER.md` + `PRICING_THRESHOLD_IQ.md` + `PRICING_DOT_ALERTS.md` all become historical docs (banner per the established pattern, file kept). D-023 positioning ("Distribution intelligence for commercial insurance") remains the canonical AS positioning — the proposal's hero copy "Commercial insurance agent data, without the legacy pricing" is HERO-LEVEL copy, not positioning supersession; the pricing-page implementation can use the agent-data framing while body-level marketing surfaces continue the distribution-intelligence claim. | 2026-05-26 | (1) **Coordination cost > coordination value.** Family-shared pricing meant every pricing decision in any product cascaded across all six. Master O reported "too confusing" — the cognitive overhead of universal credits + Charter Member exceptions + per-product overlays + cross-sell bonus tiers exceeded the strategic upside, especially pre-revenue when no customer has felt the family-pricing experience yet. (2) **Transparency beats clever for cold buyers.** The Neilson-alternative wedge needs price visibility on the page before the prospect talks to sales; opaque-quote vendors lose deals on price-page bounce rate. Universal credits made the AS pricing page require an explainer; flat tiers + sample + per-record bulk + national license make the price legible in 5 seconds. (3) **$75 paid sample reduces friction without cannibalizing.** 50 contacts × ~$1.50/contact is intentionally more expensive than bulk per-record pricing — the sample is qualification, not the volume product. (4) **Per-product autonomy unblocks each family member's GTM cadence.** DOT Intel ships its pricing when DOT Intel is ready; Bind Lab Academy ships its $29 cert when ready; AS ships its 4-tier model now. No more "wait for the family-pricing session to lock D-XXX" blocker. (5) **D-018 Charter Member math was acceptable as growth spend (~$270k over 5 years) but the operational drag was higher than the math implied** — every product's pricing page had to footnote Charter; every Stripe SKU needed Charter price variants; the "best pricing tier permanently" promise required a complete entitlements-table column per family product. Killing the program cleans the slate. (6) **Concerns from the proposal acknowledged but accepted:** (a) $12,500 national license may become a "forever ceiling" — accepted with the explicit founder-rate framing that signals it as introductory; (b) page-simplicity discipline (sample / monthly / custom / national) — locked, no add-ons or special cases approved without superseding D-034; (c) clarity on what counts as an export + refresh cadence + included fields — built into the pricing-page implementation per separate task. **Supersedes D-014 (credits + top-ups), D-015 (Enterprise+ state-based slider for AS), D-018 (Charter Member program), D-021 (Universal Credit Currency + à la carte SKUs).** All four marked SUPERSEDED in-place with cross-reference. D-019 + D-020 already retired earlier (per D-022 TIQ spin-out + D-021 supersession). |
| **D-035** | **DOT Intel 8-module risk intelligence framework + Sprint 0 corpus backfill commitment.** Extends the locked 6-score trust-layer architecture (family memory `project_dotintel_trust_layer_architecture.md`) with **Chameleon Detection (Module 6)** + **Litigation Exposure (Module 7)** for an 8-module insurance-grade risk lineup. Modules 1-5 + 8 retain prior architecture; Modules 6 + 7 are net-new. **Per-module defensibility framework locked** — every module card answers 8 questions (signal / math / visualization / user takeaway / defense / reason codes + bindingUse / maturity + evidence requirements / for ❌ modules: gap detail + path to ✅). **Corpus reality discovered + plan locked:** dotintel2 `public.carriers` count = 50,298 (verified via SQL during AS S39) — ~2-3% of full FMCSA universe of ~1.86M-2.6M; `source_last_refreshed = 2026-04-13` frozen at original seed; `authority_granted_date = 0/50,298` populated. Daily SODA cron processes deltas post-watermark; has never run a historical-universe pull. Sprint 0 plan: Path B filtered backfill (motor-carrier × active common authority → ~250-300k rows) + MCS-150 detail companion ingest. **Additional bug surfaced:** `/dashboard/territory-intelligence` Top-States table capped at exactly 1,000 carriers/state (HI/AK/CO/MN/NM/GA/OK/NY/FL/KY all show 1000) — `LIMIT 1000` smell; corrupts addressable + penetration math. Fold into Sprint 0. **Sequencing: Sprint 0 (corpus + UI cap) → Sprint A (Violation Intensity + Trend Forecast calibration) → Sprint B (Chameleon Detection) → Sprint C (Litigation Exposure) → Sprint D (Market Fit ship).** ~10-14 dotintel2 sessions total to ship all 8 modules at ✅ Preview maturity. Canonical artifacts: family memory `project_dotintel_8_module_risk_spec.md` + `project_dotintel_corpus_backfill_plan.md`; cross-repo prep at `saas-agency-database/docs/cross-repo/dotintel-risk-module-defense-matrix.md` + `dotintel2-backlog-addendum-trust-layer-sprint-abcd.md` (to move into dotintel2/docs/specs/ + BACKLOG.md in next dotintel2 session per Rule 2(b)). **Master O pre-decisions:** (a) Sprint 0 Path A unfiltered vs Path B filtered (recommended: B); (b) Supabase Pro disk allocation confirm; (c) Sprint C state-litigation data source (recommended: ATRI Tort Cost Index). | 2026-05-26 | Multiple drivers converging: ChatGPT CTO review surfaced Chameleon + Litigation as competitive moats (no competitor — CHIP / eCarrierCheck / DOT Analysis / InsuranceApps — has both); Master O directive "make sure each module applies specific value and signal and detail and insights and we can describe and defend why it's important" forces per-module defensibility discipline; corpus reality discovery proves we've been operating on a sample (credibility issue, not just coverage). Doing all three together (8-module spec + corpus backfill + per-module cards) ships a credibility leap before charter outreach scales. Sprint 0 must land first — every module above operates on a sample until then. |
| **D-036** | **DOT Intel pricing strategy v1 — usage-based credits + light subscriptions.** Per-product pricing autonomy under D-034: each Seven16 family product picks its own pricing model independently. DOT Intel chooses **usage-based credits** (lookup + PDF = 1 credit) + optional **light subscriptions** for power users — NOT a tier-style CRM stack like InsuranceApps/CHIP. **3 layers:** (1) **Free tier** — 10 lookups/mo, core carrier snapshot + basic risk signal, no card required; (2) **Pay-as-you-go credit packs** — 100/$49 ($0.49/lookup), 500/$199 ($0.40), 2,000/$599 (~$0.30), 10,000/$2,499 (~$0.25); credits valid 12 months from purchase; (3) **Light subscriptions** — DOT Starter $49/mo (150 lookups, 1 seat, $0.40 overage), DOT Team $129/mo (500 lookups, 3 seats, $0.30 overage), DOT Pro $299/mo (1,500 lookups, 5 seats, $0.25 overage). 1 credit = full intelligence view + downloadable PDF report. **Positions DOT Intel as intelligence layer, NOT a CRM** — explicit differentiator vs InsuranceApps/CHIP ("from DOT lookup to bound policy" full submission workflow + $1,500/mo tiers). Lighter, usage-based, more like enrichment API or data tool than full app subscription. Credits model lets even solo producers / niche MGAs justify a small spend to start, then ramp as they feel value. Paste-ready website copy (hero / How pricing works / credit pack section / monthly plans / try-it microcopy / FAQ) drafted in family memory `project_dotintel_pricing_strategy_v1.md` for next dotintel2 `/pricing` session. **Implementation:** Stripe catalog under shared parent LLC account `acct_1TLUF6HmqSDkUoqw` per D-026; slugs per `reference_stripe_billing_architecture` (`dotintel_credits_100`, `dotintel_starter_monthly`, etc.); usage-tracking via Supabase webhook → `credit_wallet_balance` table; free tier rate-limited at dashboard layer (existing `lib/log-dot-search.ts` hook); **open questions for Master O at pricing-session open:** (a) "DOT lookup credit" ≡ "prospect pack credit" or distinct currencies; (b) Free Tier 10/mo applies anon + auth or auth-only; (c) credit-pack expiration window default 12 months. | 2026-05-26 | Master O directive 2026-05-26 — pricing strategy delivered mid-AS-S39 for next dotintel2 `/pricing` session. Per-product autonomy under D-034 means DOT Intel doesn't inherit Agency Signal's 4-tier transparent buying model; the products have fundamentally different value metrics (Agency Signal sells contact-density; DOT Intel sells per-lookup intelligence). Usage-based credits map cleanly to DOT Intel's wedge ("type a USDOT, get a one-page report") in a way Agency Signal's subscription tiers don't. Lighter pricing also lets solo producers + niche MGAs onboard at $49 without procurement friction — captures the long tail competitors price out. Aligns with industry-standard $0.25-$0.50/enriched-record range for B2B data APIs. |
| **D-033** | **Family product catalog locked — canonical single source of truth for "what does each Seven16 product do?"** Per-product: one-liner / paragraph / code home / repo / Stripe slug / status / integration role for each of the 6 customer-facing products (DOT Intel, DotCarriers, DotAgencies, Agency Signal, Bind Lab, Bind Lab Academy) + 4 shared infrastructure services (Seven16 Group Hub, Seven16 Email, Seven16 Command, Seven16 Survey) + 1 retired (Threshold IQ spun out per D-022). Quick-reference table maps Stripe slug + `source_product` slug + pull lookup key per product for cross-product API wiring. Canonical at family memory `reference_family_product_catalog.md`. **Surface this when scoping anything cross-product** — handoffs, routing, integration code, marketing references. | 2026-05-25 | Three reasons: (1) every product session was independently re-deriving "what does my sibling do?" from scattered `.support/` files + family memory — single canonical source prevents drift + reduces context cost of cross-product work. (2) Bind Lab Academy launch as the 6th family product (D-029) created a coordination cost — every existing product's `.support/` needed updating to reference the new sibling correctly. Catalog formalizes the family-level abstraction. (3) Cross-product API integration work (D-027 + D-028) needs slug + lookup-key mappings in one place. Read order: catalog (what each product is) → integration mesh (how they connect) → bundling flow (how they sell together) → Stripe doctrine (where revenue lands). |
| **D-032** | **Bind Lab brand correction — customer-facing brand is "Bind Lab" (two words), not "Bindlab" (one word).** Sister product "Bind Lab Academy" (already two-word, locked D-029). **Technical identifiers UNCHANGED** — folder `bindlab/` + repo `gtminsightlab-cmd/bindlab` + domain `bindlab.io` + Stripe slug `bindlab` + env-var prefix `BINDLAB_*` all stay lowercase one-word forms (renaming has high blast radius + they don't take spaces). **Customer-visible brand strings get rebranded** across bindlab repo customer-facing copy, all `.support/` cross-product routing references, family memory + global CLAUDE.md mentions, Stripe `stripe_products.name`. **DONE 2026-05-25:** sed sweep across 36 `.support/*.md` files in all 4 repos + family memory + CLAUDE.md (commits `be18855` + `65f6a48` + `e40aab3` + `406da21`). **PENDING:** ~20 `bindlab/src/**` code files (BACKLOG #0a). | 2026-05-25 | Master O direct correction mid-session: parent product brand was incorrectly one-word in my earlier recommendation; both Bind Lab + Bind Lab Academy are two-word brands by intent. The split (brand has space, technical identifier doesn't) matches established multi-product SaaS conventions. Locking as D-032 prevents future drift back to one-word "Bindlab" under marketing pressure or accidental tooling outputs. |
| **D-031** | **Family-wide prohibited claim P12 — no third-party vendor naming in customer-facing content + founder-bio generality.** Added to `seven16-group-site/.support/prohibited-claims.md` as the 12th holdco-level rule; every product's `.support/prohibited-claims.md` inherits via the standard P1-P12 inheritance pattern. **No naming** specific competing vendors, named industry tools, or named external partners in any customer-visible surface (marketing pages, `.support/` content the support agent reads to customers, product UI strings, etc.). **Founder-bio generality** — references to "Master O" or any specific individual operator should be replaced with general operator-language ("our founding operator", "the family", etc.) on customer-facing surfaces; named operator references stay only in internal-facing docs. Per-product reinforcements live as P-suffix entries in each product's `.support/prohibited-claims.md`. | 2026-05-25 | (1) **Legal exposure** — naming competitors creates trademark + defamation surface area that scales linearly with mention count + product count. Family-wide ban is cleaner than per-product judgment calls. (2) **Brand consistency** — operator-led founder language scales poorly (founder turns into a single point of failure for brand voice across 6 products); generic "operator" framing keeps the family voice consistent + decouples brand from any individual. Locked at the holdco level so any new product satellite inherits it from day one. Already enforced via .support/ scrub passes 2026-05-23 (Neilson / ProgramBusiness) and 2026-05-25 (Vertafore + 7 other vendors in bindlab). |
| **D-030** | **DotCarriers + DotAgencies + Bind Lab Academy decouple from `dotintel2` monorepo into 3 standalone repos.** All 3 new folder paths + GitHub repos scaffolded 2026-05-25: `gtminsightlab-cmd/dotcarriers` at `C:\Users\GTMin\Projects\dotcarriers\` (domain dotcarriers.io); `gtminsightlab-cmd/dotagencies` at `C:\Users\GTMin\Projects\dotagencies\` (domain dotagencies.io); `gtminsightlab-cmd/bindlab-academy` at `C:\Users\GTMin\Projects\bindlab-academy\` (domain bindlab.app). All 3 initialized with README + .gitignore + initial commit on main + GitHub remote. **dotintel.io itself stays in `dotintel2`** as the authenticated intelligence platform. Cross-product Header nav stays as marketing cross-links (no shared code) — pattern per ADR-007 + D-016. **Code-level decouples deferred** to dedicated sessions per Rule 3. Recommended sequencing: Bind Lab Academy greenfield first (smallest scope — pre-revenue + 0 user data + 138 rows CMS-shape seed = cheapest migration moment), then DotAgencies (smallest authenticated surface), then DotCarriers. Per `project_dotcarriers_dotagencies_decouple_plan.md`. | 2026-05-25 | Three audiences, three GTM lifecycles, three brand surfaces — the monorepo brand-routing pattern (D-016 three-domain Option B host detection) carried us through positioning iteration but isn't the right long-term shape. Each surface gets its own deploy cadence + design system + analytics + claim-flow + monetization without coupling to dotintel.io product releases. Reduces blast radius — a regression in dotagencies.io marketing copy can't break authenticated `/dashboard` or `/carrier-intelligence` routes. Three repos = three Vercel projects = three sets of env vars, but no shared TypeScript / Tailwind / component drift risk. Greenfield rebuild for Bind Lab Academy specifically because mobile-first design priority + 0 user data + tiny CMS-shape seed = cheapest moment to reset architecture. |
| **D-029** | **Seven16 Learning Center → Bind Lab Academy is the 6th customer-facing Seven16 family product.** User-facing brand: "Bind Lab Academy" (two words). Domain: `bindlab.app` (sister to bindlab.io, mobile-first PWA). Internal taxonomy name "Seven16 Learning Center" preserved for DECISION_LOG / family memory consistency. Repo `gtminsightlab-cmd/bindlab-academy` at `C:\Users\GTMin\Projects\bindlab-academy\`. Stripe slug `bindlabacademy`. **v1 hosts the DOT Intel Certified Trucking Producer cert** (9 modules / 23 lessons / 106 flashcards, $29 via Stripe — the only LIVE monetization wedge in dotintel2 family pre-charter); designed for multi-program growth. **Mobile-first design priority** — UI works end-to-end on 320px-wide screens. **Cross-product credential loop:** cert earnings push `cross_product_entitlement_granted` events to Seven16 Command per D-027; Command fans out individual `entitlement_changed` webhooks to each affected satellite (DOT Intel access today; future: Agency Signal trial, Bind Lab early-access). Per family memory `project_seven16_learning_center.md`. | 2026-05-25 | (1) Different audience — dotagencies' platform tier targets carriers/MGAs/wholesalers; `/learn` targets individual producers earning credentials. Different acquisition funnels + brand voice + lifecycle motions. (2) Different evolution cadence — cert programs ship + iterate independently of agency-directory marketing pages. (3) Cross-product credential loop scales cross-family when Academy is its own product (cert-earner gets DOT Intel access today; future Agency Signal trial / Bind Lab early-access) without coupling those grants to dotagencies-marketing deploys. (4) Multi-program future — scoped from day one to host multiple curricula. Domain `bindlab.app` chosen because `.app` TLD signals mobile-first / PWA-friendly + sister-to-bindlab.io creates cross-product credential loop closer to point-of-sale. Greenfield rebuild because 138 rows CMS-shape seed + 0 rows user data = cheapest migration moment. |
| **D-028** | **Stripe bundling flow + schematic locked — how Stripe Products + Prices + Subscriptions + entitlements work TOGETHER to enable bundles + cross-sell + upgrades + Charter Member pricing.** **Four-layer architecture:** (1) Stripe catalog — one Product per family product + Prices per Product + Charter Member Coupon + synthetic "bundle" Products. (2) Customer subscription — ONE Stripe Subscription per customer with multiple SubscriptionItems (one-time purchases via separate PaymentIntent under same Stripe Customer). (3) Central webhook handler at `saas-agency-database/api/stripe/webhook` updates shared `stripe_*` mirror tables + `billing_events` + recomputes `entitlements`. (4) Satellite product checks read `entitlements` from local Supabase mirror — satellites NEVER call Stripe at request time. **Three bundle SKU patterns** documented (Pattern A "Container Product, multiple Prices" recommended default / Pattern B "Multiple SubscriptionItems with a Coupon" not recommended / Pattern C "Single Composite Subscription Tier" for top-tier Family Pack annual only). **Charter Member "best pricing tier permanently"** mechanics via persistent customer metadata + Stripe Coupon `CHARTER_FOREVER_15` with `duration: forever`; bundle Prices designed multiplicatively-compatible. **6 lifecycle signal triggers** for "when is the right time to bundle." Per family memory `reference_stripe_bundling_flow.md`. | 2026-05-25 | Master O directive: "create a method/process/flow chart and schematic on how Stripe will work together and add or bundle products together at the right time." D-026 documented the RULES; D-028 documents the FLOW + operating method + schematic. Key idea: satellites never call Stripe at request time — they check the local entitlements table cache. Stripe → webhook → entitlements → satellite cache → satellites is one-way + lets every satellite respond instantly while keeping Stripe as the single billing system. Pattern A as default for clean per-bundle accounting + entitlements re-derive cleanly from one Price. Pattern B rejected for coupon-stack risk + analytics complexity. Charter Member multiplicative compatibility ensures the "best pricing permanently" promise survives bundle conversion. Lifecycle signal triggers prevent bundling becoming sales-pressure; offers surface only when data says customer is READY. |
| **D-027** | **Family API integration mesh locked — Seven16 family products integrate through shared service layers (Command CRM + Email API + Stripe + Survey), never point-to-point.** **First committed integrations** per Master O 2026-05-25: DotIntel + Seven16Survey both push customer state to **Seven16 Command CRM** (`seven16command.com`) via `POST /api/events/ingest` and consume **Seven16 Email API** (`api.seven16email.com`). **Canonical wire contract LOCKED** at `seven16-command-center/docs/integrations/family-integration-contract.md` with per-satellite enumeration of event types + pull-enrichment endpoint schemas. **8 design rules** (slug-based identification / shared env-var naming / per-satellite Bearer keys / NO point-to-point inter-satellite calls / idempotent writes / webhook-driven over polling / entitlements table authoritative / observability mandatory). **8 locked best practices** (auth: per-satellite Bearer keys / source-of-truth: Command IS system-of-record / customer-ID: stable UUID generated by Command at first cross-product touch / webhook delivery: Inngest recommended / sandbox-vs-prod: separate endpoints / Bind Lab Academy cert-completion: one event with `entitlement_scope` array + Command fans out / idempotency key format: satellite-supplied stable hash / webhook signing: HMAC-SHA256 with per-satellite shared secret). Per family memory `reference_family_api_integration_mesh.md`. | 2026-05-25 | Same hub-and-spoke pattern already proven by Stripe billing (D-026) + Seven16 Email layers. NO point-to-point keeps integration graph hub-and-spoke (not fully-connected mesh that becomes unmaintainable as family grows from 6 to 9 products). Command as system-of-record means customer-support questions + BI questions have ONE answer; any other split creates "which one is right?" arguments. Slug-based identification + per-satellite Bearer keys = revocable + auditable + simpler than OAuth for first-party integrations. Idempotent writes prevent retry-induced double-writes that would corrupt customer state. Webhook-driven over polling because state changes don't fit polling cadence; central Inngest retry-with-backoff infrastructure gives 100% delivery vs per-satellite handler reinventing the wheel. HMAC signing prevents webhook spoofing. 8 best practices locked recommended-with-override so satellites can wire against concrete defaults without waiting for additional planning sessions. |
| **D-026** | **Stripe billing architecture — Seven16 Group LLC parent doctrine.** ONE Stripe account for the parent Wyoming LLC; multiple Products / Prices / entitlements; **NEVER per-product Stripe accounts**. Stripe = payment processor only; the `entitlements` table is the authorization source (satellites check entitlements at request time, not Stripe). **Canonical product_slug list:** `agencysignal` / `dotintel` / `dotcarriers` / `dotagencies` / `bindlab` / `bindlabacademy` / `seven16support` / `seven16email` / `growthion` / `seven16survey` / `seven16bundle` (synthetic bundle Product per D-028). Required **checkout-metadata schema** (parent_company / product_slug / source_domain / organization_id / user_id / plan_key / environment). **Shared billing schema** in saas-agency-database Supabase: `stripe_customers` + `stripe_products` + `stripe_prices` + `stripe_subscriptions` + `entitlements` + `billing_events`. **One central webhook handler** at `/api/stripe/webhook` in saas-agency-database (NOT per-satellite). **Env-var convention:** `STRIPE_<PRODUCT_SLUG>_<PLAN_KEY>_PRICE_ID`. **16-step live-mode cutover checklist** (gated on Wyoming LLC formation + FEIN + business bank account). Inherits + amends D-014 / D-015 / D-021 / D-022. Per family memory `reference_stripe_billing_architecture.md`. | 2026-05-24 | One LLC = one FEIN = one bank account = one Stripe account is the cleanest operating model for a multi-product family pre-revenue. Software creates product separation through metadata + Products + Prices + entitlements + reporting, not through a mess of separate Stripe accounts. **Why slug-based:** product names + customer-facing URLs may change (e.g., Bind Lab two-word rebrand per D-032) — slugs in code/metadata/env-vars stay stable across renames. **Why entitlements table is authorization source (not Stripe):** Stripe is a billing API, not a permissions API; checking Stripe on every request would be slow + brittle. The entitlements table is the satellite's local cache, updated by webhook on Subscription changes, queried at request time in microseconds. **Why one central webhook handler:** Stripe events are noisy + cross-product (a customer cancels Agency Signal + Bind Lab in the same Subscription update); per-satellite handlers would race-condition each other on the shared entitlements table. Central handler enforces the canonical resolve. **Why the 16-step cutover gates on legal formation:** live Stripe requires real LLC formation + FEIN + bank account. |
| **D-023** | **Agency Signal positioning lock — "Distribution intelligence for commercial insurance."** Product taxonomy locked at **7 pillars**: (1) **Agency Directory** — search + profile + filter + save; (2) **Producer Intelligence** — segment by title, agency, geography, vertical; (3) **Carrier Appointment Intelligence** — agency × carrier × LOB × state × vertical relationships (264k rows live); (4) **Vertical / Segment Intelligence** — 12 verticals shipped, 8 still empty per BACKLOG; (5) **Build Lists / Saved Lists** — the self-serve monetization engine; (6) **Distribution Expander** — Enterprise+ second-ICP per D-015; (7) **Data Quality / Hygiene** — canary scrub + dedupe + Neilson denylist + email denylist + audit log + verified/claimed profile flow (queued). Adjacent competitive references: **Neilson Marketing** (primary anchor per D-015, $25k all-50 → $12,500 ceiling = 50% undercut) + **ProgramBusiness** (validates searchable market discovery as a parked-for-now Tier 1.x direction — featured-program/storefront UX patterns to borrow when saved-list intelligence matures). Agency Signal is NOT: a CRM, an AMS, a DOT risk-scoring platform (DOT Intel's lane), a generic list broker (ZoomInfo/Apollo), a Neilson replacement at consumer tier (only Enterprise+ slider competes), a full ProgramBusiness market clone. **Verified / Claimed Profile** added to Tier 1.x roadmap (queued, not active): agencies claim + correct their own data → inbound SEO + trust + future monetization. **Scope discipline:** architect's 12 proposed new files (5 strategy + 7 domain + 1 ADR) compressed to this DECISION_LOG entry + the existing `docs/agency-signal-status.html` + one new memory file (`project_agency_signal_positioning.md`) — preserves family "~5 files per slice" budget. Architect's proposed 6 new tables NOT created this turn (3 duplicate existing schema: `agencies`/`contacts`/`agency_carriers` already exist at 33k/135k/264k rows; the 3 genuinely new ones — `saved_list_snapshots`/`changes`/`distribution_expander_segments` — belong in their own implementation session). | 2026-05-18 | "Multi-tenant directory" undersells; "ZoomInfo for insurance" overlaps; "Neilson replacement" sounds commercially risky at consumer tier. "Distribution intelligence for commercial insurance" is the wedge that survives a 30-second VP-of-Distribution explanation and frames AS as an intelligence layer rather than a data list. The 7-pillar taxonomy gives the product a coherent customer-facing surface map for both the $19 producer wedge and the Enterprise+ Distribution Expander sales motion — replaces our previous internal/operational 5-domain framing which mixed customer-facing pillars (search, build-list) with infrastructure concerns (Stripe wiring, family hub doc layer). Neilson + ProgramBusiness as named adjacent references protects against scope drift back toward generic list-broker positioning AND establishes ProgramBusiness as the long-term anchor for the parked market-discovery direction once saved-list intelligence + data trust mature. Ratifies ~80% of existing family doctrine (D-002 / D-011 / D-014 / D-015 / D-018 / D-021) with a sharpened positioning lead; the remaining 20% is the 7-pillar refactor + verified-profile-flow + ProgramBusiness as a named reference. Source: architect strategy review 2026-05-18. **AS does NOT intersect with products outside the Seven16 family architecture** — AMS/CRM/submissions/quote-bind/claims/policy admin all live elsewhere per D-022. ADR-023 captures the AS product-scope statements without naming or tracking external products. |
| **D-022** | **Threshold IQ spun out of Seven16 family — focus consolidated on DOT Intel cluster + Agency Signal.** Family-active products reduce to two surfaces: **DOT Intel cluster** (dotintel2 repo, 3 brand domains: dotintel.io + dotcarriers.io + dotagencies.io + Learning Center) and **Agency Signal** (saas-agency-database repo, directory.seven16group.com) + the seven16-platform control plane (Supabase only, no app yet). **TIQ-specific decisions D-009 / D-019 / D-020 preserved per Rule 4 but no longer active.** `PRICING_THRESHOLD_IQ.md` archived (banner added, file kept). Charter Member offer (D-018 amended by D-021) preserved as **6-surface** offer (was 7): universal credits +40% bonus, DOT Alerts 25% off, directory listings 25% off, lead downloads on bonus-credit math, Learning Center 25% off, Charter badge recognition. Compound savings example on `directory.seven16group.com/charter` rebuilt with DOT-heavy 15-truck operator scenario (commit `fe57bce`): baseline drops from $5,580/yr to ~$1,512/yr; heavier-usage scaling to $4,000–$7,000/yr. **2027 Bundle D-005** ($179/mo Seven16 Intelligence = DOT Intel Business + Agency Signal Growth) **unchanged — bundle never included TIQ. D-021 universal credit currency unchanged** — credits span DOT Intel + Agency Signal + DOTCarriers + alerts; TIQ was per-org license, not credit-based. **Cleanup actions deferred to a follow-up session** ("TIQ wind-down"): Vercel project `prj_c6kzFEhpw6Uwb12TECUidKlBxOwr` deletion, GitHub `gtminsightlab-cmd/seven16-distribution` archive (preserves git history for reference), Supabase `yyuchyzmzzwbfoovsskz` pause/delete, Cloudflare `thresholdiq.io` DNS decision, Stripe sandbox cleanup, Desktop launcher `Open Claude — Threshold IQ.bat` removal. Master O continues building a CRM/AMS shape product **separately, outside the Seven16 family architecture** — different directory (not under `C:\Users\GTMin\Projects\`), not registered in FAMILY_HEALTH.md or PRODUCT_REPO_INDEX.md. | 2026-05-15 | Three reasons make this the right call: (1) **Focus.** DOT Intel + Agency Signal are the revenue-adjacent products with active customer-discovery paths (per D-017f, DOT Intel customer-discovery commitment after CSA SODA ingest closes; per Agency Signal STATE.md §5, Stripe live-cutover trigger is first paying customer). TIQ was further from revenue; deprioritizing concentrates effort. (2) **Architectural cleanliness.** Every cross-product cascade has had to touch TIQ docs as well, doubling the coordination overhead. Removing TIQ from the family cuts cascade work ~30% and simplifies the Rule 2(b) cross-repo prep artifact pattern (now saas-agency-database ↔ dotintel2 only, not three-way). (3) **Separation of concerns.** Master O continues building the CRM/AMS shape separately, which keeps the work moving without the family-doctrine overhead that wasn't earning its keep for that specific product shape. Preserves Rule 4 by NOT deleting D-009/D-019/D-020 (decisions stay in record even when products leave); follows the same pattern as §4 retired-brand register (Agency Vantage — note: §4 BindLab status amended 2026-05-22 to family-active, no longer retired). The Charter Member offer's drop from 7 surfaces to 6 is honest reduction — anti-slop math on /charter reflects the new reality rather than inflating to maintain a "$5,000+/yr" headline that the 6-surface offer doesn't naturally deliver at the 15-truck operator scale. |

---

## 2. Pricing (every consumer tier under $500 P-card threshold)

| Product | Free | Mission tier | Mid | Enterprise |
|---|---|---|---|---|
| **DOT Intel** | Free | $29 Pro | $149 Business | $499+ Enterprise |
| **Agency Signal** | Free | **$19 Producer** | $99 Growth | $399+ Enterprise |
| **Bundle 2027** | — | — | $179/mo Seven16 Intelligence (DOT Intel Business + Agency Signal Growth) | — |

**Adjacent product pricing:**
- **Growtheon:** $97/mo flat white-label CRM (GHL competitor). Internal use first; reseller / white-label margin model unresolved (open question).
- **Coaching layer:** $47 PDF / $97 course / $199/mo cohort.

**Decision: pricing copy on the live Agency Signal site is placeholder.** Don't push revisions to the Snapshot vs. Growth Member gap, "100,000 producers" claim, or Free Trial structure until data inventory catches up. Master O is loading data incrementally and will revise marketing claims to match.

---

## 3. Trust copy + Hygiene Credit (Agency Signal — held for rollout)

These were locked 2026-04-24/25 but are NOT yet shipped to the live site. Roll out when data inventory catches up to the claims.

**Enrichment-sourcing language (use generic framing, never name vendors):**
- "Two of the top 10 B2B intelligence platforms" — not "data brokers" (regulatory connotation).
- "LinkedIn data via licensed enrichment partners" — never "we sync with LinkedIn".
- Optional anchor: "the same $15k/year tools enterprise sales teams pay for".

**AI discovery language:**
- ❌ "Twin AI agents"
- ✅ **"Dual-Agent Verification Pipeline"** (one agent discovers, the other verifies)

**Refresh cadence:** 2× per year, paired with Hygiene Credit below to offset the gap vs. competitors' monthly/45-day cadences.

**Hygiene Credit mechanics (Option A):**
- Auto-discount month 6 and month 12 of Growth Member by 10% ($89.10 instead of $99).
- Stacks with any promotional discount — always applied, never suppressed.
- **Marketed as loyalty reward, not data-decay compensation.** "Our way of rewarding Members who stay through a full refresh cycle." Do NOT frame as "data might be stale, here's a refund."
- Stripe wiring: Subscription Schedule with phased pricing OR programmatic coupon at month 6 and 12. **NOT YET WIRED.**

---

## 4. Retired and parked brands (do not reference in current build)

| Brand | Status | Future plan |
|---|---|---|
| **BindLab** | **Family-active under Seven16 Group holding company** (amended 2026-05-22 — was previously framed here as "retired"). Per SESSION_26 close clarification: Seven16 Group is the holding company owning **Agency Signal + BindLab + DotIntel** as three sibling products. BindLab is functionally separate from Agency Signal but lives in the same family. **Repo / Supabase project / domain status TBD** — when BindLab gets its first concrete build session, register it in `docs/context/PRODUCT_REPO_INDEX.md` + `docs/context/FAMILY_HEALTH.md`. Until then, treat BindLab as a roadmap-active brand, NOT a retired one. The earlier "sales development + coaching brand" framing remains a possible product shape but is not the locked direction. |
| **Agency Vantage** | Retired now | Will reprise later. Specific scope TBD. |

**What carried forward (already absorbed into current build, no need to reference originals):**
- BindLab multi-entity hierarchy → Supabase shared-org architecture.
- Agency Vantage agency development framework → Agency Signal + coaching layer.
- BSB pipeline recruitment automation → Seven16Recruit.

---

## 5. Cancelled work — do not reopen

When Master O says "skip that" / "take off the to-do list", it's closed scope, not deferred. The list:

- **Carolina Casualty (75+ PU) distribution channels** — cancelled session 11.
- **Berkley Prime extension work** — cancelled session 11.
- **Wiring ISC (Integrated Specialty Coverages) to Berkley carriers** — cancelled session 11. ISC is loaded as agency_wholesaler with NO Berkley appointments. Don't add them.

(See `feedback_cancelled_means_closed.md` in memory for the rule.)

---

## 6. Standing rules from Master O

These are working rules, not point-in-time decisions. Check before doing things they cover.

| Rule | Source memory | Applies when |
|---|---|---|
| **Plugins-first, escalate-last.** Use Supabase / Vercel / Stripe / Cloudflare-via-WebFetch / Bash MCPs before asking Master O. He's the last resort. | `feedback_operating_context.md` | Every session, every task |
| **Explain like he's 5.** Numbered steps, exact button names, paste-ready commands, predict the next prompt. | `feedback_explain_like_5.md` | Whenever Master O has to click / type something himself |
| **Exhaustive handoffs.** Chronological log + migrations + commits + blocked items + identifiers + lessons + opening move. Long memory cost is small; thin handoff cost is real human time. | `feedback_handoff_quality.md` | End of every session involving migrations / commits / debugging |
| **Canary filter + dedupe on every external data load.** Vendor files have decoys. Ask Master O for canary patterns before guessing. Dry-run + report stats before loading. | `feedback_canaries_and_dedupe.md` | Any new vendor xlsx / csv / external feed |
| **Cancelled = closed scope.** Delete from task list, document under "Do not reopen" in handoff. | `feedback_cancelled_means_closed.md` | When Master O says "skip that" |
| **Carrier name lookups use fuzzy/strong match,** not exact. pg_trgm normalized lowercase, tolerate Inc/Corp/Co suffix variants. Internal/programmatic linking by carrier_id is fine. | `feedback_carrier_search_strong_match.md` | Any user-facing carrier search / autocomplete / filter / picker |
| **Pricing copy is placeholder until data inventory catches up.** Don't propose revisions to scale claims (100k producers, $12.5k National Database, 70% email density) or pricing tiers based on data scale. | `feedback_pricing_and_data_strategy.md` | Any marketing copy revision touching scale numbers |
| **OneDrive atomic-write workflow.** Build files >5KB in `/tmp` via bash, `cp` atomically, verify with `wc -c` + `md5sum`. NEVER stream-Edit a >10KB file in OneDrive. | `feedback_onedrive_atomic_writes.md` | Any file write/edit larger than ~5KB into the OneDrive folder |
| **Sandbox doesn't retain GitHub credentials.** Each session needs a fresh PAT. Push from `/tmp/repo-push` clone, never from OneDrive. | `feedback_sandbox_no_github_creds.md` | Every push to GitHub |
| **OneDrive `.git` is permanently broken.** No alternate working clone exists. Don't go hunting for one. | `reference_git_repo_state.md` | Any task involving git in the OneDrive folder |
| **Always recommend next path as CTO/PM.** At every decision point, lead with an opinionated SaaS-best-practice recommendation framed via dependency order / longest-lead-time / revenue-adjacency / reversibility / foundation-first. Don't hand back a flat menu of options. (Logged in `dotintel2/docs/SESSION_26_HANDOFF.md` as a local "D-015" — promoted to family standing rule here on 2026-05-14, not a numbered D-decision.) | `feedback_always_recommend_next_path.md` | Every "where should we start?" / "what's next?" / "which of these first?" moment |
| **No paid third-party data services until DOT Intel is revenue-generating.** Free FMCSA + USPS Web Tools only. Defer Twilio Lookup, paid VIN history, commercial address validators to a post-revenue backlog. (Re-confirmed by dotintel2 SESSION_26_HANDOFF as local "D-014"; promoted to family standing rule here on 2026-05-14.) | `feedback_no_paid_services_until_revenue.md` | Any task that would commit budget to a third-party data API |
| **Every screen meets the Front-End Production Standard (D-024).** Loading / empty / error / retry / success / mobile / keyboard / screen reader / partial data / slow connection / user-leaving-mid-request / malformed-response handling — all required before "done." Apply-on-touch: existing screens hit the bar when next edited; new screens hit the bar before merge. Full 10-standard / 12-point doctrine in `ENGINEERING_DOCTRINE.md` §"Front-end production standard (D-024)." Toast lib `sonner`; a11y lint `eslint-plugin-jsx-a11y`; shared primitives at `components/ui/*` per repo. Locked 2026-05-18. | `feedback_frontend_production_standard.md` | Any screen, workflow, or component edit |

---

## 7. Architectural decisions (Agency Signal, in production)

| Decision | Date | Detail |
|---|---|---|
| Hosting on subdomain | 2026-04-23 | `directory.seven16group.com` until cutover to `agencysignal.co`. Apex reserved for marketing. Cloudflare CNAME `directory` → Vercel, DNS-only (grey cloud). |
| Multi-tenant from day one | 2026-04-23 | All tables RLS-enabled, scoped by `current_tenant_id()`, super-admin bypass. Default tenant `Seven16 Group` (id `ce52fe1e-aac7-4eee-8712-77e71e2837ce`). |
| Multi-seat invitations | 2026-04-26 (session 9, migration 0055) | Paid plans get 3 seats (owner + 2 invitees). Free = 1. Auto-flip on auth via trigger. |
| Stripe sandbox first, live cutover later | 2026-04-26 | Sandbox account `acct_1TLUF6HmqSDkUoqw`. Live cutover trigger: first paying customer ready to convert. |
| 13 admin modules dark-themed | 2026-04-25 (session 6+7) | Admin uses `#0F172A`/`#111827` dark palette; rest of app stays light. 13-module sidebar layout. |
| Vertical Manager — 4 + 8 verticals | 2026-04-25 | 4 original (Transportation, Healthcare & Human Services, Construction, Agriculture) + 8 added migration 0051. Per-vertical carrier mapping, score weights, materialized view. |
| Trucking-segment badges | 2026-04-26 (migration 0079) | Per-vertical detail page tags carriers as Non-fleet specialist / Mid-market / Mid-fleet / Large-fleet / Specialty E&S / Unsegmented. Empirically derived from 1,419-row Master Trucking DB. |
| Carrier search hits all 1,363 active carriers | 2026-04-26 (migration 0069) | Empty search → ≥150 agency_count default view. Active search → all carriers including the long tail. Fixes "Canal not findable" gap. |
| V5 grain mismatch — no v5_signals migration | 2026-04-26 (migrations 0057-0060) | V5 is parent grain; Supabase agencies are branch grain. Original V5_DATA_INTEGRATION roadmap superseded. Three options documented; recommended path = cluster Supabase branches into synthetic parent rows. |
| Berkley operating-units + 13 wholesalers | 2026-04-27 (migration 0082) | Power-unit bands: 1-14 PU non-fleet → Berkley Small Business; 15-75 PU mid-fleet → Berkley Prime; 1-100+ umbrella → Berkley National; 75+ PU large-fleet → Carolina Casualty (distribution NOT wired — cancelled). |

---

## 8. Architectural decisions (DOT Intel rebuild — pre-build)

Greenfield. Most decisions deferred until Phase 2 build kickoff. What's already locked:

| Decision | Detail |
|---|---|
| Same domain, new bones | dotintel.io stays. Old project (Supabase `vbhlacdrcqdqnvftqtin`) becomes reference-only. |
| Shared Seven16 stack | Vercel + GitHub + Supabase shared-org + Stripe + Cloudflare + Doppler + Sentry + 1Password. |
| Day-1 working-clone fix | Clone outside OneDrive from minute zero (`C:\Users\GTMin\Projects\dot-intel\`). `gh auth login` once. Don't repeat the OneDrive .git mistake. |
| BDM pre-call brief in scope | Prompt 6 Objection #5 says it's the DOT Intel adoption driver. Must be in September launch. (See open question #4 — surface when starting feature spec.) |
| Bootstrap doc location | `docs/spinoffs/dot-carrier-intel/BOOTSTRAP.md` in the Agency Signal repo until DOT Intel has its own repo. |

**Deferred until Phase 2 kickoff:**
- New repo + new Supabase project, or new product surface inside an existing project? (Recommendation in BOOTSTRAP.md §1.5: new repo + new Supabase, but pull FMCSA SAFER via shared ingestion if available.)
- Three-ICP scope: insurance pros ($30-99/mo) / DOT carriers self-service ($10-20/mo) / lead-gen for small trucking ($TBD).

---

## 9. Open questions — surface when contextually relevant

These are tracked but parked. Master O doesn't want them all raised at once.

1. **Directory domain strategy** — DOT Carrier Directory and Agency Marketplace Directory: subdomains under `dotintel.io` / `agencysignal.co`, or new TLDs? *Surface when:* building/naming the directory products (Phase 3, mid-Oct 2026).
2. **Growtheon margin model** — reseller/affiliate vs. white-label control? Pricing structure and margin TBD. *Surface when:* building Growtheon offer pages.
3. **Seven16Recruit stealth duration** — gated on attorney review of W-2 employment agreement. Has the attorney been engaged? *Surface when:* any public-facing Recruit work.
4. **BDM pre-call brief in DOT Intel Phase 2 spec** — this is the adoption driver per Prompt 6. *Surface when:* kicking off DOT Intel rebuild feature spec.

---

## 10. How to add to this log

When a new decision lands, append to the right section with:
- **What** the decision is (one line, written like a verdict).
- **When** it was made (date).
- **Why** Master O made it (one line — protects future-you from undoing it).
- If it supersedes a prior decision, **strike through** the old one and link to this entry.

Don't rewrite history. The log gets fuller, not cleaner.

— end of DECISION_LOG —
