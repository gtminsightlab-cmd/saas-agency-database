// Deprecated: replaced by components/app/sidebar.tsx (authed) and components/marketing/nav.tsx (public).
// Kept as a placeholder so any stale imports don't break the build.
export function Nav() {
  return null;
}
